/-
# Rule30.Proofs

Empty on purpose. In the real project the dispatcher appends one import
here per closed node; here nothing closes, and the fixture proofs
`verify_test` writes under `Rule30/Proofs/` are built by module name and
deleted again, never imported.
-/

{"ts":"2026-09-09T17:52:39Z","kind":"sent","raw":"Prove `leftDiagonal_onset_le_of_stepMod_preperiod` in `Rule30/Proofs/LeftDiagonalOnsetLeOfStepModPreperiod.lean`. The statement to prove, exactly:\n\n```lean\ntheorem leftDiagonal_onset_le_of_stepMod_preperiod\n    (H : ∀ k x : ℕ, x < 2 ^ (k + 1) → ∃ p > 0, ∀ t ≥ 2 * k,\n      (fun r => ((4 * r) ^^^ ((2 * r) ||| r)) % 2 ^ (k + 1))^[t + p] x\n        = (fun r => ((4 * r) ^^^ ((2 * r) ||| r)) % 2 ^ (k + 1))^[t] x) :\n    ∀ k, ∃ p > 0, ∃ N ≤ k, PeriodicFrom (leftDiagonal k) p N :=\n```\n\nDescription: The onset wall reduced to a preperiod bound for a finite map, with no cellular automaton in the hypothesis. If for every k and every start x < 2^(k+1) the orbit of x under r -> (4*r XOR (2*r OR r)) mod 2^(k+1) is repeating from step 2k on, then every left diagonal has settled by its own index. The hypothesis is a statement about the integers mod 2^(k+1) and nothing else. It is stronger than needed -- only the start 1 is used -- and is stated in the strong form because that is what the survey measures: the kernel confirms every start at every width to 12, and a survey to width 18 finds the period never above 4. Proved by Cadence at leftDiagonal_onset_le attempt 6, which abandoned the wall itself for the sixth time; re-verified by the captain with axioms exactly the three permitted.. Estimated size: M. Start by creating the file with the imports you need (`import Rule30.Basic`, plus any served module), then iterate with `lake build Rule30.Proofs.LeftDiagonalOnsetLeOfStepModPreperiod`. If `Rule30/Proofs/LeftDiagonalOnsetLeOfStepModPreperiod.lean` already exists it is a previous attempt's work on this node: read it first and keep whatever builds."}

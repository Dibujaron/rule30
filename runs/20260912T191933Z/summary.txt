
connector  Vernier
model      opus
problem    centerColumn_other_isEventuallyPeriodic_of_center
vantage    EPSILON-BIASED SAMPLE SPACES AND SMALL-BIAS GENERATORS, READ BACKWARDS -- Naor-Naor, Alon-Goldreich-Hastad-Peralta, and the balanced-code literature -- aimed at PRIZE 2. Handed over by Rosetta at the end of its row-marginal sighting of this evening, as the vantage it could not reach because it had no fetched primary source for the duality and marked it UNVERIFIED rather than lean on it. THE CLAIM THAT MAKES IT A VANTAGE, and your first job is to FETCH IT OR MARK IT: Rosetta section 3.3 argues that a proof of row balance is a proof that the reachable set of rule 30 rows is an EPSILON-BALANCED CODE. If that duality holds, then small-bias generator theory is the one literature in which this explicit set has all weights near n/2 is A THEOREM rather than a measurement -- that field builds such sets on purpose, from algebraic constructions, and knows exactly which properties force balance. THE QUESTION TO CARRY: which of that field sufficient conditions for small bias does rule 30 reachable set FAIL, and why? A named failed hypothesis is a REASON for the failure, where this project currently has only the measurement. That is the deliverable and a negative naming the failed condition is a success. WHAT IS ALREADY KNOWN ABOUT THE REACHABLE SET, all of it to check rather than accept, all of it from sessions today. Sextant 4^-t obstruction: the set of rows at depth t reachable from a finite configuration is a regular language with a computable automaton at each t, and is a subset of F_2^(2t+1) of density 4^-t; rule 30 is pre-injective so the map from a finite configuration to row t is injective. Rosetta this evening: at the prize own parameters the reachable set degenerates to A SINGLE POINT, which is why the positive-density additive-combinatorics route died -- so be careful WHICH t and WHICH parameter regime any hypothesis is being asked about, because that distinction is exactly what killed the previous session. Rosetta also proves an exact fence: all Fourier mass of the relevant object sits on the LEFT CONE BAND, bits 0 to 2t. Portage and Parallax between them fenced the whole measure-theoretic quadrant with one sentence worth carrying -- the field owns the ARRAY and the prize owns a DIAGONAL of it -- and the reason this vantage escapes that is the same reason the row-marginal one did: a row is an element of a group and a code is a subset of a group, so the object here is the one the field actually owns. WHAT THE BOARD FENCES FOR P2: Prize 2 is centerColumn_density_tendsto_half_iff_excess_nat, closed; the best bound is centerColumnCount_ge_of_pow at N - 2 log base 5 of N, which is N(1-o(1)) where the prize needs o(N); Talus caps the whole RUN route at N(1 - 1/(c log N)) for any run bound however sharp; Chorobates shows normality at block length 1 IS Prize 2. NOT WORTH A SESSION: anything needing a measure on the column; anything needing the column to have LOW complexity, since it is measured maximal at p(n) = 2^n to n = 14; anything downstream of Hochman-Shmerkin, priced at zero by four independent fences this evening. IF THE DUALITY DOES NOT HOLD, come back at hour one and say so in a paragraph rather than at hour five with a document -- I paid 17.71 dollars today for a session whose question my own obstruction entry had already closed in bold, and I would rather buy a short true negative. TREAT EVERY INHERITED CLAIM IN THIS BRIEF AS A CLAIM WITH AN AUTHOR RATHER THAN AS A FACT, mine included. Craft: PDFs and publisher pages mostly will not fetch from a guarded session, 403s and undecodable binary are the norm, arXiv abstract pages usually work where the PDF does not, a search summary is not a quote, and prefer computing to fetching wherever the object is finite -- the reachable set is finite at each t and this project has an automaton for it.
sighting   c:/Users/dibuj/dev/rule30/docs/connections/2026-09-12-epsilon-biased-sample-spaces-and-small-bias-generators-read-backwards-naor-naor-alon-goldreich-hastad-peralta-and-the-ba.md
document   exists, 43889 bytes
ended      finished — the connector reported its sighting written; that is its claim about the document, and a captain's reading is the only adjudication
ceilings   600 turns, $80.0
cost       $8.92
turns      44
session    0f14c3b8-8c9a-4fe1-b0d6-50c3e5a449df
log        c:/Users/dibuj/dev/rule30/runs/20260912T191933Z/connector-1

The stated duality fails on linearity and I said so at hour one; read backwards the reachable set is a sample space whose bias is exactly the cone band, and the field's condition rule 30 fails has a name (resiliency), a mechanism (one-sided permutivity), and a reason (Siegenthaler at n=3).

next vantage, in the connector's words:
**The theory of Boolean functions and their *restrictions* — decision-tree and random-restriction complexity, Håstad's switching lemma, the Ajtai–Linial / Kahn–Kalai–Linial line on influences — read as a theory of what conditioning does to bias.** This session's one real finding is that the prize's cone hypothesis is a *restriction* of the row generator's input, that it kills the single coordinate carrying all the resiliency, and that the bias afterwards does not decay: `0.101` at `t = 22` and `0.493` at `t = 23`, against a random-function null of `2⋅10^{-4}`. That is precisely the shape the restriction literature owns — how a Boolean function's Fourier weight redistributes when some inputs are fixed, which coordinates carry the influence afterwards, and when a restricted function simplifies. The measured object is already built and exact: the centre cell as an explicit Boolean function of `t` variables, whose algebraic degree I measured running `0.8t` (reaching `18` at `t = 22`) and whose ANF density is `0.01`–`0.16` — sparse and high-degree at once, which is an unusual combination the field has names for, and which no session here has looked at. The specific question a connector should carry is **influences**, and I ran the two-line script rather than hand it on as a guess (`explorer/vernier2_influence.mjs`, exhaustive over all `2^t` members for `t = 4..22`). Three things came out, and the third is the one I would carry:

- The surviving first-order correlation is **never** on an outer cell. The largest singleton Walsh coefficient sits at configuration cell `1, 2, 3, 4` or `5` at every `t` from `5` to `22` — always within five of the origin, never within fourteen of the cone's outer end — and it *hops* among them rather than staying put (`1,1,1,4,3,2,3,1,1,4,4,1,2,3,2,5,1,3` for `t = 5..22`).
- The **influence profile** is a random function's on the inside and dead on the outside. At `t = 22` the per-cell influences read `.505 .449 .543 .494 .475 .455 .420 .337 .258 .197 .131 .104 .037 .025 .002 .002 0 0 0 0 0 0`: the first seven cells sit at `≈ 1/2`, which is exactly what a random balanced function gives, and the rest decay to zero. Total influence `4.44` against a random function's `t/2 = 11` — **below** the null, and below it entirely because the outer half contributes nothing rather than because the inner half is structured. (My first draft of this bullet called `4.44` "roughly `0.2t`, between the `√t` and `t` nulls"; that is wrong twice over — the null for a balanced function is `t/2`, and the profile is not a single rate.)
- **The effective cone is strictly smaller than the light cone, and measurably so.** At `t = 22` the configuration cells `17…22` have influence *exactly zero*: the centre cell at time `22` does not depend on them at all, in any input, though the light cone permits it. Effective radius `13` at `t = 15` and `16` at `t = 22`, ratios `0.87` and `0.73`, falling. That is the left damage front read as a Boolean influence, and it is the *worst case over all inputs* rather than an average — which is precisely the quantity crystals A3 prices at zero for arbitrary pairs. Whether the worst case over this cone-conditioned family is genuinely below `1` is the question, and this is the first instrument that measures it as a function rather than as a front.

What I could not reach at all, and which sits outside every vantage tried this evening: **the merit-factor and low-autocorrelation-binary-sequence literature**, the one branch of this whole region whose object is a *single explicit sequence* rather than a set — where the Legendre sequence's balance and correlations are proved by Weil sums and the general problem is famously open. It is the only place I found where the field's object has the prize's cardinality, and I ran out of session before building a dictionary for it.

I could not reach the restriction/influence vantage properly from where I stood because I spent the session on the *set* and that is a question about the *function*; the two are the same object read along different axes, and I only saw the second axis at the end, when the ANF degree table came out near-maximal and I had nothing in the field of small-bias sets to do with it.

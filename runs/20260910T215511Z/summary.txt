
connector  Groma
model      opus
problem    centerColumn_other_isEventuallyPeriodic_of_center
vantage    The switch-index sequence over a finite alphabet: does obstruction 9's residual become tractable when it is read as a sequence of integers rather than bits? Dioptra's handoff, and the question it could not reach. THE SETUP, all of it now on the board and kernel-proved: white_run_monotone says that inside one maximal white run of the centre column, column 1 switches white-to-black AT MOST ONCE, so each run carries a single integer -- the switch index -- rather than a word. Obstruction 9 reduces P1 to 'column 1, read at the white times, is eventually periodic', and the board now holds centerColumn_not_isEventuallyPeriodic_of_white_times, a proved sufficient condition of exactly that shape. So the residual is: IS THE SWITCH-INDEX SEQUENCE EVENTUALLY PERIODIC? THE STRUCTURAL POINT: for a periodic boundary b, white runs are bounded by some L_max, so the switch indices live in a FINITE ALPHABET of size L_max + 1. That is a different object from an unbounded one and obstruction 9's analysis did not have it. THE LIMITATION, which must not be lost: this applies to the family X_b, NOT to the seed -- rule 30's own centre column has long runs (16 identical cells at M = 22711) and nothing bounds them. WHAT I WANT: read the symbolic dynamics and combinatorics-on-words literature against this object. Factor complexity, Morse-Hedlund (a sequence over a finite alphabet is eventually periodic iff its factor complexity is eventually constant -- is that a usable criterion here, and what would have to be measured?), automatic and substitutive sequences, return words, the S-adic hierarchy. Specifically: is there any theorem of the form 'a sequence over a finite alphabet arising as the return-time or switch-index statistic of a cellular automaton orbit is not eventually periodic', or any criterion that could be checked? And the calibration Talus established, which applies here: a bad verdict below 10^6 rows is worth nothing, so say what depth any proposed measurement would need. A NEGATIVE IS VALUABLE: if the finite alphabet buys nothing because every criterion in that literature needs the sequence to be generated by a substitution or an automaton and nothing puts the switch indices in either class, say so plainly and say why -- that closes a route this board would otherwise spend sessions on.
sighting   c:/Users/dibuj/dev/rule30/docs/connections/2026-09-10-the-switch-index-sequence-over-a-finite-alphabet-does-obstruction-9-s-residual-become-tractable-when-it-is-read-as-a-seq.md
document   exists, 63663 bytes
ended      finished — the connector reported its sighting written; that is its claim about the document, and a captain's reading is the only adjudication
ceilings   600 turns, $80.0
cost       $31.24
turns      17
session    1b05eb9d-e68c-420e-acc7-cbb7f738283f
log        c:/Users/dibuj/dev/rule30/runs/20260910T215511Z/connector-1

Six-section sighting delivered: the finite alphabet buys nothing for a proof (it is a change of units, and on the hard boundaries the integer and bit readings are the same sequence), every low-complexity structure class is excluded by measurement to 10^7 rows, and the one direction-correct mechanism in CA theory — blocking words — is dead for rule 30 by left-permutivity; the surviving prize is a kernel-checked first-passage identity restating the residual as a statement about the leftmost black cell.

next vantage, in the connector's words:
**Attack from the theory of `ℤ²` subshifts of finite type and their directional/one-sided dynamics — specifically Kari–Moutot's "decidability and periodicity of low complexity tilings" and the algebraic-geometry line of Kari–Szabados — but pointed at the *strip*, not at the picture.** Everything in this document reduced to one question it could not touch: how much can the complexity of column 1 exceed that of column 0? That is a question about the width-2 strip of the space-time diagram, and it is exactly the object the low-complexity-tiling literature is built for; the residual is the statement that a strip whose left column is periodic has a right column that is periodic too, which in that language is a **periodic-decomposition** question about a two-row configuration under an algebraic annihilator. I could not reach it from where I stood because I spent the session on the *one-dimensional* recoding the vantage named, and one dimension is where the periodic-forcing theorems are weakest — there are exactly two, Morse–Hedlund (which needs an upper bound nothing supplies) and equicontinuity (which §3.2 shows rule 30 fails outright). In two dimensions the theorems run the *right* way: they conclude periodicity from low complexity, which is the direction this residual needs, and Kari–Szabados' decomposition theorem ("there exist periodic integral configurations `c₁,…,c_m` such that `c = c₁ + ⋯ + c_m`", <https://arxiv.org/html/1605.05929>) is a conclusion of exactly the residual's shape. The seam that connector will have to break through is the one that killed §4's Nivat entry: rule 30's picture has a cone and an origin and is not shift-invariant, so the hypothesis `P(m,n) ≤ mn` is never satisfied globally — the work is to find the *local* version, over the strip and under the periodic-column hypothesis, and to say honestly whether anything in that literature survives the loss of shift-invariance.

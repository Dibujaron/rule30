
connector  Rosetta
model      opus
problem    centerColumn_other_isEventuallyPeriodic_of_center
vantage    NORMAL NUMBERS AND EXPLICIT CONSTRUCTIONS, AIMED AT PRIZE 2 -- Davenport-Erdos, Bailey-Crandall on the normality of the Stoneham constants, Champernowne and the 2-adic constructions where balance and non-periodicity of an explicitly built expansion are PROVED rather than conjectured. This is Parallax own regretted vantage, named at the end of its arithmetic-dynamics sighting of this afternoon: the one literature that bounds a SINGLE orbit digits without a measure and without a multiplier. THE QUESTION TO CARRY, in Parallax words: what do the constants whose normality IS proved have in common, and which of those properties can rule 30 picture be shown to have? Parallax could not take it because it needs an explicit construction of the digits and rule 30 digits are defined by a recurrence rather than built -- so the FIRST thing to establish is whether that distinction is fatal or merely inconvenient, and to say which, with a reason. WHY THIS AND NOT ANOTHER P1 VANTAGE: every session this project ran today was aimed at Prize 1 and every one came back with a fence. CLAUDE.md tells the captain to spread across the three prizes rather than mine whichever is yielding, and says the least-built region is the least TRIED rather than the least promising. Prize 2 -- the centre column black density tends to one half -- is that region, and Parallax says this literature is the only direction in which that prize has ever been won for a NAMED constant. WHAT THE BOARD ALREADY KNOWS AND YOU SHOULD CHECK RATHER THAN ACCEPT. Prize 2 in the board vocabulary is centerColumn_density_tendsto_half_iff_excess_nat, closed, which says balance holds exactly when for every d the black count is eventually within N/d of N/2. The strongest bound anyone has is centerColumnCount_ge_of_pow, closed today week, giving N - 2 log base 5 of N, which is N(1-o(1)) where the prize needs o(N); Talus attack of 2026-09-10 caps the whole RUN route at N(1 - 1/(c log N)) for any run bound however sharp, so that route is finished. Chorobates 2026-09-12 sighting adds a fence you must not cross: normality at block length 1 IS Prize 2, and finite-state randomness is by Schnorr-Stimm equivalent to Borel normality, so any P1 route through the column is random in sense X needs X strictly below finite-state. Read that fence carefully -- it means a normality result here would BE the prize rather than approach it, which is exactly why the interesting question is about the METHODS of the proofs for Stoneham and Champernowne rather than about whether the centre column is normal. TREAT EVERY INHERITED CLAIM IN THIS BRIEF AS A CLAIM WITH AN AUTHOR RATHER THAN AS A FACT, mine included: two claims I wrote into a brief this morning as settled turned out false when the session checked them anyway. Craft for this region: PDFs and publisher pages mostly will not fetch from a guarded session, 403s and undecodable binary are normal, a search summary is not a quote, prefer computing to fetching wherever the object is finite.
sighting   c:/Users/dibuj/dev/rule30/docs/connections/2026-09-12-normal-numbers-and-explicit-constructions-aimed-at-prize-2-davenport-erdos-bailey-crandall-on-the-normality-of-the-stone.md
document   exists, 39967 bytes
ended      finished — the connector reported its sighting written; that is its claim about the document, and a captain's reading is the only adjudication
ceilings   600 turns, $80.0
cost       $8.31
turns      48
session    26c1a808-6783-4e28-b3f2-0ca0eaabf847
log        c:/Users/dibuj/dev/rule30/runs/20260912T174418Z/connector-1

Written: six sections, five connections, a long section 4 — the normality literature has three proof settings (renormalisation, known block decomposition, explicit algebraic orbit), rule 30's centre column has none of them, and the Stoneham method turns out to be a multiplier method whose orbit is a group of density 2/3 where rule 30's is the transient of a rho.

next vantage, in the connector's words:
**The additive combinatorics of Bernoulli convolutions and self-similar measures — Erdős, Solomyak, Hochman, Shmerkin — attacked at the question "when does a deterministic sum with exponentially separated scales have an absolutely continuous / dimension-one distribution".** The reason is a seam this sighting kept hitting from the wrong side. Everything in §3 failed because rule 30 supplies no *group* and no *measure*, and the normality literature needs one of them. Hochman's inverse theorem for entropy is the one modern result that produces equidistribution-strength conclusions for a **single** explicitly given self-similar object without a group structure and without assuming a measure — it gets dimension from a *non-concentration* hypothesis on the scales, which is a hypothesis one can sometimes verify by computation on an explicit system. The rule 30 object to point it at is not the column but the pair (settled region, transient band): the board has measured exponential separation between the diagonal period regimes (`3, 8, 29, 400, 87867, 2107985255` — each roughly the square of the last), which is exactly the scale structure those theorems eat, and nobody here has asked whether the centre column's empirical block measures satisfy a non-concentration bound at those scales. I could not reach it from where I stood because it needs the block-frequency measures of the column at dyadic scales, which is a computation nobody has run and which the subshift sighting of 2026-09-12 stops just short of.

The one I could not reach at all, and name so the next connector can: **the Cassaigne–Ferenczi–Zamboni line on words of low but super-linear complexity, where balance is proved from a complexity bound alone.** Every mechanism in §3.1 needs structure; that literature needs only a *bound*, and rule 30's column has a measured complexity. If there is a theorem of the form "complexity below `f(n)` forces the letter densities to exist", then the question becomes *how large* the column's complexity is rather than what generates it — which is the first question in this whole region that rule 30's own measurements could plausibly answer. I did not fetch anything in that area and do not know whether such a theorem exists; that is the vantage.


theorist  Talus
model     fable
topic     Prove periodic_polyTime, and nothing else: for every f : Nat -> Bool that is eventually periodic, there is a Turing.TM2ComputableInPolyTime Computability.encodeNat Computability.encodeBool f. No rule 30 appears in this statement anywhere. WHY THIS IS THE SESSION'S WHOLE JOB. Connector Pantograph established on 2026-09-11 (docs/connections/2026-09-11-make-p3-sayable-*.md, and the elaborating sketch explorer/pantograph_scratch_p3.lean) that the uniform machine model docs/prize.md said still had to be built is ALREADY in the pinned Mathlib: Turing.FinTM2 plus the little-endian Computability.encodeNat. P3Core is one line against it and it elaborates. But NON-VACUITY IS NOT ESTABLISHED, and that is the half that matters: periodic_polyTime is stated with sorry and ShortcutExists is an unproved def, so P3Core_implies_P1 rests on sorryAx by #print axioms. Prove periodic_polyTime and that derivation becomes sorry-free, P3 implies P1 inside this repo, and Prize.lean's P3 can be restated against a real model -- which it currently must not be, for exactly this reason. THE CLAIM TO FALSIFY FIRST, in the connector's words: that this is provable at all. Check whether haltList's requirement that the machine restore tm.initialState and empty every non-output stack makes the construction harder than it looks; that is the step the connector expected to cost a day. A NEGATIVE IS A RESULT and closes a route that currently looks open. THE CONSTRUCTION IT NAMES AS WHAT SETTLES IT: a FinTM2 with sigma = ZMod p x ZMod p x Fin (N+1), K = Bool, one label, one step per input digit, time = X + c. It depends on encodeNat being little-endian, so the machine pops the least significant bit first and can carry (n mod p, 2^i mod p) in a finite state. Size guessed L, and the connector guessed it would guess low. DO NOT broaden the topic. Do not adjudicate the DTIME(poly(log n)) versus Omega(n) threshold -- that is the connector's Topic 2 and it wants a complexity theorist, not this session. Do not touch Rule30/Prize.lean or docs/prize.md.
attack    c:/Users/dibuj/dev/rule30/docs/attacks/2026-09-12-prove-periodic-polytime-and-nothing-else-for-every-f-nat-bool-that-is-eventually-periodic-there-is-a-turing-tm2computabl.md
document  exists, 20169 bytes
ended     finished — the theorist reported its attack written; that is its claim about the document, and a captain's reading is the only adjudication
ceilings  600 turns, $80.0
cost      $14.53
turns     63
session   6530f028-b949-4a96-8b03-20c0d36f9b82
log       c:/Users/dibuj/dev/rule30/runs/20260912T002323Z/theorist-1

periodic_polyTime is proved in the kernel with clean axioms, the connector's P3Core_implies_P1 is re-proved without sorry, and two mutant files show the halting-configuration and onset-cap checks bite; the document records all four claims as survivors with the Lean runs behind them.

next topic, in the theorist's words:
The rule-90 witness, as a theorem rather than a script. The connector's litmus needs the model to admit a fast machine for a genuinely aperiodic sequence, and its witness is rule 90's column 1, black exactly at t = 2^j − 1 for j ≥ 1 — checked in explorer/pantograph_shortcut.mjs to 20,000 rows and nowhere in Lean. Rule 90 is already definable on the board (Rule30/Basic.lean defines all 256 rules through `step (r : Fin 256)`), so the topic is two bounded claims: that column 1 of rule 90 from a single black cell is `decide (∃ j ≥ 1, t + 1 = 2^j)`, a Pascal-triangle-mod-2 fact with a short induction, and that a FinTM2 computes it in |input| + 1 steps — the machine is C1's with σ = Bool × Bool ("exhausted?", "all digits so far 1") and needs no ZMod at all. With both, the litmus's shortcut half becomes a statement about an elementary cellular automaton with the same seed as rule 30, and "P3 is not implied by P1" is machine-checked in this repo rather than measured. The threshold question (polylog against Ω(n)) remains the connector's Topic 2 and wants a complexity theorist; and the one row of the model dictionary still argued rather than proved — that an infinite work-stack alphabet cannot smuggle information into a FinTM2 — is worth a short Lean fence before Prize.lean is restated, since a cheating machine there would make P3Core false for a stupid reason exactly as TM0 did.

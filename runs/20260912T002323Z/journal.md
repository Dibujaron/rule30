## Talus on theorist-1 — 2026-09-12T00:42:18Z

Dib — band first: the mathematics is Nothing (an eventually periodic sequence is a finite automaton on the digits of n, folklore), the result is project-internal, and it is done. The topic asked whether `periodic_polyTime` is provable at all, with Mathlib's halting convention flagged as the step likely to cost a day. It is provable and it is proved: `explorer/talus8_scratch_periodic.lean` builds a `FinTM2` with one stack, one label, and a five-component finite state, pops one input digit per step, and halts in |input| + 1 steps; `lake env lean` accepts it with axioms propext, Classical.choice, Quot.sound. The halting convention cost exactly one `load` before `halt` and the choice of a single stack. `explorer/talus8_scratch_p3.lean` is Pantograph's scratch with the sorry replaced: `P3Core_implies_P1` now prints clean axioms, so P3 stated against Mathlib's own model provably implies P1 inside this repo, and the precondition docs/prize.md set for restating Prize.lean's P3 is met (I did not touch Prize.lean or prize.md; that is yours and Rowan's). What died was only my own Lean: the connector's guessed state was two components short, and every lost hour was `rw` refusing to match terms that were definitionally equal but not syntactically. Two mutant files sit beside the proof so the checks are seen to fail: delete the `load` and Lean rejects exactly the halting-configuration lemma; drop the onset cap and the kernel computes the wrong answer on a genuinely eventually periodic sequence. For your TypeScript ear: `TM2ComputableInPolyTime f` is a type whose values are (machine, polynomial, proof-of-correctness-within-the-polynomial) triples, and Mathlib had constructed exactly one value of that type before today, for the identity function. This is the second.


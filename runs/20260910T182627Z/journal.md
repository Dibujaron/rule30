## Seeder on seed-1 — 2026-09-10T18:44:49Z

I read the closed table and the crystals before writing anything, and the first thing I settled was what NOT to do. Both P1 walls are recorded on the board as logically equivalent to Prize 1 rather than reductions of it — Jen's theorem makes their hypothesis and conclusion jointly impossible, so the implication is P1 in a coat. There is nothing under them to decompose and I did not invent something. That left the two diagonal walls, and both say plainly they do not bear on a prize; I have disclaimed that on all five nodes rather than let a reason imply otherwise.

Crystal 62 names the residual: price the early settlings — the levels that settle before their driver's self-loop is cut, which is the whole distance between the truth (slope 1.25) and the reset cascade (2.00) against a budget of 2. I found the rule. On the packed row, bit n+2 of the next row is bit n XOR (bit n+1 OR bit n+2), so if bit n+1 is black in BOTH rows the two ORs are true and bit n+2 inherits from bit n, which already agrees: the front crosses two levels in one row. In diagonal coordinates the same fact reads as one index of onset buying two diagonals, and it follows from two closed lemmas.

I measured before proposing, not after. explorer/seeder_ladder.mjs runs a ladder on this rule plus the reset rule plus crystal 45's white branch, never consulting the truth: level 5001 at index 8486, slope 1.697, and its index never exceeds 2k at any of 5000 levels, where the reset-only ladder runs at 1.989 and crystal 62 refutes it outright at k=119. The new ladder's worst ratio, 1.873, sits at level 118 — the same event that breaks the old one, which is the part that convinced me this is the right rule rather than a nearby one. explorer/seeder_front.mjs adds that every one of 1872 early advances in 4000 levels is zero-cost, so the early settlings are double jumps and not partial savings.

I deliberately did not propose the ladder itself. The previous ladder was sound and unreachable, and its hypothesis shape is what killed it; twelve attempts and $51.80 went into that wall while a captain-voiced sentence pointed at it. A measured slope over 5000 levels is not a proof that the slope stays below 2, and I said so in the disclaims.

The other three: the period wall still has no packed-row interface (the landed one carries an onset hypothesis that wall does not need), so dropping it is one node; the adapter between "one return equation" — which is the form every computation and every stated equivalence on this board produces — and the "∀ t ≥ N" the interface demands, whose absence is why stepMod_preperiod_le_of_le_11 composes with nothing today; and the reduction of crystal 61's universal form to odd starts, which the halving law gives and which explorer/seeder_tails.mjs shows is lossless over all 2^n starts to n = 20.

What I could check, I checked against the artifact. All five routes elaborate under lake env lean, and I verified them the way a captain would land them: a script rebuilds each theorem from the proposal file's own statement bytes plus its route and compiles the result, so the routes are checked against the same bytes the check will use, not against my memory of them. All five come out on [propext, Classical.choice, Quot.sound]. The witness on the packed-row node returns true over T < 200 and n < 40 at shift 16 — real rows well past row 18, reachable only because rowNat is cheap where evolve is not. I have seen this pipeline fail, this session, at the right line: my first route for stepMod_preperiod_of_return was rejected for a rewrite that did not match, and I fixed it rather than reporting around it.

What I could not check: the diagonal double-advance node carries no witness. Its hypotheses are periodicity statements, which no finite range expresses, and leftDiagonal through evolve costs 3^(j+k), so a naive witness cannot reach index 18 where the first transient lives. I said that in the disclaim instead of inventing coverage. And two side confirmations fell out that I did not go looking for: my tail sweep independently reproduces crystal 61's worst ratio (1.381 at n=19) from a fresh implementation, and the longest cycle of the truncated map steps 1 to 2 to 4 at widths 4 and 9 — crystal 57's onset-constant staircase, arrived at from a different direction.


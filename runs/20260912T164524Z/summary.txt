
theorist  Talus
model     opus
topic     RUNG 2 OF THE OCCURRENCE LADDER, which is the p = 2 instance of Prize 1. Claim to falsify: all four words of length two occur in the centre column in every window [a, 4a], for every a >= 2. Source: Ephemeris sighting of this morning, docs/connections/2026-09-12-the-subshift-of-the-centre-column-*.md section 5 Topic 1. WHY IT IS THE RIGHT TARGET: it is the only statement I can find on this board that is at once (i) open, (ii) NOT logically equivalent to a prize -- unlike the two open P1 walls, whose hypothesis is the negation of the goal -- (iii) not vacuous over every function from Nat to Bool, and (iv) measured. THE REDUCTION, which you should check rather than accept: 01 and 10 are free, since rung 1 (the closed centerColumn_window_not_constant) applied at a and again at 4a forces both inside [a, 16a]; so the open content is 00 occurs infinitely often OR 11 does, which is exactly the centre column is not eventually alternating; and with the CLOSED centerColumn_not_eventually_constant that is precisely not exists N, for all n >= N, c(n+2) = c(n), the p = 2 instance of Prize 1. I verified centerColumn_not_eventually_constant is proved on the DAG before writing this. ONE CORRECTION TO EPHEMERIS, which undersells its own rung: section 5 says rung 2 gives the p = 2 instance and NO MORE. By crystal 21 -- a tail of period p has at most p distinct factors of each length, already used on this board to kill periods from factor counts -- four distinct length-2 factors occurring infinitely often force p >= 4, so FULL rung 2 kills every period below 4, and rung L every period below 2^L. That does not make the ladder a route: its limit is every word occurs, which is disjunctivity and at least as hard as the prize, so Ephemeris a rung not a route warning stands. It changes the price of one rung. MEASUREMENT, which I re-ran myself rather than quoting: node explorer/ephemeris2_rung2.mjs, window [a,4a], length 2, exactly one failure at a = 1, holding for every a >= 2 up to 100000. Rungs 3 to 6 also hold, past a = 11, 23, 35, 113, and the window multiplier that clears length L climbs like 2^L. DO NOT SPEND THE SESSION ON THE ALTERNATION / LEFTWARD-PROPAGATION ROUTE. Two independent sessions have hit the same wall there: Ephemeris own section 4.2 calls the nearest version of it a tautology, the rule at the origin read backwards, after an hour; and today P1 seeder node centerColumn_periodic_two_alternating reports that an alternating tail propagates leftward WITHOUT closing -- columns -1 and -2 pinned to a single free bit per pair, cone boundary never reached because the run bounds put the forced block at depth below the time, crystal 69 ratio again -- and that the argument is special to p = 2 and gives nothing at p >= 3. WHERE I WOULD LOOK INSTEAD: the occurrence framing in the run vocabulary. 11 occurs infinitely often is EXACTLY the k = 2 instance of the hypothesis of the closed node centerColumn_not_isEventuallyPeriodic_of_long_black_runs, whose hypothesis is for all k N, exists t >= N, for all s < k, centerColumn (t+s) = true. So the board already owns the top of that ladder (all k implies P1) and rung 2 asks for its bottom rung, in a vocabulary that has closed lemmas in it -- centerColumn_black_run_lt_start, column_alternating_of_black_run, column_black_run_of_alternating. Either colour alone earns the instance; you do not need both. TREAT EVERY INHERITED CLAIM IN THIS BRIEF, INCLUDING MINE, AS A CLAIM WITH AN AUTHOR RATHER THAN AS A FACT -- earlier today I wrote known already, do not re-derive into a brief and two of the things I asserted that way turned out false when the session checked them anyway. BAND EXPECTATION, so nobody is sold anything: novel if proved, and small -- a combinatorial lemma about rule 30. Say plainly in section 1 what band you actually land in.
attack    c:/Users/dibuj/dev/rule30/docs/attacks/2026-09-12-rung-2-of-the-occurrence-ladder-which-is-the-p-2-instance-of-prize-1-claim-to-falsify-all-four-words-of-length-two-occur.md
document  exists, 43918 bytes
ended     finished — the theorist reported its attack written; that is its claim about the document, and a captain's reading is the only adjudication
ceilings  600 turns, $80.0
cost      $35.45
turns     114
session   87ad885f-62bd-4031-96a7-844738a2d8bb
log       c:/Users/dibuj/dev/rule30/runs/20260912T164524Z/theorist-1

The document is complete: rung 2's open half reduces to one exactly-measured linear inequality (C2), the brief's own single-colour recommendation is closed with a measurement, and C2's only mechanism is measured to be a count rather than Condrey's rigidity — so it is well-posed but has no route.

next topic, in the theorist's words:
**Which target words the cone can see: classify `W ↦ f_W(a)` for the short words, with the instrument this session built.** The question I parked here — is C2's extinction rigidity or a count — got answered inside the session (§3.2), and the answer makes this the right successor rather than a consolation. Four targets have now been measured on identical machinery, with four different behaviours: the constant word gives `f ≈ a` and is **rigid** (survivors collapse to exactly `1`, which is Condrey's mechanism); the alternating word gives `f ≤ 3a` and is finite but a **count** (hundreds of survivors, then extinction); `11`-free is **unbounded within reach** (`≥ 47` at `a = 5`); and `00`-free is finite exhaustively (`16, 16, 27, 31`) for a reason nobody has identified, being invisible to the left-half relaxation. That is four points of a classification nobody has drawn, and the classification is what the occurrence ladder actually needs: **rung `L` is reachable exactly for those words the cone can see, so knowing which words those are tells a captain which rungs are worth a session before any of them is attempted.** The concrete task: run `explorer/talus10_no11.mjs`'s word-branching search over *every* target word of length `≤ 4` (as a subshift of finite type on the column, which is what "avoid `w`" is), tabulate `f_W(a)` for `a ≤ 6`, and sort by behaviour. It is one script, the instrument exists and is controlled against two theorems, and the output is a table rather than a hope. Two specific things to look for. First, whether "rigid" coincides with "the target word makes column 1 monotone" — that is Condrey's mechanism and it is the only known source of a route, so the rigid class is exactly the reachable class. Second, the `00`-free anomaly, which is the one entry above whose finiteness has no explanation and which, if it is linear, gives "`00` occurs infinitely often" — a **single-colour** result strictly stronger than everything in this document. The cheap companion, in the same sitting: the alternating maxima sit on plateaus (`8` for `a = 1…4`, `17` for `a = 8…13`, `26` for `a = 16…18`, `36` for `a = 19…26`) with jumps at `a = 5, 6, 8, 14, 15, 16, 19`. Condrey's constant-word maxima have a closed form; these have none that I can see. A closed form would be the strongest possible evidence that a mechanism exists after all and would reopen C2's route; its continued absence out to `a = 34` would be the strongest evidence that the count is the whole story.

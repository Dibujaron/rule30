## Vesper on centerColumn_other_of_cohomologous_column — 2026-09-08T21:28:17Z

Proved centerColumn_other_of_cohomologous_column — the "cohomologous column" lemma Portage handed over. No parked proof existed (only a sorry'd type-check in explorer/portage_scratch_evendriver.lean), so I built it from scratch. Turned out cheap: put the two eventually-periodic hypotheses (the centre column, and its xor with the shifted target column) onto one common period via the already-served isEventuallyPeriodic_common_period, then the centre-column term cancels straight out of the xor equation with a two-line auxiliary lemma. The only real bookkeeping was shifting the resulting fact — which is naturally about evolve(t+j) x — back down by j to land on evolve t x, the statement's actual conclusion; that shift is pure ℕ arithmetic (subtraction bounded by hnN), no ℤ/cast friction at all since x itself never enters any arithmetic. Built clean first try after one false start (an unnecessary `dsimp only` that made no progress and had to come back out). Axioms confirmed standard three by the append-then-remove #print axioms trick.



theorist  Talus
model     opus
topic     Prize 2's residual: find ANY bound on the centre column's excess that uses rule 30. THE MEASUREMENT THAT DEFINES THIS TOPIC: the board has twelve proved P2 nodes and TEN OF THEM ARE TRUE OF ANY BOOL SEQUENCE. Only centerColumn_zero and the stepMod counting dictionary mention rule 30's dynamics at all, and both trivially. So P2 has twelve green nodes and zero facts about rule 30's behaviour, and that is the honest state of the region. What is missing is a single statement about the excess E(N) = 2*count(N) - N that would be FALSE for some Bool sequence -- anything at all that distinguishes rule 30 from a coin. THREE THINGS TO TRY AND FALSIFY. (1) The board proved centerColumn_density_tendsto_half_of_nearby_cuts today: balance need only be checked on a THIN set of N, because the tail between a cut and N cannot move the density. Is there a natural sparse set of cut points that rule 30 SUPPLIES -- the doubling depths 3, 8, 29, 400, 87867, the eventually-white diagonals, the rows where the packed row has a distinguished form -- at which the excess is easier to control than at a general N? That would turn the localisation from a generic lemma into a rule-30 statement. (2) Dioptra's white-time forcing law and the board's black-time law together determine 0.6885 of the centre column from column -1, measured over 200,000 rows. Does that coverage give a bound on the excess -- if most cells are a known function of a neighbouring column, does the count inherit anything? Be careful: a determination is not a distribution, and the obvious argument probably fails; say precisely where. (3) The run structure: a block of 16 identical cells occurs at M = 22711, and the uniform-block formulation is MEASURED DEAD. What is the actual run-length distribution, and is any moment of it provably constrained? FALSIFY HARD and report a negative plainly. If the honest answer is that no bound on the excess is reachable without a genuinely new idea, say so with the reason, because that is worth more than a twelfth generic lemma. Calibration from Talus: a verdict below 10^6 rows is worth nothing.
attack    c:/Users/dibuj/dev/rule30/docs/attacks/2026-09-10-prize-2-s-residual-find-any-bound-on-the-centre-column-s-excess-that-uses-rule-30-the-measurement-that-defines-this-topi.md
document  exists, 35449 bytes
ended     finished — the theorist reported its attack written; that is its claim about the document, and a captain's reading is the only adjudication
ceilings  600 turns, $80.0
cost      $22.22
turns     92
session   9907d5e6-403d-448f-b86d-876e596ca52e
log       c:/Users/dibuj/dev/rule30/runs/20260910T220215Z/theorist-1

Document written with all six sections: all three named routes closed (two by argument, not measurement), the excess measured to 10^7 rows and found inside a fair coin's scatter on every statistic including a 40-draw null, and one surviving claim — a run of the centre column is no longer than its start time, giving the only quantitative rule-30 bound on the excess, |E(N)| <= N - log2 N.

next topic, in the theorist's words:
**The row count `b(t)`: is the row marginal provable where the column marginal is not?** This session's one piece of content is that rule 30's local law constrains a row and not a column. `C3` bounds the triangle's black density by `2/3` in two lines and `C4` gives `b(t+1)` exactly from row `t`'s run structure; **no argument of either shape exists or can exist for the centre column**, because crystal 40 makes the centre column free while the row is not free at all. The row density is A070952, crystal 29 calls it "purely empirical" and says only the cone is provable, and nobody on this board has attacked it — while `centerColumn_run_boundary` makes it P2's sibling marginal exactly: `count(N)` is the number of rows whose run-boundary set contains the origin, and the size of that set in row `t` is `b(t+1)`. Three things make it a better target than a thirteenth P2 lemma. `C4` converts row balance into a statement about `ρ(t)` and `G₂(t)`, finite combinatorics of one row rather than a limit. The inequality is already tight at `t = 1`, so the method has no slack being wasted. And the question has a clean residual with the same shape as everything else here: since `b(t+1) = ρ(t) + 2G₂(t) + 2`, row balance is exactly *"a rule 30 row has about one maximal black run per four cells, and about half its interior gaps are long"* — a statement about the run structure of a single row, which is a finite object, where P2 is a statement about a limit along a direction the rule does not constrain. **One honest caveat, because I nearly wrote the opposite:** the row excess is not anomalous. `|2b(t) − (2t+1)|` reaches `3061` at `t = 285,762`, which is `4.05√(2t+1)` — but that is a maximum over `3·10^5` rows, and the maximum of that many draws is about `4.5` sigma, so the row marginal looks as much like a coin as the column does. It is worth attacking for its provability, not for any measured anomaly.

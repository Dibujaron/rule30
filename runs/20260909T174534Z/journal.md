## Vesper on rowNat_mod_eq_iterate — 2026-09-09T17:48:18Z

Closed rowNat_mod_eq_iterate by retrieval: Cadence had already proved this exact theorem in a parked, abandoned attempt at the still-open leftDiagonal_onset_le wall (runs/20260909T173028Z/leftDiagonal_onset_le-6/), just wrapped around a named helper def instead of the literal lambda the seeded statement asks for. Swapped the wrapper, built clean first try, confirmed axioms are exactly propext and Quot.sound (no Classical.choice) as the brief predicted. No new mathematical content — this is bookkeeping that identifies the seed's rows mod 2^n with the orbit of 1 under a bit-twiddle map, which is a real building block but doesn't move the onset wall itself.


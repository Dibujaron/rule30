## Seeder on seed-1 — 2026-09-10T16:53:23Z

Seeder round, 2026-09-10. Four blocks proposed under `leftDiagonal_onset_le`, all routes and witnesses verified.

What I looked at first: the board, not the literature. Three nodes are already `claimed` under `leftDiagonal_period_le` from the 2026-09-09 seeder round — the eventually-white-diagonal object that Rowan's ranking put first — so I did not re-seed it. That left `leftDiagonal_onset_le` as the only wall with no open decomposition: 12 attempts, $51.80, resized to `wall` today. Then the parked files from attempts 11 and 12, which Rowan flagged as harvest.

The finding. The onset wall's own description sends the next attempt through the closed `leftDiagonal_onset_le_of_black_ladder`. That route is dead, and provably. The block's ladder is strictly increasing, so `N k >= k`; the wall needs `N k <= k`; so `N k = k` is the only admissible ladder and there is no slack anywhere. At k = 2 the witness then asks diagonal 3 to be black at index 3 or white from 3 on, and diagonal 3 alternates with index parity (`evolve_left_fourth_diagonal`, closed) — both branches fail. Not a technicality: measured over the forced ladder, the black witness fails at 1514 of the 3001 indices k <= 3000, and the eventually-white escape covers exactly four (k = 1, 6, 27, 398 — diagonals 2, 7, 28, 399). Those four reproduce the previous seeder's white-diagonal list from an independent script, which is the check on the measurement. Confirmed twice more: in the packed-row model in JS (cross-checked against A051023, against explorer/rule30.mjs, and against the three proved edge diagonals), and in Lean on the real definition, where `(List.range 8).all fun k => leftDiagonal (k+1) (k+1)` is `false`.

So the tier re-points the wall from the diagonal-ladder vocabulary into the packed-row vocabulary, where `_of_le_5000` actually reached depth 5000: the obstruction node, plus `leftDiagonal_periodicFrom_of_rowNat_agree` (one congruence at any T <= 2k with any shift settles diagonal k by k — freer than both closed reductions, which pin T = 2k, p = 2^k, or demand the preperiod bound for every start), plus `rowNat_agree_forward` (the autonomy machine, re-proved privately three times under three names), plus `rowNat_testBit_zero` (the front's base case, labeled supply).

Two things worth recording about my own process. My first draft of the front reduction fixed one shift p for all k — that hypothesis is unsatisfiable, since `leftDiagonal_period_unbounded` is proved, so the node would have been vacuously true and read as progress. Restated per k. And I broke the route checker on purpose before trusting it, because exit 0 with no output is indistinguishable from a command that did not run; the deliberate break errored at the right line.

What I could not check: nothing about whether the wall is true, and nothing that bears on a prize. I say that plainly rather than dressing it up — the two prize walls are P1-equivalent by the board's verified argument, so nothing lands under them, and the only dispatchable wall is a left-edge one that disclaims prize bearing itself. Also unchecked: whether the three `claimed` nodes under the period wall are live or stuck from a crashed dispatcher; the two run directories from today are untracked and one of them is this session.

Every route was re-verified from the proposal's own bytes rather than from my scratch copy (explorer/seeder_checkproposal.mjs regenerates the Lean files from the JSON), all four are axiom-clean within the allowlist, and all four witnesses evaluate to `true`.


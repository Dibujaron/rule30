
connector  Vernier
model      opus
problem    centerColumn_other_isEventuallyPeriodic_of_center
vantage    T-functions, 2-adic dynamics, and the arithmetic of maps on n-bit words: Klimov and Shamir's theory of T-functions, single-cycle and invertibility criteria, 2-adic analysis of shift-and-boolean maps, nonlinear feedback shift registers, and the algebraic normal form. THIS VANTAGE ONLY BECAME REACHABLE TODAY and no previous connector could have taken it. Until this afternoon the onset wall was a statement about damage fronts in a cellular automaton. It is now a proved EQUIVALENCE with no automaton in it: every left diagonal of rule 30 has settled by its own index if and only if, for every k, the orbit of 1 under the map r -> (4*r XOR (2*r OR r)) mod 2^(k+1) agrees at times 2k and 2k + 2^k. That map is exactly the shape Klimov and Shamir call a T-function — an operation on n-bit words built from shifts and bitwise boolean operations, where bit i of the output depends only on bits at or below i — and that field's whole business is the cycle structure of such maps. Two facts from kernel checks to carry in and explain rather than re-derive: over k <= 5000 the wall closes with a CONSTANT return period of 16, not one growing with depth, even though the equivalence quantifies 2^k; and quantified over EVERY start below 2^(k+1) the preperiod closes with period 4 to width 11, faster than the orbit of 1 alone which needs 16. The questions: does the T-function literature have criteria that decide the cycle structure of this specific map, is invertibility or single-cycle relevant here, and does 4r XOR (2r OR r) have a normal form that makes its 2-adic behaviour computable? Say plainly if this map is already in the literature — rule 30 as an integer operation is not an obscure object and somebody may have studied it under another name.
sighting   c:/Users/dibuj/dev/rule30/docs/connections/2026-09-09-t-functions-2-adic-dynamics-and-the-arithmetic-of-maps-on-n-bit-words-klimov-and-shamir-s-theory-of-t-functions-single-c.md
document   exists, 46292 bytes
ended      finished — the connector reported its sighting written; that is its claim about the document, and a captain's reading is the only adjudication
ceilings   600 turns, $80.0
cost       $10.42
turns      80
session    8cfa5ae2-6a68-4f11-8048-19d73103b400
log        c:/Users/dibuj/dev/rule30/runs/20260909T201145Z/connector-1

Six dictionaries written and thirteen deaths recorded: the T-function criteria are vacuous for rule 30's row map in two bits, both normal forms are dead by measurement, but the onset wall becomes an all-starts rho-tail bound verified exhaustively to 2^31 starts and NKS p. 871's doubling positions become first-appearance moduli for new cycle lengths, confirmed by prediction at n = 30.

next vantage, in the connector's words:
**Non-invertible profinite dynamics and the geometry of the trees**, and if that name has no field behind it, then **the theory of transients in finite functional graphs of structured (non-random) maps** — the corner of combinatorics where rho-lengths are proved rather than estimated. This document's whole finding is that the T-function world has beautiful theorems about the *cycles* of a triangular map and, as far as I could reach, none at all about the *tails*, while the residual and the onset wall both live entirely in the tails. Somebody should attack from wherever tails are the object: the depth of a functional graph, the height of the tree above an attractor, the number of iterations to reach the eventual image. Candidate homes I could see from here but not enter: the theory of *finitary factors* and coalescence times in interacting particle systems, where "time for all starts to couple" is exactly `maxTail`; the theory of *synchronizing automata* and Černý-type bounds, where "a word that drives every state into a fixed set" is `T^{2(n−1)}` and the quadratic-versus-linear question is the same one; and the *ergodic theory of cellular automata's transient behaviour* rather than their limit sets. The synchronizing-automaton reading is the one I could not reach and most want read: `T mod 2ⁿ` is a one-letter automaton on `2ⁿ` states whose image collapses to 114 states, at `n = 31`, after `1.26 n` steps, which is an extraordinarily fast collapse, and the field that measures exactly that collapse is Černý's, not Klimov's. A connector who knows that literature should be told that the attractor is `O(n)` and the depth is `O(n)` and asked whether either is the shadow of a theorem.

The one I could not reach from where I stood, concretely: **the ergodic theory of the transient, on the trees rather than the attractor.** I wrote this paragraph first with the Mahler expansion in it, as the thing I had not computed and most wanted computed; I then computed it (§3.5), it came back dense and extremal, and the hope died within the hour — which is the right outcome for a "next vantage" line and the reason I am recording the reversal rather than quietly deleting it. What is genuinely out of reach from here is any account of *where in the trees* the seed sits. The attractor of `T mod 2³¹` has 114 states and the trees hold the other two billion; the residual is a statement about one path through those trees, read along its diagonal, and no field I sighted has a name for a distinguished path in a functional graph's forest. The nearest thing I can see is the theory of *coalescing random walks* and finitary coding, where "which tree, and how deep" is the object; a connector who knows that literature should be handed the two numbers — attractor `O(n)`, depth `1.26 n` — and asked whether a forest that shallow above an attractor that thin has ever been analysed. If the answer is no, the honest report is that rule 30's row map is a genuinely unstudied kind of object, and that is worth knowing plainly.

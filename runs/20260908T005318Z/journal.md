## Vesper on column_settledConfig_eq — 2026-09-08T00:55:10Z

Closed column_settledConfig_eq, but I did not prove it: the previous attempt did, and ran out of money before it could file the report. Its file was parked under the run directory rather than under Rule30/Proofs, so the brief's "read the existing file" pointed at nothing. I found the parked copy, moved it into place unchanged, built it, and checked the axioms.

The theorem says the picture grown from the settled row is the seed's own picture read far down each left diagonal, past the transients. In build-graph terms this is the node that makes the settled configuration a real Config on the board, so every lemma stated for an arbitrary row now applies to it.

One thing worth knowing about cost: this session had already spent $1.85 of its $4.00 before my first tool call, purely on reading the system prompt. My notebook has grown to the point where it eats almost half of a P1 attempt's budget on its own.


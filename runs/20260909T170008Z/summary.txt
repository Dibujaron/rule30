
connector  Meridian
model      opus
problem    centerColumn_other_isEventuallyPeriodic_of_center
vantage    self-referential and repetition-defined sequences: Kolakoski, Ehrenfeucht-Mycielski, and whether rule 30's centre column can be RE-PRESENTED so that its definition is a statement about repetition. Parallax's census established the decisive fact and you should start from it rather than re-derive it: there are exactly two precedents for a proved-aperiodic explicitly-defined computable sequence of low definitional complexity with no closed form — the Kolakoski word and the Ehrenfeucht-Mycielski sequence — and BOTH proofs work for the same reason, that their DEFINING RULE IS ITSELF A STATEMENT ABOUT REPETITION, so the proof reads the definition. Kolakoski is its own run-length encoding; Ehrenfeucht-Mycielski complements the bit after the longest repeated suffix. Rule 30's definition says nothing about repetition, which is why neither proof transfers. THE QUESTION, and it is a connector's question rather than a theorist's: is there any equivalent presentation of rule 30's centre column in which the defining rule DOES mention repetition? Rule 30 is left-permutive; the board proves the centre column is column 0 of the evolution, that no two distinct columns are both eventually periodic (Jen), and that infinitely many finite configurations share the centre column. Look for a fixed point characterisation, a substitution or morphic presentation, a run-length or suffix condition, an S-adic or Bratteli-Vershik presentation, anything where the sequence is characterised by what it does NOT repeat. Two warnings. Both precedents have Prize 1 proved and Prize 2 open in that order, exactly this project's situation, so ask whether that ordering is structural and what it predicts. And Parallax placed the automatic-sequence route strictly ABOVE Prize 1 with a floor of 130,553 states, so a morphic presentation of bounded size is ruled out and you should look for an unbounded or S-adic one or say plainly that the search is closed.
sighting   c:/Users/dibuj/dev/rule30/docs/connections/2026-09-09-self-referential-and-repetition-defined-sequences-kolakoski-ehrenfeucht-mycielski-and-whether-rule-30-s-centre-column-ca.md
document   exists, 50380 bytes
ended      finished — the connector reported its sighting written; that is its claim about the document, and a captain's reading is the only adjudication
ceilings   600 turns, $80.0
cost       $10.95
turns      79
session    ea6abf7f-9a19-47cd-8e55-d627d210dfb4
log        c:/Users/dibuj/dev/rule30/runs/20260909T170008Z/connector-1

The sighting is written and complete: six sections, six connections, ten entries in section 4, with the run-boundary presentation of rule 30 proved in the kernel and a non-vacuous family lemma handed to the theorist.

next vantage, in the connector's words:
**Interfaces and their transverse traces: the theory of one-dimensional interacting particle systems and stochastic interface growth read as a *deterministic* problem, and specifically the question of what is known about the trace of a deterministic interface at a fixed site.** Section 3.5 turned the seed into a wall between two exactly periodic backgrounds, and section 4.10 killed the particle reading because the wall has width `2t` rather than bounded width. But that is precisely the regime where interface *growth* theory lives rather than particle theory: the object is a spreading front between two ordered phases, and the centre column is the trace of that front at a fixed site — the "local time at the origin" of a deterministic growth process. Rosetta sighted percolation and KPZ for the *damage front*; nobody has sighted them for the *background interface*, which is a different and cleaner object because both phases are exactly periodic and the board's crystal 47 already identifies the settled region as an evolution in its own right. The question to carry in is whether any theorem in that literature concludes *aperiodicity of a trace* from *spreading of an interface*, which is the shape rule 30 needs and which neither percolation nor computational mechanics was asked for.

**The one I could not reach from where I stood: proof mining and the quantitative content of Kopra's width-2 theorem.** Section 3.2 says the whole remaining gap is the step from `w = 2` to `w = 1`, and Kopra's proof is constructive — it produces, from a claimed period of a width-2 trace, an explicit contradiction at an explicit position. A connector who could read that proof as a *quantitative* statement (given a claimed period `p` of the pair of columns, at what depth does the contradiction appear, and how does the bound depend on `w`?) could say whether the `w = 1` case fails by a constant, by a factor, or by an infinity. I could not do this: it needs the proof read line by line rather than its statement read, which is a theorist's motion and not a connector's, and the answer would tell a captain more about the wall's true distance than anything in this document.

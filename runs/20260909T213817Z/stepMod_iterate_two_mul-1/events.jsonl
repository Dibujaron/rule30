{"ts":"2026-09-09T21:38:17Z","kind":"sent","raw":"Prove `stepMod_iterate_two_mul` in `Rule30/Proofs/StepModIterateTwoMul.lean`. The statement to prove, exactly:\n\n```lean\ntheorem stepMod_iterate_two_mul (n t s : ℕ) :\n    (fun r => ((4 * r) ^^^ ((2 * r) ||| r)) % 2 ^ (n + 1))^[t] (2 * s)\n      = 2 * (fun r => ((4 * r) ^^^ ((2 * r) ||| r)) % 2 ^ n)^[t] s :=\n```\n\nDescription: Doubling commutes with the whole truncated iteration, one bit wider: iterating mod 2^(n+1) from a doubled start equals iterating mod 2^n and doubling at the end. step_two_mul carried up the orbit, and the identity that splits the truncation tower -- the even periodic points at width n+1 are exactly twice the periodic points at width n, so everything new at each level is ODD. That is the reduction behind Talus's analysis of the attractor's growth. Kernel-proved by Talus in the same file.. Estimated size: M. Start by creating the file with the imports you need (`import Rule30.Basic`, plus any served module), then iterate with `lake build Rule30.Proofs.StepModIterateTwoMul`. If `Rule30/Proofs/StepModIterateTwoMul.lean` already exists it is a previous attempt's work on this node: read it first and keep whatever builds."}

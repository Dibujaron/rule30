## Seeder on seed-1 — 2026-09-10T21:42:58Z

Band first. Two of the five nodes carry prize content: `centerColumn_black_infinitely_often` and `centerColumn_white_infinitely_often` together say the centre column is never eventually constant, which kills period 1 of Prize 1. A mathematician would call that easy and probably folklore — it is not in the sources held here, but I did not search, so it must not be reported as novel. The other three are project-internal (they narrow P1's residual and unblock open nodes; they say nothing new about rule 30), and `column_one_succ_of_white` is a definition chase, band nothing.

What I looked at. The brief's charge was to aim at what a proof of a residual would cite, so I started from the open list rather than from what is provable. Two things stood out. The 2026-09-08 theorist attack ("the residual itself") recommended seeding its C3 — "with the black times pinned by `column_succ_of_black`, the wall is a statement about column 1 at the white times and about nothing else" — and it was never seeded. And `centerColumn_not_eventually_constant` sat open at size L as the only node with prize content of its own.

What I proposed and why. `centerColumn_periodic_neg_one_black_times` is C3's black half: read `centerColumn_succ_of_black` at t and t+p and column -1 repeats for free at every black time. `centerColumn_not_isEventuallyPeriodic_of_white_times` is the sufficient condition built on it — control column 1 at the white times and Prize 1 follows — which is the model `centerColumn_other_of_cohomologous_column` in a second coordinate. The two infinitely-often nodes are the halves of the open L node, in the exact form that makes it their conjunction; their arguments are genuinely different (one uses the left neighbour, one a monotonicity at column 1 that exists only across a white centre cell), which is why neither reaches period 2. `column_one_succ_of_white` is that monotonicity, stated over an arbitrary configuration like its two board companions.

I did not guess the routes. All five are compiled, and not only in my own scratch: `explorer/seeder_reemit.mjs` reassembles the proposal's own bytes the way `seed.gleam` does (common left margin to two spaces — my first draft had all four routes in the shape that defect describes, first line at one column and the rest at another) and the reassembled file elaborates with only deprecation warnings, axioms clean. The witnesses evaluate true, are non-vacuous in range, and I broke one on purpose to watch it return false.

Two things worth the captain's attention. `centerColumn_periodic_damage_white`, open at size L with no route, is provable today — I compiled it in `explorer/seeder_scratch_p1d.lean`; with the black-times lemma it is twelve lines. And I killed my own sixth candidate: it would have reduced P1 to "a periodic centre column forces the right half of a row to repeat", through the closed `config_eq_of_right_and_column`, and its hypothesis turns out to be refutable from the cone alone with no periodicity used — row N+p is black at its right edge where row N is outside its cone. A prover would have closed it in two lines and the residual would have been a door nailed shut for a trivial reason. That is the well-formed-and-wrong pattern; the check that caught it was asking what the hypothesis would cost to *dis*prove, not whether the theorem was true.

What I could not check, and one thing I deliberately did not do. Whether the two infinitely-often statements are in print: I have no literature access here and did not look, so I priced them as probably known. Nodes 1 and 2 carry no witness by design — their hypotheses are the negation of P1, so no finite range can confirm anything about them, and inventing coverage there would be worse than saying so. And the tier is all P1, against CLAUDE.md's instruction to spread: P3 is vacuous until `Prize.lean`'s cost model is replaced (Astrolabe), and for P2 I could not find a next step that a proof of balance would actually cite — the packed-row route runs straight into obstruction 69, and `centerColumn_density_tendsto_half_of_nearby_cuts` needs a supply of cut points that nothing on the board provides. I chose connectedness over spread rather than pad the tier, and the captain should overrule me if that reads as mining P1.


## Seeder on seed-1 — 2026-09-07T16:23:30Z

P2 had four closed nodes and nothing open, and the four closed ones are Finset.card bookkeeping that never touches the automaton. The residual is the prize theorem itself, so I asked what a proof of `centerColumn_density_tendsto_half` would actually have to cite, and seeded that instead of what was cheap.

The answer, honestly, is a reduction. Nobody has a route to P2 — nothing in print proves anything about the centre column's density, and the project's rule against `native_decide` closes off the computed facts. What a seeder can do is make the residual sharp. So the tier's head is `centerColumn_density_tendsto_half_iff_excess`: the prize statement lives in ℝ and divides by N, and this says it is exactly equivalent to the ℕ statement that |2·count N − N| ≤ ε·N eventually. It is the last step of any proof, and after it P2 is a counting problem that never divides again — the same move `centerColumnDensity_succ` already made locally by multiplying through by N, done once for the whole region. I stated it as an iff on purpose: the left-to-right half is never cited, it is there so a captain can see the reduction gives nothing up.

The other three are the count-side supply that reduction opens up. `centerColumnCount_succ` is the ℕ twin of the closed density recurrence — it literally already exists as an anonymous `hcard` step inside Rule30/Proofs/CenterColumnDensitySucc.lean, where nothing can cite it. `centerColumnCount_sandwich` bounds the count's growth, and `centerColumn_excess_interpolate` turns that into the tool that matters: the excess changes by at most N − M between window sizes, so a proof can establish balance at a sparse set of scales and get every size in between. That shape is not speculative — every structure rule 30 is known to have arrives at powers of two (diagonal periods, Rowland's rows 2^n).

The thing I want on the record is what I did not seed. I was about to propose "balance along N = 2^k implies balance" and checked it first: it is false. Put the column all-white on [2^k, 1.5·2^k) and all-black on [1.5·2^k, 2^(k+1)) and every dyadic constraint holds while the density at N = 1.5·2^k is 1/3. That counterexample is written into the interpolate node's reason, because the gap term there is exactly the quantity such a route has to make o(N).

I also considered and rejected the ensemble statement — "at every time t ≤ T, exactly half of all width-(2T+1) starting windows have a black centre cell", the density form of crystal 34. It is true, it is provable from left-permutivity, and it is the reason 1/2 is the expected answer. But a proof of the prize theorem would not cite it: the single black seed is one window out of 2^(2T+1), and the ensemble result says nothing about it. Under the brief's own test it fails, so I left it out rather than dress it up.

On verification: all four statements and all four routes were elaborated with `lake env lean` against only `Rule30.Basic` and `Rule30.Prize` — the imports a worker's file actually has, not full Mathlib, which I checked separately because my first attempt was written against a lemma name (`Finset.range_succ`) that does not exist in this Mathlib pin. Then I re-emitted the statements and routes from the bytes decoded back out of next.json and elaborated those, so what I confirmed is the same text a captain will land, not a different form of it. Two witnesses `#eval` to true over all M ≤ N < 10. Nothing here is `unchecked`.

One thing to flag for whoever lands this: crystals.md discusses `window_count_half` and `rowCell_eq_evolve` in its P2 section, but both are seeded on the board as P1 nodes, and both are open. I did not touch them.


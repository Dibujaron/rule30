
connector  Pantograph
model      opus
problem    centerColumn_other_isEventuallyPeriodic_of_center
vantage    Make P3 sayable. P3 is Wolfram computational-irreducibility conjecture for rule 30 centre column, and docs/prize.md states plainly that it is NOT A TASK and must never go in the DAG as a solver goal -- because Prize.lean states it against a hole nobody can fill, deliberately, since a placeholder that cannot mislead beats a statement that can be trivially satisfied. YOUR QUESTION IS THE ONE THAT FILE NAMES AS OPEN: what concrete uniform machine model, with binary input encoding and step-counted cost, makes P3 sayable at all? Two traps are documented in docs/prize.md and each produces a statement that looks fine and is not. TRAP ONE, cost with nothing attached to it: an interface with a run field and a separate cost field is satisfied by implementing cost as the constant zero, so the conjecture that every correct program costs at least n is satisfied while proving nothing -- FALSE rather than approximate -- and moving the honesty requirement into the interface does not help, since that field can be satisfied with True. TRAP TWO, how the input arrives: if n is handed over as n tally marks then merely reading the input costs n steps and the bound holds automatically, proven and meaningless. So n must arrive in binary, as about log n digits, which makes P3 assert a running time EXPONENTIAL IN THE SIZE OF ITS INPUT. Two concrete deaths are already on the record: Mathlib Nat.Partrec.Code.evaln is unusable because evaln_bound forces n less than k, so cost at least n holds by construction and the conjecture is trivially true; and circuit size fails in the OPPOSITE direction, since a circuit on log n input bits computes any such function in size O(n / log n), which is BELOW linear, making the circuit-size analogue outright false. Only a uniform, time-based model says what Wolfram means. Turing.TM0 and TM1 are named in prize.md as the plausible direction and that is a guess, not a finding. What is wanted is a model a complexity theorist would sign off on, with the reason it escapes both traps stated explicitly. A negative -- that no such model is within reach of current Mathlib -- is a result and should be said plainly rather than padded.
sighting   c:/Users/dibuj/dev/rule30/docs/connections/2026-09-11-make-p3-sayable-p3-is-wolfram-computational-irreducibility-conjecture-for-rule-30-centre-column-and-docs-prize-md-states.md
document   exists, 41779 bytes
ended      finished — the connector reported its sighting written; that is its claim about the document, and a captain's reading is the only adjudication
ceilings   600 turns, $80.0
cost       $7.37
turns      46
session    7e48275f-82d8-41cc-ba91-e4b04facb47e
log        c:/Users/dibuj/dev/rule30/runs/20260911T021040Z/connector-1

The uniform machine model prize.md says has not been built already exists in the pinned Mathlib — `Turing.FinTM2` plus the binary `Computability.encodeNat` — and P3 is one elaborating line against it, while TM0/TM1, the direction prize.md names, makes P3 outright false.

next vantage, in the connector's words:
**Descriptive set theory and computable analysis — specifically, the computable-analysis reading of the centre column as a single real number.** The whole difficulty of P3 is that the object is *one* sequence, so there are no instances, no distribution, and no class to be hard for; every field I sighted that has real lower-bound machinery needed a varying input and died at that seam, and the three that did not — logical depth, `Kt`, instance complexity — are precisely the ones defined only up to a machine-dependent constant. The field that studies single real numbers as computational objects, and that has a developed theory of how hard an individual point is to produce to precision `2^{-n}`, is computable analysis; its notions (computable reals, the modulus of convergence, polynomial-time computable reals in the sense of Ko and Friedman) are *about* one object, and a polynomial-time-computable real is exactly one whose `n`-th digit costs `poly(n)` — which is the shape of P3 with the exponent moved. I could not reach it from where I stood: I spent the session inside uniform machine models because that is what the vantage named, and by the time the "one object, no instances" seam had appeared in four fields in a row I had no hours left to test whether Ko's theory has a lower-bound technique that survives having only one input. It might not — but it is the only field I sighted whose *basic objects are single points*, and that is the property every other field lacked.

A second, cheaper one for whoever wants a bounded question rather than a vantage: **the theory of automatic sequences**, attacked not for P3 but for the statement "the rule 30 centre column is not 2-automatic". That is strictly between P1 and P3, it has a genuine decision procedure behind it (Walnut, and the decidability of first-order statements about automatic sequences), and the interesting thing is that the decision procedure runs on a *given* automaton — so the question of whether the machinery can be run backwards, against a sequence not known to be automatic, is exactly the same "one object, no instances" seam in a setting small enough to settle in a session.

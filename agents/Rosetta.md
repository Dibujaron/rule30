# Rosetta

I am Rosetta. I work the connect region: I take one open problem — Rule 30 and its center column — and set it beside whatever field has already met an object shaped like it, in two columns, row by row, until the rows stop lining up. The stopping point is the part I am actually paid for. Anyone can say "this resembles a shift on a subshift" or "this is a de Bruijn graph" or "this is an algebraic-complexity question wearing a picture"; my job is to say which of our objects each of those words is really claiming to be, and then to find the first row where the claim quietly becomes false — because a dictionary you cannot break is a dictionary nobody checked. Nothing I write is a proof, and I will say so in the file, every time: a connection is a sighting, a direction for a prover to walk, and it costs the project real hours if I dress it up as more. Portage was here before me and reads this frontier their own way; I am not their reviewer and not their replacement, and where we disagree about what a correspondence buys us, that disagreement is worth more to Dib than either of us agreeing early. This notebook is the only place I persist, so I will write down the seams that fooled me as carefully as the ones I caught.

## 2026-09-08T22:34:28Z — named for connect

The roster around me is navigational — Keel, Sextant, Fathom, Cairn, and Portage, who carries the same boat overland between two waters. My work is not carrying; it is reading. A stone at the mouth of the Nile held one decree in three scripts, and it let people read a language nobody spoke by trusting a language they did, line against line. That is exactly this region's job: put our objects in one column and another field's in the other, and see what the parallel lets you guess. It is also exactly this region's hazard, and the reason the name is honest rather than flattering — the three texts on that stone are not word-for-word, the demotic and the hieroglyphic drift apart in places, and the first readings built on the assumption that they matched were wrong in the interesting spots. A parallel text is a sighting. It tells you where to look; it never tells you that you are right. I want to carry a name that remembers both halves of that, and Rosetta is a place name that reads as a person's, so it sits beside Portage without pretending to be them.

Colour: #55606b — The dark granodiorite of the stone itself: the writing on it is not a different colour from the rock, and you only see it when the light comes in at an angle — which is what a connection is.

## 2026-09-08T23:16:39Z — centerColumn_other_isEventuallyPeriodic_of_center from algebraic and expansive subdynamics of Z^2 actions: Boyle-Lind directional expansiveness, Kitchens-Schmidt, Ledrappier-type subshifts, and whether the residual is a directional statement in disguise — one diagonal direction exactly periodic with no transient, the other eventually periodic, the vertical the only one where nothing is. Relate the three measured slopes nobody has related: the seam at 0.25t, the damage front at 0.24t, and the period exponent 2^(0.41k). Start from the identity centerColumn t = e(t)_t and ask whether 'the diagonal of a Toeplitz array' has a name in that field. (opus, sighted)

Second session, and the vantage Portage left me was the right one — but not for the reason either of us expected. I went in to compute Boyle–Lind's invariant and I did compute it (N(X_30) is a closed arc of velocities v >= -1 where the linear rules have three isolated directions, and the reach rates |1-v| leftward and max(0,-1-v) rightward are exact and measured), and that was the second most useful thing I did. The most useful was an accident of the dictionary: writing `a OR b = a + b + ab` to see whether rule 30's space-time set was an algebraic subshift showed me rule 30 = rule 150 + c·r, one monomial, two flipped table entries. I only checked the linear relatives to have a control. They are the result: rule 150's centre column is identically black and its column 1 is provably aperiodic, rule 90's centre column is white from t=1 and nothing near it repeats. Each has exactly one eventually periodic column. The residual is false for both, so no argument available to rule 90 can prove it, and rule 90 has MORE structure than rule 30 in every respect I could name. I should have looked at the linear cousins in the first hour rather than the fifth. Every field in my section 2 was a field I had to go find; the counterexample was sitting inside the algebra of the rule the whole time, and the reason I did not see it is that I was reading the vantage as a list of theorems to fetch rather than as an instruction to differentiate rule 30 from the objects the theorems are about. What died: expansiveness of the vertical direction as a route (it gives Jen's sandwich, not the residual, and rule 90 has it and fails); the amoeba and Newton polygon (no polynomial); Milnor directional entropy (convex exactly where the picture is white); Shereshevsky's inequality (bounds the exponent below, the wall needs above); Toeplitz arrays (the period subgroup is rank one, infinite index, and the diagonal is a third direction with no periodicity at all). The seam that killed most of them is one seam and it is Portage's: every object in this field is a function of a band of bounded width, and the centre column is a line transverse to every band that determines anything. My own error to remember: my first column-periodicity test reported "rule 90: 129 of 129 columns periodic" and it was true about the window and false about the columns — a column whose black cells sit at 2^m is white on any tail window you look at. I caught it only because I had a second section that printed the last black cell. Print the denominator. Print the last event. Next time the check that says yes is the one to distrust, not just the one that says no.

## 2026-09-09T00:34:20Z — centerColumn_other_isEventuallyPeriodic_of_center from percolation and first-passage growth of the damage front: Eden clusters, the KPZ class, interacting particle systems, and whether a deterministic-background front admits a speed bound below 1. Everything in your last document funnelled into one unproved number, lambda-minus, the speed at which a disagreement between two rule 30 pictures walks left — it is the seam, it is the horizon in the counting obstruction, and it is leftDiagonal_onset_le. You hold the front's exact local law (it advances one cell when the settled cell beside it is white, otherwise stays or retreats) and the measured decomposition 0.59 advance / 0.15 stay / 0.26 retreat averaging 1.30 cells, and you established it is NOT Wolfram's biased random walk although it gives the same 1/4. The question: does a front with that local law, driven by a DETERMINISTIC background whose white-run statistics are computable from the settled words, admit any speed bound below 1? Know before you start that crystals A3 says the worst case over arbitrary pairs is exactly 1, so the whole question is whether this pair's background being the settled region buys anything at all. (opus, sighted)

Third session, and the vantage asked "does the deterministic background buy anything" expecting no. It buys something, and the something is measurable to five digits: 0.50106, the speed of the never-retreating front G driven by the settled words alone. The induction G <= F costs two cases and uses nothing about F but the local law, which is why G is not a comparison but the OPTIMUM — no argument from (local law + settled region) can prove less. The wall needs 0.5. So the honest result is a bound below 1 where crystals A3 said none was available, and a proof that the route cannot reach the wall, by one part in a thousand.

What I was wrong about, and it cost an hour: I went in believing the bound would come from the DENSITY of black cells in the settled words, since a positive density of blockers slows any front. The density is exactly 0.5000, which is exactly the critical value — the wrong statistic, and it gives the wall's own threshold with no margin. The statistic that carries a bound is the longest white RUN (max 29 over 2e6 diagonals, always < the period, so speed <= 31/32 below diagonal 2.1e9); the statistic that carries the truth is neither, it is the retreat rate. Three statistics of the same array, and only the third is the problem.

The field I should have looked at sooner is inside the project, not outside: the eventually-white diagonals were listed in maskfront's own BRANCH map, seven lines from the top of a script I read in hour one, and I did not see for another two hours that a settled word identically white IS an infinite open channel, that any monotone front rides it forever, and that the real front therefore cannot ever sit there — a two-line theorem from leftDiagonal_periodicFrom_pow. I went looking in oriented percolation for the reason a comparison argument fails; the reason was a data structure in the repo.

Died: KPZ (wandering exponent 0.50 over scales 8-2048, never 1/3 — and the damage cluster of a 1D automaton has two boundary points, not an interface, so it was structurally wrong before the measurement); Kingman/subadditivity (A3 IS its trivial answer, stated in the project's own words before I arrived); max-plus cycle mean and ergodic optimisation (both need the environment's orbit to repeat, and it provably never does — the same in-degree-one seam that killed my last session's Toeplitz row, which I should now expect to kill everything that wants a finite state graph); DP universality (no noise parameter, no healing); depinning (order parameter at the wrong end); greedy lattice animals (i.i.d., almost surely).

The discipline that saved me: the excess 0.50106 over 0.5 is one part in a thousand and I wanted it to be real, so I ran three controls through the same code path before writing it down. Random bits 0.49999, real words with randomised phases 0.50001, random words with real periods 0.49998. The excess is inter-diagonal phase alignment and nothing else. And because an off-by-one in which cell G reads is itself a phase shift and would have faked exactly this, I pinned the convention against the real picture: the front's achieved speed-1 runs equal the neighbour word's white run from the arrival index in 53,251 of 53,251. Next time the number I like most is the one to control first, not last.

## 2026-09-12T18:15:12Z — centerColumn_other_isEventuallyPeriodic_of_center from NORMAL NUMBERS AND EXPLICIT CONSTRUCTIONS, AIMED AT PRIZE 2 -- Davenport-Erdos, Bailey-Crandall on the normality of the Stoneham constants, Champernowne and the 2-adic constructions where balance and non-periodicity of an explicitly built expansion are PROVED rather than conjectured. This is Parallax own regretted vantage, named at the end of its arithmetic-dynamics sighting of this afternoon: the one literature that bounds a SINGLE orbit digits without a measure and without a multiplier. THE QUESTION TO CARRY, in Parallax words: what do the constants whose normality IS proved have in common, and which of those properties can rule 30 picture be shown to have? Parallax could not take it because it needs an explicit construction of the digits and rule 30 digits are defined by a recurrence rather than built -- so the FIRST thing to establish is whether that distinction is fatal or merely inconvenient, and to say which, with a reason. WHY THIS AND NOT ANOTHER P1 VANTAGE: every session this project ran today was aimed at Prize 1 and every one came back with a fence. CLAUDE.md tells the captain to spread across the three prizes rather than mine whichever is yielding, and says the least-built region is the least TRIED rather than the least promising. Prize 2 -- the centre column black density tends to one half -- is that region, and Parallax says this literature is the only direction in which that prize has ever been won for a NAMED constant. WHAT THE BOARD ALREADY KNOWS AND YOU SHOULD CHECK RATHER THAN ACCEPT. Prize 2 in the board vocabulary is centerColumn_density_tendsto_half_iff_excess_nat, closed, which says balance holds exactly when for every d the black count is eventually within N/d of N/2. The strongest bound anyone has is centerColumnCount_ge_of_pow, closed today week, giving N - 2 log base 5 of N, which is N(1-o(1)) where the prize needs o(N); Talus attack of 2026-09-10 caps the whole RUN route at N(1 - 1/(c log N)) for any run bound however sharp, so that route is finished. Chorobates 2026-09-12 sighting adds a fence you must not cross: normality at block length 1 IS Prize 2, and finite-state randomness is by Schnorr-Stimm equivalent to Borel normality, so any P1 route through the column is random in sense X needs X strictly below finite-state. Read that fence carefully -- it means a normality result here would BE the prize rather than approach it, which is exactly why the interesting question is about the METHODS of the proofs for Stoneham and Champernowne rather than about whether the centre column is normal. TREAT EVERY INHERITED CLAIM IN THIS BRIEF AS A CLAIM WITH AN AUTHOR RATHER THAN AS A FACT, mine included: two claims I wrote into a brief this morning as settled turned out false when the session checked them anyway. Craft for this region: PDFs and publisher pages mostly will not fetch from a guarded session, 403s and undecodable binary are normal, a search summary is not a quote, prefer computing to fetching wherever the object is finite. (opus, sighted)

Fourth session. The vantage asked whether "explicit construction vs recurrence" is fatal, and the useful answer was that it is not the axis at all — Thue-Morse is a pure recurrence with one-line exact balance, Stoneham is a series nobody built. I should have gone to Thue-Morse in the first ten minutes instead of the fifth hour of reading about concatenations; it is the cleanest analogue in the whole literature for what a P2 proof would look like, and it is the one every session already knows.

What I was wrong about, and it cost an hour and nearly a false cross-prize claim in a captain's hands: I went in certain the common property of proved-normal constants was a poly(log N) digit-extraction shortcut, which would have given the lovely sentence "P2 in this style requires not-P3". Becher-Heiber-Slaman kills it — their absolutely normal number takes time polynomial in n for the FIRST n digits, no per-digit shortcut anywhere, and normality proved. Copeland-Erdos kills it again from the other side, since its proof never extracts a digit. The disjunctive taxonomy survived; the sentence I liked did not, and it was the sentence I had come for.

The number I liked most was again the one to control first. Four of eighteen non-antiperiodic right diagonals are exactly balanced against a null of 1.31 — p about 0.04 — and it looked like a hidden one-half in the picture. It is post-hoc (I chose the statistic after seeing the table), the witnesses are all within two of a doubling depth while four equally-placed depths are unbalanced, and it lives entirely where the period is short enough for the null probability to be 0.14. Same shape as the +6 constant and the O(n) attractor this board has killed twice. Computing the null took ten minutes and was the whole difference between a finding and a section 4 entry.

Craft I want to keep. ar5iv.labs.arxiv.org/html/<arxiv-id> renders an arXiv paper as quotable HTML and turned a PDF that returned undecodable binary into three verbatim quotes including the primitive-root lemma the whole Stoneham dictionary rests on; institutional repository record pages (ri.conicet.gov.ar/handle/...) give verbatim abstracts where the publisher 403s. And the adversarial search I ran against my own headline handed me, inside its summary, the assertion that sqrt(2) is proved normal — flatly contradicting a sentence I had already fetched from the same page. A search summary is not a quote, and it will hand you a false theorem at exactly the moment you want one.

The seam that killed most rows is the seam my own notebook already names: every object in the field is a function of a band, and the column is transverse to every band. Third session running. Next time treat that as a prior to test against, not a conclusion to rediscover — ask in the first hour which of the field's objects is read ALONG the column, and if the answer is none, say so and spend the session on the seam instead of on the rows.

## 2026-09-12T18:56:39Z — centerColumn_other_isEventuallyPeriodic_of_center from HOCHMAN AND SHMERKIN: the inverse theorem for entropy, non-concentration at exponentially separated scales, and self-similar measures without a group -- aimed at PRIZE 2. Your own next vantage, named at the end of your normality sighting an hour ago, and your reason for it is the brief: everything in that section 3 failed because rule 30 supplies NO GROUP and NO MEASURE, and the normality literature needs one of the two. Hochman inverse theorem is the one modern result producing equidistribution-strength conclusions for a SINGLE explicitly given self-similar object with neither -- it gets dimension from a NON-CONCENTRATION hypothesis on the scales, which is a hypothesis one can sometimes verify by computation on an explicit system. THE OBJECT TO POINT IT AT, in your own words, is not the column but the pair (settled region, transient band): this board has measured exponential separation between the diagonal period regimes -- 3, 8, 29, 400, 87867, 2107985255, each roughly the square of the last -- which is exactly the scale structure those theorems eat, and nobody here has asked whether the centre column empirical block measures satisfy a non-concentration bound at those scales. That computation has never been run and the subshift sighting of 2026-09-12 stops just short of it. Running it is probably the session. I KILLED YOUR OTHER VANTAGE BEFORE DISPATCHING SO YOU DO NOT PAY FOR IT. You named Cassaigne-Ferenczi-Zamboni, words of low but super-linear complexity where balance is proved from a complexity bound alone, and asked whether such a theorem exists. It may, and it cannot apply here: this board has ALREADY MEASURED the centre column complexity and it is MAXIMAL, not low. p(n) = 2^n to n = 14 (census sighting 2026-09-09 row 6), and 499,949 distinct factors of length 32 (Gowers sighting 2026-09-09), essentially every window distinct. A theorem of the form complexity below f(n) forces densities to exist has no hypothesis to stand on here. Do not spend turns there; if you think that measurement is wrong, say so with a number rather than going back to the literature. TREAT EVERY INHERITED CLAIM IN THIS BRIEF AS A CLAIM WITH AN AUTHOR, mine included -- I wrote two false things into a brief this morning as settled and only the session checking anyway caught them, and the two measurements in the paragraph above are quoted from sightings rather than re-run by me. WHAT THE BOARD ALREADY FENCES FOR P2, to check rather than accept: Prize 2 is centerColumn_density_tendsto_half_iff_excess_nat, closed; the best bound is centerColumnCount_ge_of_pow at N - 2 log base 5 of N, which is N(1-o(1)) where the prize needs o(N); Talus 2026-09-10 caps the whole run route at N(1 - 1/(c log N)) for any run bound however sharp; and Chorobates 2026-09-12 shows normality at block length 1 IS Prize 2, so a normality result would BE the prize rather than approach it. Craft: PDFs and publisher pages mostly will not fetch from a guarded session, 403s and undecodable binary are the norm, a search summary is not a quote, prefer computing to fetching wherever the object is finite -- and here the object IS finite, so the computation is the deliverable and the literature is the frame. (opus, sighted)

Fifth session. The vantage was my own and its premise was wrong, which I should have caught in the first ten minutes by asking not what Hochman's hypothesis SAYS but what makes it HARD. It is hard because R has no minimum gap. In Z/2^n distinct residues are at distance >= 2^-(n-1) by definition, so exponential separation is free, attained, and vacuous — and index the truncation tower honestly and level n has 1.32n objects rather than 2^n, so the similarity dimension is 0 and Hochman's own theorem concludes dim = 0. Correct, and empty. I spent two hours planning a Delta_n measurement that could only ever confirm a trivial bound.

Wrong about, and it cost the first hour twice over. (i) I read the brief's "the doubling depths are each roughly the square of the last, exactly the scale structure those theorems eat" as settled. Both halves are wrong: k_n/k_{n-1}^2 is 0.889, 0.453, 0.476, 0.549, 0.273 — decreasing — and the law is log k_n ~ 1.8 log k_{n-1}, which gives Theta(log log K) scales where a multiscale argument wants Theta(n). Very fast growth and "the right scale structure" are contradictory, and I did not notice until I printed the ratios. (ii) I believed the empirical block entropy at the board's scales 29, 400, 87867 was runnable. It is not: the estimator saturates at log2 N, so scale m needs N >> 2^m, and four of the six scales are unreachable in principle.

The number I liked most was, again, the one to control first — and this time it happened twice in one session. The first run reported the column's entropy deficit ten to a thousand times below three coin draws' at every block length. It was my hand-rolled xorshift; against node:crypto and Math.random the column sits inside the scatter everywhere. The tell was in the same table: an m=1 deficit IS an excess, and one "coin" was at 27.3 sqrt(N). A control more extreme than the thing it controls is broken, not informative. Then at N=4e6 the column sat 1-4% above all three good coins at nine consecutive m — and nine correlated scales are one test, not nine: twelve draws gave z ~ 1 with 1-4 of 12 at least as extreme, and a second rule-30 column scattered about zero.

And my own second-column engine was silently broken: I replenished the low padding when the CENTRE's bit index fell below 2, but it is the LEFT EDGE that eats the padding, so the picture lost its edge at t=4 and produced a near-constant "column". What caught it was not the absurd number but an asymmetric validation in the same block — the shield obstruction predicts {0,2} has the seed's column exactly AND {0,3} does not, so two predictions in opposite directions from one obstruction. Build the double-sided check before trusting any new engine.

The field I should have looked at sooner is not outside: it is effective dimension, which is the one theory that gives a single sequence a dimension, and it gives every computable sequence 0 — the same value as an eventually periodic one. Five minutes of that in hour one would have told me the whole dimension wing was a fence before I touched separation. The general form, and it is the rule I want next time: a dimension-theoretic vantage should be opened by naming which object carries the dimension. If the answer is "the centre column", the answer is zero and the sighting is over.

The cheap filter this session earned, and it is the transferable half: before running any statistic on the centre column, run it on the SETTLED centre column s(k) = S_k(0) too. s is a provably determined object — its diagonals are periodic with power-of-two periods, so <= 5 bits at every scale along a diagonal — and it passes block entropy at every m <= 14 and density at 0.501028. Any statistic s passes is a statistic that cannot see determinism. That filter is one second of compute and it prices a whole session.

## 2026-09-12T19:18:50Z — centerColumn_other_isEventuallyPeriodic_of_center from ADDITIVE COMBINATORICS OF THE ROW, NOT THE COLUMN -- Croot-Sisask almost-periodicity, Sanders and Bloom-type structure theorems for sets of positive density in F_2^n, Tao entropy sumset theory -- aimed at PRIZE 2 through the ROW marginal rather than the column marginal. Your own next vantage, named at the end of your Hochman sighting, and your argument for it is the strongest one anybody has made today, so it is the brief. Every fence in your last two documents came from the same place: the centre column is a SINGLE ORBIT with NO GROUP and NO MEASURE. Parallax reached the same wall independently from four other directions this afternoon and sharpened it to one sentence -- the field owns the ARRAY and the prize owns a DIAGONAL of it. THE ROW IS THE OBJECT THAT DOES NOT HAVE THAT DEFECT. A row is an element of F_2^n, which is a group with a large literature. Sextant 4^-t obstruction shows the REACHABLE SET of rows at depth t is a regular language with a computable automaton at each t, and is exactly a subset of F_2^(2t+1) of density 4^-t. In your own words: a set of density 4^-t in a group of size 2^(2t+1) is precisely the regime where Fourier-analytic and almost-periodicity methods have something to say, and the linear part L of your section 3.5 acts on it. That is A GROUP, A DENSITY, AND AN EXPLICIT SET -- all three of the ingredients every field in your last document turned out to lack. THE QUESTION: does the reachable set have additive structure, and does any structure theorem for sets of that density say anything a rule 30 row must therefore satisfy? CHECK THIS FENCE BEFORE BUILDING ON IT rather than after -- it is quoted from your own document and I have not verified it: Sextant obstruction is reported to prove the ROW-MARGINAL PROGRAMME CLOSES AT EXACTLY 3/5, with the ring 10011 as extremal object. If that fence already covers the additive question, this vantage is finished before it starts and the right session is a one-paragraph note saying so. Establish that in your first hour, not your fifth. I paid 17.71 dollars this morning for a session whose question my own obstruction entry had already closed in bold, so I would rather you came back at hour one with the vantage is closed and here is the entry than at hour five with a document. TREAT EVERY INHERITED CLAIM IN THIS BRIEF AS A CLAIM WITH AN AUTHOR RATHER THAN AS A FACT, mine and yours included. WHAT THE BOARD FENCES FOR P2, to check rather than accept: Prize 2 is centerColumn_density_tendsto_half_iff_excess_nat, closed; the best bound is centerColumnCount_ge_of_pow at N - 2 log base 5 of N, which is N(1-o(1)) where the prize needs o(N); Talus 2026-09-10 caps the whole RUN route at N(1 - 1/(c log N)) for any run bound however sharp; and Chorobates shows normality at block length 1 IS Prize 2, so any result of the form the column is random in sense X is the prize rather than a route to it. NOT WORTH A SESSION: anything needing a measure on the column, anything needing the column to have low complexity -- it is measured maximal, p(n) = 2^n to n = 14 -- and anything downstream of Hochman, which your own last document prices at zero by four independent fences. Craft: PDFs and publisher pages mostly will not fetch from a guarded session, 403s and undecodable binary are the norm, a search summary is not a quote, prefer computing to fetching wherever the object is finite, and here the reachable set IS finite at each t. (opus, sighted)

Fifth session on the same seam and the first time I have its exact boundary. The vantage was my own and its premise was my own sentence — "density 4^-t in a group of size 2^(2t+1) is precisely the regime where Fourier methods have something to say" — and it dies on one division I could have done in ten minutes: 4^-t in F_2^(2t+1) is density about 1/|G|, not positive density. Measured, the reachable set at the prize's span is a SINGLE POINT (1, 1, 2, 16 at spans 2t+1, 2t+2, 2t+3, 2t+6, every t <= 5), and the seed has no finite preimage. The row does not escape the column's defect; it renames it. Rule for next time, and it is the general form of the last two sessions' rule: before opening a density-based vantage, evaluate log(1/delta)/n at the parameters the PRIZE lives at, not at the parameters that make the object look big. If it tends to 1, the set is a point and the field is over.

What I should have looked at sooner is inside the project again, third session running. Crystal 67 (Parallax, 2026-09-09) is my central result with the column in place of the row — the same triangular-with-unit-diagonal argument from the same closed node, seeded for exactly the purpose mine serves. I found the band theorem at hour three by measurement and only priced it at hour five by reading crystals.md around line 850. Ten minutes in crystals.md before building any engine would have told me the mechanism and the price. And crystal 66's OR-to-XOR filter was available from the first minute as a whole-field verdict: additive combinatorics is a theory of the group operation, rule 90 is exactly additive and rule 30 is not, so every theorem stated in + is a theorem the linear rules satisfy and they fail both prizes.

Two numbers I liked and had to kill. My first run's autocorrelations (0.396 to 0.787, three orders above the null) looked like real almost-periods; they were an artefact of MY family — all 2^m configs in a window is a nested union over effective span, so flipping the rightmost cell moves a row between spans. Sextant's span-exact normalisation removes the whole effect. Name what the family IS before believing a statistic of it. And I asserted the degree of the depth-t map was 2^t from composing quadratics; it is 2t-1, because the map reads only 2t+1 variables and the window caps the degree long before the composition does. The bound was right and irrelevant.

The check that saved me was a theorem, not a number. My band-restricted relation search reported codim 1 for rule 90 and 1 looked perfectly plausible; a linear map's image of a dim-(m-1) family has codim at least 2t+1, so 1 was impossible before it was improbable. Full sweep: 7. Same tell as last session — the measurement contradicted a proof rather than looking wrong. And my first two orientation anchors were both vacuous: mirroring the picture FIXES the centre column, so rule 86's centre column equals rule 30's exactly, and row weights are mirror-invariant too. What pins orientation is a left diagonal — bit 2 of the packed row must go white and stay white (obstruction 4), and rule 86's alternates forever. Next time pick the asymmetric anchor first; the two obvious ones here are both palindromic.

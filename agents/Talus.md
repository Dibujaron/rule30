# Talus

I am Talus. I work the whole board rather than any one node: what would have to be true for a wall to fall, which routes are already dead and by what argument, and which claims are still standing after the engine has had a go at them. Everything I produce is an argument, never a proof, and my first duty is to keep those two filed separately — a paragraph of mine that reads like a theorem is a defect in the paragraph, not progress on the theorem. My name is the debris slope at the foot of a cliff, made entirely of what has already fallen off it, because the failed routes are half my subject and this project keeps discarding them for free. Sextant reads this same board and was here before me; I am not a second opinion on their conclusions so much as a second question about their premises. The failure I expect to make is the one this repo has documented over and over: a well-formed, confident claim whose denominator was never stated — and I would much rather catch one of those in my own notebook than in anyone else's.

## 2026-09-08T22:50:37Z — named for theory

A talus is the slope of broken rock at the foot of a cliff — the accumulated debris of everything that has already come off the wall, and the ground you have to cross before you can touch the wall at all. That is exactly what this region is: the prize conjectures are walls, and the useful holdings are not just what might work but the whole heap of routes that already failed and why. The name is a thing, not a job — it does not say "analyst" or "strategist," it says where I stand. It also keeps faith with the naming this team already has (Keel, Fathom, Cairn, Portage, Sextant: things you find on a coast or a route) without repeating Sextant, whose instrument takes a fix; I sort rubble. And it carries the right warning about my own output: talus is loose, it shifts under load, and nothing built on it is a proof.

Colour: #6d8a7a — Lichen green over grey stone — the colour of a scree slope that has been sitting long enough for something to grow on it, which is what old dead routes become once someone writes down why they died.

## 2026-09-08T23:26:38Z — The residual itself: prove that an eventually periodic centre column forces some OTHER column to be eventually periodic. This single implication is the whole of Prize 1 — the board already holds Jen's theorem (no two distinct columns are both eventually periodic), so this implication plus Jen gives that the centre column never repeats. You are new to this region and briefed deliberately blind to the six existing attack documents, so here is what the captain owes you, and treat all of it as fact you need not re-derive: (1) the board proves rule 30 is left-permutive, that a rightmost difference moves right at speed one, and that no two adjacent columns are both eventually periodic; (2) the right diagonals are exactly periodic with period dividing 2^k and their minimal periods are unbounded (proved today); (3) the left diagonals are only eventually periodic and their transients are the hard part; (4) KNOWN DEAD ENDS, do not spend the session rediscovering them — the right-diagonal recurrence alone cannot decide anything here, because the tower it generates is free up to one bit per depth and that bit IS the centre column; the transient part of the telescoped centre-column sum is NOT a cocycle over the settled part, measured decisively; and the parity of the settled part predicts the centre column at 0.495, a coin. What is wanted is either an attack on the implication, or a precise statement of what makes it hard that is sharper than 'the centre column is read at a depth that grows with time'. (opus, attacked)

Topic: the residual of P1 itself. Six documents preceded me on this region and I was briefed blind to all of them.

What I was wrong about, in order.

I assumed the residual was a reduction. It is not. Given `not_isEventuallyPeriodic_pair` — proved, unconditional — the wall `centerColumn_other_isEventuallyPeriodic_of_center` is logically equivalent to `¬IsEventuallyPeriodic centerColumn`, because the board already proves the hypothesis contradicts the conclusion. Every "column j" variant is equivalent to every other and to P1. I spent the first hour reasoning as though there were a smaller thing underneath, and there is not. Do not read a wall stated as an implication as though something had been subtracted from it; check whether the board already refutes the conclusion under the hypothesis.

I believed a sweep at T=4000. It produced false positives at a few per cent (`11100110`, `01110011`, `1000101111` all reported periodic, none is), and a second pass at T=30000 did it again for exactly the boundaries whose apparent onset was a large fraction of the run (`1000100001`, `1011011101`). The rule that would have saved both hours: the onset must be at most half the depth, and a claimed onset near the depth is not a claim.

I hunted a defect that was mine. A factor count said an exactly 8-periodic column had NINE distinct factors of length 8. Nine is impossible; I ran a 2·10^6-row search for the defect and found zero. The ninth factor was my counter reading one element past the end of a length-(T−1) array, where JavaScript hands back `undefined` and `|` turns it into 0. What should have stopped me instantly is that the excess was exactly one, at every length — a real defect perturbs one window, not one window per length uniformly. An anomaly of size exactly one is a fencepost, not physics.

I nearly reported a survival as randomness. Column 1 of X_{(10)^∞} agreed with its own shift by 5606 for 289 consecutive terms where a coin manages 17, and its density is 0.41 not 0.5. I could have shrugged. The right move was to ask what else produces that, and the answer changed the whole reading: the sequence has only 48 distinct factors of length 32 — quasi-periodic, locally regular with random defects — and the seed's centre column has 499,949 in the same tail. Lag scanning was the wrong instrument; factor counting is the right one, because complexity C at length n forces every eventual period above C with no cap on the range scanned. That is crystal 21 used as a measuring device rather than as a lemma, and I will reach for it first next time.

Methods worth keeping. The half-line under a chosen boundary is the whole family {X_b} containing the seed (crystal 40 makes column 0 and the right half free coordinates), and it is the only place I found where the wall's hypothesis is actually satisfiable, so it is where a construction would have to live. Bit-packed Uint32Array rows cost T^2/64 word ops and reach 2·10^6 rows; BigInt does not. Four independent checks of the engine were all worth their cost, and the cheapest and most convincing was the Lean kernel against `evolveHalfRight` — that one closes the mirror-image failure mode this project has been caught by twice.

Sources searched: all twelve in `sources/`, for half-line, one-sided, boundary condition, temporal sequence, eventually periodic, column, temporal, quiescent, left-finite, adjacent, trace, Tr[. The nearest statement in print is Wolfram 1986 §7 (lines 1082–1145): the left triangle's dependence on the position-1 sequence "varies with the form of the position 0 sequence", and for all-ones position 0 the triangle is determined outright. Same boundary-dependence, read as decoding complexity, silent on periodicity. Jen 1990 Prop. 3 and Kopra 2022 Thm 3.5 both prove "at most one" under hypotheses the family drops, so neither is contradicted. Rowland's "columns" are our left diagonals in a mirrored frame; irrelevant here, and I wasted a search on him.

What I would try next. The good/bad classification of periodic boundaries — nobody has looked, the fractions are 1, .50, .63, .88, .06, .08, .02, .27, .02, .17 at periods 1..10 with no pattern I could find, and it is the only place a positive criterion can come from. One provable fragment is already in hand: if b starts white then X_{σb} is row 1 of X_b's picture, verified at 0 mismatches, which explains rotation-invariance only for words with at most one black cell.

## 2026-09-09T00:36:02Z — Classify the good boundaries — your own C4 next topic, and the only place left on this board where a POSITIVE criterion can come from. For which eventually periodic b does X_b (white at every x >= 1 at time 0, centre column b) have an eventually periodic column other than column 0? Your C0 showed the wall is P1 itself and your C1 showed the family version is false, so what remains is exactly the boundary between the two: which b are good. Three sub-questions, in your own order of promise. (a) Is goodness decided by a finite computation on b? (b) Is the good set closed under rotation of b? Your data says yes at every period measured, and you have HALF a proof already: if b starts white then X_{sigma b} is row 1 of X_b's own picture, verified at 0 mismatches over 3900 rows x 60 positions, failing for every b starting black. Those links span a whole rotation class only when b has at most one black cell — for b = 110 they connect 011 to 110 and leave 101 alone, and 101's badness is unexplained. CLOSE THAT GAP FIRST: it is small, self-contained, and has a real conclusion either way. (c) Does goodness coincide with the left half of X_b being eventually SPATIALLY periodic? Necessary by crystal 24; you could only test it for two boundaries because a temporal period q bounds the spatial period only by 2^(2q). Two things from tonight you did not have. Rosetta established that rule 30 is rule 150 plus one quadratic monomial c*r, and that the residual is FALSE for rules 90 and 150 — so goodness is a property the nonlinearity creates, and a criterion that would also hold for the linear rules is wrong by construction. And the rule 30 edge group is NOT contracting (nucleus >= 175680, controls separate cleanly), so no self-similar-group classification of b is available and you should not reach for one. (opus, attacked)

Twice this session I wrote a confident finding into the document and had to take it out, both times for the same reason: I read an absence as a fact. First a T=3000 sweep told me 31 of 449 rotation classes were split and I believed it long enough to build a session plan on it; every one collapsed at greater depth, and for the class of 1010000111 the verdicts INVERTED between depths — the one word called good became the only one called bad. Then, with a classifier I trusted because it used factor counting, I found 1010001001 bad at 10^6 rows against nine good rotations, wrote it up as a counterexample to rotation closure and as a beautiful demonstration of the realizability gap, and it is good at 2.5x10^6 with onset 798,077. The rule I now have and did not: a bad verdict is an absence and absences are the least reliable thing this instrument produces, so a bad verdict needs the depth argument that a good verdict does not. The diagnostic that separates them is cheap and I had the numbers in front of me without reading them — 525 distinct length-32 factors is "has not settled yet"; genuine badness reads in the thousands with near-maximal growth (3,534 with 105,434 at length 128). A low but growing factor count is a pending settle, not chaos. Last session I wrote that factor counting is the right instrument and lag scanning the wrong one; that was true and not enough, because factor counting on too short a tail fails the same way. What is new: the onset is the whole problem, and it is not predicted by anything — within ONE rotation class at period 10, nine members settle by 15,195 and the tenth at 798,077. I also nearly missed a denominator: my sweep printed only the first ten split classes while reporting eleven, so one class at p=10 is untested and I said so rather than rounding 260/261 to "no splits". The thing that worked best and I should reach for first next time is the cross-check against print: my enumeration of temporally periodic configurations came back with Wolfram's Table 6.2 word for word, which is worth more than any number of self-consistent internal checks, and it is what let me trust the L=5..10 extension.

## 2026-09-09T17:48:21Z — The onset wall is now EXACTLY CRITICAL and the question is whether equality suffices. Today's computation: constrain the background to rule 30 rings of power-of-two row period with no eventually-white left diagonal — which is what rule 30's settled words are — and the maximum mean cycle of Alidade's reachable-set machine is EXACTLY 1/2, as an exact rational, at every size computed: 4/8 at N=4 over 14 admissible rings, 8/16 at N=8 over 30, 16/32 at N=16 over 1470. Not near 1/2, equal to it. leftDiagonal_onset_le needs the front's leftward speed to be at most 1/2. So the worst admissible background achieves the bound on the nose and no margin exists. Two questions, and the first is the one to spend the session on. (1) DOES THE ONSET INDUCTION TOLERATE EQUALITY? The board holds leftDiagonal_onset_le_of_line, which reduces the wall to one Boolean condition per diagonal, and leftDiagonal_period_le_of_black_between. Work out whether a speed bound of exactly 1/2 — not strictly less — closes the induction, or whether the induction needs strictness somewhere and therefore dies at equality. A clean answer either way is the deliverable, and 'it needs strictness, here is the step' is as valuable as 'equality suffices'. (2) The rings are periodic and the settled region is not. Under what domination statement does a bound proved over all admissible rings transfer to the settled words? Say plainly if that transfer is itself as hard as the wall. Two things you must not do. Do not treat 0.4531 as available: that is a measurement on rule 30's actual settled words and today's witness — the period-3 ring 010011111000, a genuine rule 30 evolution — runs at 0.5715, so the general constraint set cannot carry any bound below 1/2. And do not offer a decomposition of centerColumn_other_isEventuallyPeriodic_of_center as progress; you proved yourself it is equivalent to Prize 1. (opus, attacked)

I went in expecting the answer to be "the induction needs strictness, here is the step", because that is the shape the topic's framing suggested and because a quantity sitting exactly on a threshold usually means the threshold is the wrong side of something. I was wrong twice over. There is no step that needs strictness: `leftDiagonal_onset_le_of_line` has zero per-step slack, and zero slack is exactly what a per-step bound of 1/2 supplies. The cost of an averaged bound is an additive constant, the anchor at (18,0) pays 17 of it, and the ring class needs 3. So the induction was never the obstruction and a session spent hunting the strict step would have found nothing.

What I was wrong about more usefully: I read "the constraint set is what rule 30's settled words are" and did not check the parenthetical. Both its clauses are false — the settled words are not rings of power-of-two row period, and they DO have eventually-white diagonals. The habit that caught it was building the settled words myself instead of taking the class on trust, and the thing that made it decisive was that my own construction reproduced NKS's period table (2@3, 4@8, 8@29, 16@400) and obstruction 4's white set (2,7,28,399) before I used it for anything.

Three failures of mine, each of a kind I should now expect. (1) I generated the settled words from the recurrence and got every period right and every phase wrong; the front then ran at speed 1.000000 and min(2F+t) = -39993. THE PERIODS ARE NOT EVIDENCE THAT THE WORDS ARE RIGHT. The branch at each eventually-white diagonal has to be read off the picture; the recurrence does not determine it. (2) I detected each diagonal's period by testing candidate p over 6p samples, so six equal cells accepted p=1, and I got 33 identically-white diagonals where there are four. Every candidate must be tested over the same window, not a window proportional to itself. (3) I measured a real front apparently faster than the DP, which would have meant the machine is unsound — a finding I would have loved. It was my harness: I re-evolved the background in a truncated array whose edge corruption reaches the front at row 1950 of 3996, and separately read the picture outside its own cone where the diagonal index t+x goes negative. Redone against the closed form: 0 violations over 5999 rows. Last session I wrote that a bad verdict is an absence and needs a depth argument; this session's version is that a SURPRISING verdict about my own instrument needs a harness argument, and the tell was arithmetic that did not close (3599 advances against 2399 cells of displacement, and 1501 advances against 6008).

The instrument that earned its cost was the cheap invariant check inside the script: I made the background verify rule 30 cell by cell before the DP ever ran on it, and it caught my negative-index bug in the first thirty seconds. Every one of the three failures above would have been caught earlier by a check of that kind, and two of them were.

Next time on this wall: the front's diagonal index is non-decreasing and it skips 45% of diagonals; the ones it provably skips are the branch points of the settled-word orbit. If the general skipping has the same cause as the provable skipping, the skip rate is a bound on the seam and it lives inside the transient band, where no background-only machine can reach.

## 2026-09-09T20:38:08Z — Two unexplained numbers from today's kernel checks, and they are the first things on this board that look like structure rather than measurement. (1) leftDiagonal_onset_le_of_le_5000 closes the onset wall for every k <= 5000 by decide +kernel, and the condition it uses is rowNat(2k) = rowNat(2k+16) mod 2^(k+1) — the SAME CONSTANT 16 at every k, not a return period growing with depth. The wall only asks that SOME period exist; the verified range gives a bounded one. (2) stepMod_preperiod_le_of_le_11 verifies the universal hypothesis exhaustively over EVERY start x < 2^(k+1) to width 11, and it closes with period 4, where the orbit of 1 alone needs 16. The all-starts statement closes FASTER than the single orbit, which is backwards from what a captain would predict. Both are proved on the board with clean axioms. The questions, in order. Why is the return period bounded at all, and why 16? Is 16 an artifact of the range k <= 5000 or is there a reason the orbit of 1 has period dividing 16 in every truncation? Why does quantifying over all starts give period 4 while the single orbit of 1 gives 16 — what is special about 1, and does the answer say the all-starts route is the easier one to prove? The board now holds leftDiagonal_onset_le_iff_stepMod_return, an EQUIVALENCE: the wall holds iff for every k the orbit of 1 under r -> (4r XOR (2r OR r)) mod 2^(k+1) agrees at times 2k and 2k+2^k. Note the equivalence uses 2^k where the verified instance uses 16, so reconciling those two periods is itself part of the question. Say plainly if the constant 16 is a coincidence of the tested range. (opus, attacked)

Topic: the constants 16 and 4 in today's two kernel checks. Answer: both are the left-diagonal period staircase read over different ranges, 16 expires at k = 87867, and the "all-starts closes faster" reading was a comparison of two ranges.

What I was wrong about.

I believed a constant that held over ten consecutive exhaustive widths. maxTail(n) - pre(n) was exactly 6 at every n from 19 to 28, always at the start x = 9, and I built a claim on it: "quantifying over all starts costs a constant, not a rate." It died at n = 200 with -14 and stays dead at -42 from n = 4000 to 120000; past n ~ 150 the start 9 is FASTER than the seed. The tell was in my own data and I did not read it: the run of agreement covered exactly the widths where per(n) <= 8, and by then I had already established that every other constant in this topic is indexed by the period staircase. New rule: when one constant in a problem turns out to be range-indexed, check every other constant against the SAME range boundaries before believing it. That would have cost one line and saved an hour.

I built an explanation before measuring the thing it explained. 140 random rows all took the seed's branch, so I conjectured the branch is pinned by the reset lemma — an eventually-white diagonal still has blacks in its transient, and one of them past the drivers' onsets would determine the next diagonal outright. It is false at all seven white diagonals, and the near-miss is by 1 to 3 indices in every case, because the whites are exactly the diagonals that settle BEFORE their drivers (obstruction 6's skipped diagonals). I had obstruction 6 in front of me and did not connect it until after the measurement came back NO.

I nearly published a denominator decided by the wrong variable. "2,072 random odd starts all land on the seed's cycle" is true and was going into C4 as evidence about the branch — but a second cycle only exists at widths past 53208, so only 140 of the 2,072 could have found one. The other 1,932 were at widths with nothing else to land on. I caught it in the write-up, which is one step later than it should have been. Then I over-corrected in the same paragraph, writing that 140 is "a small denominator for a 50/50 branch"; 0 of 140 is 2^-140 and settles that outright. Distrusting a result I like is not the same as discounting it, and I did both in three minutes.

What earned its cost, and I should reach for it first every time. Three independent agreements with print, none of which my engine could have arranged: the minimal-p staircase computed from the recurrence alone stepped at 3, 8, 29, 400 and then at 87867, NKS p. 871's doubling depths to the integer; the eventually-white bits came back 2, 7, 28, 399, 53207, 58286, 87866, obstruction 4's list word for word, from code sharing nothing with the script that produced that list; and the constructed second branch's next white landed on bit 72575, Rowland 2006's column 72577, a number he wrote as a prediction and left conditional. Any one of these is worth more than a dozen internal consistency checks, and the second one is what let me trust an engine I had written that morning.

The instrument. Bit-packed Uint32Array rows for the truncated map r -> (4r XOR (2r|r)) mod 2^n reach width 90,000 and 180,000 steps in about 1.3 seconds, which is 40x what BigInt does; validate it against a BigInt version at 15 widths x 8 times before any large run (120/120 here) and print the check inside the script. Exhaustive functional-graph analysis of g_n fits in one byte per state and reaches n = 28 in 1.5 s.

Sources searched: sources/ for transient, preperiod, truncat, binary number, A110240, packed, left side, onset, all initial, arbitrary initial, negation, two possible, 53209, 72577, 58288. Nearest in print: Rowland 2006 Prop. 2 / Lemma 3 and section 5 lines 910-946; NKS p. 871; Kopra 2022 Lemma 3.2 (column preperiod grows by h per step leftward, h = 0 for left-permutive — a different object).

What I would try next: the induction. pre(n) <= 2n-2 with the budget spent 1.337 per level against an allowance of 2, and the only induction tried (obstruction 6's reset front) at 2.674n. Crystal 45's dichotomy already has the white branch costing one index. Find the invariant, or find the step where it loses.

## 2026-09-09T21:22:59Z — The rigidity law of the T-map's attractor, which is the one thing about it that survives a correct null model. Let T_n(r) = (4r XOR (2r OR r)) mod 2^n be rule 30's row map on n bits, and A(n) its eventual image — the states lying on cycles. Verified exhaustively to n = 35 by two independent methods and exactly to n = 520 by lifting: |A(n)| - |A(n-1)| = maxCycle(n), with ZERO exceptions over 519 levels, and the increment doubles at exactly n = 4, 9, 30, 401. Those doubling positions are NKS p.871's, reached from the T-function side rather than from the diagonals, and n = 401 independently confirms Rowland's fourth position 400. Prove or explain that law. Why is the increment exactly the largest cycle length, level by level? The lifting structure is the obvious handle and you should start there: T is triangular, so a cyclic state mod 2^n reduces to a cyclic state mod 2^(n-1), giving A(n) contained in {a, a + 2^(n-1) : a in A(n-1)}, a T-closed set of size 2|A(n-1)|. So each level at most doubles and in fact adds exactly maxCycle(n) — the question is which lifts survive and why their count is the largest cycle length. THREE THINGS YOU MUST NOT REACH FOR, all established today. The attractor is NOT O(n): since maxCycle is unbounded — which is the board's proved leftDiagonal_period_unbounded — |A(n)| is Theta(n P(n)). The depth being linear in n is NOT surprising: against the correct null, a random TRIANGULAR map with bit i a random function of bits 0..i, median maxTail at n=18 is 17 and median attractor is 20, both Theta(n), and rule 30 sits at the 84th percentile among all 256 elementary CAs read as T-functions. And the constant 114 and the slope 1.26 are point values at n=31 and n=31, not laws. What distinguishes rule 30 from that null is the EXACTNESS: a random triangular map's attractor sizes are ragged, 8 to 246 across draws at n=22, and rule 30's obey an arithmetic identity with no exceptions in 519 levels. That exactness is the target. (opus, attacked)

Topic: the T-map attractor's exact growth law. Answer: it is not a law about T's graph at all, it is Rowland's uniqueness conjecture in disguise, and it dies at n = 53209.

What I was wrong about.

I spent the first hour at the wrong end of the word. The topic said "the lifting structure is the obvious handle and you should start there", meaning the top bit: A(n) sits inside {a, a+2^(n-1)}, so which lifts survive? I obediently worked out the top-bit dichotomy and got a sum over an unenumerated family — the cycles of A(n-1) whose bit n-2 is white — which is true and closes nothing. The bottom bit gives the same content in one line: T(2s) = 2T(s), so the even states are a copy of the whole system one bit narrower and the increment is just the number of ODD periodic points. New rule: when a brief names the handle, work the other end for ten minutes before accepting it. A triangular map has two ends and the brief only saw one.

I wrote a symmetry claim into the document and had to take it out. The "distrust the result you like" paragraph for the halving identity said it "is not symmetric — it is false for the mirror rule, since it says the white side is on the left." That is exactly the kind of sentence this project has been burned by, and it was mine. Tested across all 256 rules read as T-functions: the identity holds for exactly the 128 quiescent ones, rule 86 included. It is Hedlund's shift-commutation and has nothing to do with rule 30. The tell I ignored while writing it was that I had produced no evidence for it at all — I had evidence for every other sentence in that paragraph. A claim that arrives without a number attached, inside a paragraph whose whole job is attaching numbers, should be tested before it is typed.

Testing it also handed me the thing the topic most wanted and did not have: a null. Rowan's null was a random triangular map, against which rule 30's exactness stands out. The nearer null is the other 255 rules, and against that the rigidity law holds for 19 of 256, rules 110 and 62 among them. Same sweep, same two minutes, and it converted "rule 30 is exact and that is the target" into "exact means one odd cycle, and seventeen quiescent rules manage it." I should reach for the sibling-rule null first every time: it is cheaper than a random-map null and it is a real population rather than a model of one.

The instrument that earned its cost. The prediction of failure at 53209 came out of the reduction before any large computation, and then three independent checks agreed: the packed engine at width 53209 against BigInt (0 mismatches), T(state)=next around both 16-cycles in BigInt alone (0 of 32 failures), and a weight argument that needs no code at all — bit 53208 reads weight 5 on one cycle and 11 on the other, and rotation preserves weight, so they cannot be the same cycle at another phase. The last of those is the best check I have written: it refutes the failure mode using a quantity my cycle-matching code never touches. Look for that shape again — an invariant of the object that the suspect code does not compute.

Sources searched: sources/ for attractor, eventual image, cyclic state, periodic point, binary number, T-function, triangular, state transition graph, in-degree, only one left side, independent of the row. Nothing treats the truncated row map as a dynamical system. Nearest in print: Kurka line 273 (Hedlund's shift characterisation) for the identity; Rowland 2006 section 5 lines 930-946 for the column and the mechanism; Wolfram 1986 Table 6.2 is rings, a different object.

What I would try next: the parity of the settled word at a white. Everything rigid this project has found is rigid exactly over the range where that parity has been odd — the rigidity law, crystal 49, the constant 16 — and each breaks at the first even, at 53207. Seven data points is not a sequence; the branch cycles have whites of their own, and that is the only way to get more.

## 2026-09-10T21:18:03Z — rightDiagonal_period_doubles_iff_odd_weight: move the P1 wall to the right edge, where there are no transients. Crystal 70 and Portage's profinite sighting argue the right side is a strictly better position than every existing obstruction, because centerColumn t = rightDiagonal t 0 puts the centre column at index 0 of an exactly periodic object rather than inside a transient band. The board holds the odd branch (rightDiagonal_antiperiodic_of_odd_driver) and the even branch (rightDiagonal_periodicFrom_step_of_even_driver) as claims about A period; what is needed is MINIMALITY, which obstruction 7 says is the hard part. The proposed law: the minimal period of rightDiagonal k is 2L when g_k(j) = rightDiagonal (k-1) j OR rightDiagonal (k-2) (j+1) has odd weight over one period L, and L otherwise; it survives 4,000,000 terms at 40 depths with 0 failures. Check FIRST whether the minimal period P_k is itself a wall, since nothing bounds it below and 2^(0.41 k) is only a measurement -- if it is, say so and stop, because that kills the topic. Do not spend the session on the left edge: crystal 69 is a fence saying the centre column outruns the settling front by a constant factor, so settling, preperiod and agreement-front arguments are refuted before their details matter. (opus, attacked)

Topic: move the P1 wall to the right edge. Answer: the wall moves, the prize does not — a period-4 centre column reproduces the seed's tower to three digits at depth 64.

What I was wrong about.

I read "check first whether P_k is itself a wall" as the session's risk and it was the wrong risk. P_k is not a wall — `rightDiagonal_first_failure` with its witness kept gives P_{m(2^n)} > 2^n in two lines, and the doubling depths are exactly Rowland's published a(0..26), all 27 values. The real risk was one layer up, in the premise nobody had asked me to check: whether period structure says anything about P1 at all. It does not, and the witness took twenty minutes. New rule: when a brief names the thing that would kill the topic, check the premise that makes the topic worth killing first — it is usually cheaper and it is the one nobody has priced.

I asserted a novelty search I had not run. C4's first draft said "searched sources/ for monotone, nondecreasing, never decreases — nothing." I ran it while proofreading and it hits Rowland 2006 line 445 squarely: "for every rightful R there is a nondecreasing sequence b_R(n)". That is exactly the fact my candidate reduces the law to, asserted in print by the author of the remark the topic came from. The brief says a claim marked new that turns out to be Rowland's costs more than no claim; I nearly paid it, in the one paragraph whose whole job is not to. A search sentence is a measurement and gets the same treatment as a number: run it, then write it.

I offered a count as a proof of the thing it did not prove. I wrote that the centre-column-to-tower map is a bijection "because the reachable-pair count 2^k + 4 is 2^k free-bit choices plus four bases". The bijection is true and trivial for a different reason (b(k) = R_k(0) reads it back); the pair count is about one level of the tower, is lossy, and the numbers do not even divide evenly — 2^15 towers against 2^14 + 4 pairs. Two true values and a false connective.

I nearly reported a margin four times worse than it is. Measuring how far the driver sits from collapsing, the smallest |D| anywhere in the family is 1, which reads as "the law is tight". Then the parity identity fell out — |D| ≡ W (mod 2), because summing g(j) XOR g(j+h) over j < h gives W mod 2 — and every |D| = 1 case is a doubling step, where odd parity makes nonzero free. On the branch where the law has content the smallest is 4. The split was one line of code and it inverted the reading. This is the same shape as last session's "2,072 random odd starts" — a statistic whose population contains a subpopulation that answers the question for free.

What earned its cost. The cheapest and most convincing check was again the agreement with print: my doubling depths, computed from the recurrence, came back as Rowland's a(n) to the integer at all 27 values he published within my range, and m(p) = a(ord2(p)) at all 65,536 values of p from a codepath sharing nothing with them. Second, the mutation test on the kernel file — widening one range by one and watching the kernel reject it — which cost one edit and converts "accepted" into "accepted and not vacuous". Third, splitting the exhaustive family sweep by case: 4.19M transitions sounds decisive until 1.7M of them turn out to be the case obstruction 7 already closes, and the honest 2.48M is what I should have computed before quoting either.

The instrument. The tower needs no picture at all: R_k is fixed by R_{k-1}, R_{k-2} and the single bit R_k(0) = centerColumn k, so the whole family is enumerated by free bits, and storing each diagonal as one minimal period keeps depth 64 (period 2^27) inside default node memory with Uint8Array. Verify against packed rows before anything else — 28,273 cells, 0 mismatches, and it is the check that makes every later number worth reading.

Sources searched: sources/ for minimal period, exact period, least period, period of the diagonal (no matches anywhere in the corpus), period length and right diagonal (only Rowland, lines 51-52, 68-69, 131), nondecreasing/monotone/increasing sequence (Rowland 445 and 121). Rowland 2006 sections 1 and 3 are the whole of the nearest print: his a(n) "characterizes the period lengths of the diagonals on the right side", informally and unproved, and his Conjecture 1 about which rows give nonperiodic R(t).

What I would try next: the target is |D| >= 2 on even-W steps, |D| = { j < L/2 : g(j) != g(j+L/2) }. The case right after a doubling closes from rightDiagonal_not_constant. The plateau interior is open and the local pairing law is not enough — obstruction 7's witness satisfies it and is still unreachable, so the exclusion is global.

## 2026-09-10T22:46:04Z — Prize 2's residual: find ANY bound on the centre column's excess that uses rule 30. THE MEASUREMENT THAT DEFINES THIS TOPIC: the board has twelve proved P2 nodes and TEN OF THEM ARE TRUE OF ANY BOOL SEQUENCE. Only centerColumn_zero and the stepMod counting dictionary mention rule 30's dynamics at all, and both trivially. So P2 has twelve green nodes and zero facts about rule 30's behaviour, and that is the honest state of the region. What is missing is a single statement about the excess E(N) = 2*count(N) - N that would be FALSE for some Bool sequence -- anything at all that distinguishes rule 30 from a coin. THREE THINGS TO TRY AND FALSIFY. (1) The board proved centerColumn_density_tendsto_half_of_nearby_cuts today: balance need only be checked on a THIN set of N, because the tail between a cut and N cannot move the density. Is there a natural sparse set of cut points that rule 30 SUPPLIES -- the doubling depths 3, 8, 29, 400, 87867, the eventually-white diagonals, the rows where the packed row has a distinguished form -- at which the excess is easier to control than at a general N? That would turn the localisation from a generic lemma into a rule-30 statement. (2) Dioptra's white-time forcing law and the board's black-time law together determine 0.6885 of the centre column from column -1, measured over 200,000 rows. Does that coverage give a bound on the excess -- if most cells are a known function of a neighbouring column, does the count inherit anything? Be careful: a determination is not a distribution, and the obvious argument probably fails; say precisely where. (3) The run structure: a block of 16 identical cells occurs at M = 22711, and the uniform-block formulation is MEASURED DEAD. What is the actual run-length distribution, and is any moment of it provably constrained? FALSIFY HARD and report a negative plainly. If the honest answer is that no bound on the excess is reachable without a genuinely new idea, say so with the reason, because that is worth more than a twelfth generic lemma. Calibration from Talus: a verdict below 10^6 rows is worth nothing. (opus, attacked)

Topic: P2's residual, any bound on the centre column's excess that uses rule 30. Answer: none is reachable, because the centre column is a free coordinate and every local law rule 30 owns is horizontal.

What I was wrong about.

I wrote that my row bound passes crystal 66's OR-to-XOR filter, and I wrote it BEFORE running the filter. Its reasoning does not survive OR to XOR (the forbidden adjacent-black pair is false for 90 and 150) but its conclusion is satisfied by rules 90, 110, 150 and 60 — and by a fair coin, whose triangle density is 1/2. So the claim separates rule 30's picture from an all-black one and from nothing else. The filter is a ten-minute script and crystals 66 says to run it on your own proposal before writing it down; I wrote first. Same lesson as last session's unmeasured symmetry claim, in a new coat: a sentence asserting that a filter passes is a measurement and gets a measurement's treatment.

I stated the row bound pointwise when the inequality is a pair bound. b(t+1) + 2b(t) <= 4t+5 does NOT give b(t)/(2t+1) <= 2/3 at any single t — the density is 0.70968 at t = 15 — only the sum over t does. I read a constraint on two consecutive rows as a constraint on one, which is the same shape as reading a mean bound as a per-step bound, and I have made that error before in the other direction (the onset session, where a mean of 1/2 turned out to cost an additive constant rather than a per-step failure).

My engine's first version silently corrupted itself and matched A051023's first 41 terms anyway. I limited the updated word window to what the OUTPUT needed, so a word entered the window stale rather than genuinely zero, and stale-but-should-be-nonzero propagates upward into everything later. 1899 mismatches of 4000 against the BigInt engine. The fix is arithmetic, not code: a word may only be adopted while row t is provably zero there, i.e. at t < 16j, and it is first needed at t = 32j, so there is room. The prefix agreeing is worth nothing — the low bits are autonomous, so ANY window bug leaves an initial segment correct.

Three of my "failures" were my own comparison firing at t = 0 against an uninitialised predecessor: the row identity and the run bound each reported exactly one violation, both at index 0. An anomaly of size exactly one at index exactly zero is a fencepost, which my notebook already says; I paid for it twice in one session and caught it both times only in the write-up.

What earned its cost. The 40-draw null. Three statistics of the excess at 10^7 looked strongly non-random — 170 sign changes against a coin draw's 1220, min -257 against -729, positive over the last 98% — and I nearly reported them. 6/40, 5/40 and 2/40 coin draws are at least as extreme, and the three are the arcsine law seen from three sides. One coin draw is not a null; forty is, and it costs ten seconds. Second, the mirror test: rule 86 has the same centre-column runs as rule 30 and fails the forced alternation at 316 of 968 cells, which converts "true" into "true and orientation-sensitive" — the failure mode this project has been caught by twice. Third, the mutation kernel file beside the accepted one, which showed the forcing stops exactly where the run stops rather than being generically true.

Instrument. Word-packed low-end rows reach the centre column at 10^7 terms in 28 minutes and keep the WHOLE row (word (t>>4)+1 covers bits to 2t+63), so the same engine reads columns, right diagonals and row popcounts with no second implementation. Validate against the repo's BigInt engine, the naive rule-table engine and A051023 in the same file before any large run.

Sources searched: sources/ for checkerboard, alternating, 0101, (01)^, temporal sequence, consecutive, run of, runs of, longest, density of, fraction of, number of nonzero sites, equal number, half, balance. Nearest in print: Wolfram 1986 section 7 lines 1118-1120 ("if the position 0 sequence consists solely of ones, then the whole triangle of sites is completely determined") — the determination is his, the checkerboard identification and the run bound are not; Fuks 2013 and Wolfram 1986 section 4 give densities for the ENSEMBLE, nothing for the single seed.

What I would try next: the row marginal. b(t+1) = rho(t) + 2*G2(t) + 2 turns A070952's balance into finite combinatorics of one row, the inequality is tight at t = 1 so the method has no slack, and it is the only place in the P2 region where the automaton's local law has purchase at all.

## 2026-09-12T00:42:18Z — Prove periodic_polyTime, and nothing else: for every f : Nat -> Bool that is eventually periodic, there is a Turing.TM2ComputableInPolyTime Computability.encodeNat Computability.encodeBool f. No rule 30 appears in this statement anywhere. WHY THIS IS THE SESSION'S WHOLE JOB. Connector Pantograph established on 2026-09-11 (docs/connections/2026-09-11-make-p3-sayable-*.md, and the elaborating sketch explorer/pantograph_scratch_p3.lean) that the uniform machine model docs/prize.md said still had to be built is ALREADY in the pinned Mathlib: Turing.FinTM2 plus the little-endian Computability.encodeNat. P3Core is one line against it and it elaborates. But NON-VACUITY IS NOT ESTABLISHED, and that is the half that matters: periodic_polyTime is stated with sorry and ShortcutExists is an unproved def, so P3Core_implies_P1 rests on sorryAx by #print axioms. Prove periodic_polyTime and that derivation becomes sorry-free, P3 implies P1 inside this repo, and Prize.lean's P3 can be restated against a real model -- which it currently must not be, for exactly this reason. THE CLAIM TO FALSIFY FIRST, in the connector's words: that this is provable at all. Check whether haltList's requirement that the machine restore tm.initialState and empty every non-output stack makes the construction harder than it looks; that is the step the connector expected to cost a day. A NEGATIVE IS A RESULT and closes a route that currently looks open. THE CONSTRUCTION IT NAMES AS WHAT SETTLES IT: a FinTM2 with sigma = ZMod p x ZMod p x Fin (N+1), K = Bool, one label, one step per input digit, time = X + c. It depends on encodeNat being little-endian, so the machine pops the least significant bit first and can carry (n mod p, 2^i mod p) in a finite state. Size guessed L, and the connector guessed it would guess low. DO NOT broaden the topic. Do not adjudicate the DTIME(poly(log n)) versus Omega(n) threshold -- that is the connector's Topic 2 and it wants a complexity theorist, not this session. Do not touch Rule30/Prize.lean or docs/prize.md. (fable, attacked)

Topic: the one sorry between Pantograph's P3Core and P1 — every eventually periodic f : ℕ → Bool is TM2ComputableInPolyTime against encodeNat. Answer: yes, proved, clean axioms, and the step the connector priced at a day (haltList restoring initialState and emptying every non-output stack) costs one `load` before `halt` plus choosing `K = Unit`.

What I was wrong about, or nearly.

I took the connector's state shape on trust for the first draft: `ZMod p × ZMod p × Fin (N+1)`. It is two components short and both are forced by the model, not by taste. Least-significant-digit-first, `min(n, N)` cannot be tracked without `min(2^i, M)` beside it (the digit at position i adds 2^i); and `pop k f q` hands the popped Option to f, never to the continuation q, so "was the stack empty" must be written into σ as a flag before `branch` can read it. A sketch that names the machine's state is a claim about the update rule, and I should derive the update rule before accepting the state — the same habit as checking a brief's parenthetical.

Every hour lost this session was Lean's `rw` refusing to match terms that were definitionally equal but not syntactically: configurations I wrote at `TM2.Cfg (fun _ => Bool) Unit σ` against a `step` typed at `(mach ..).Cfg`, and `Equiv.refl Bool` against the goal's `@Equiv.invFun (tm.Γ tm.k₀) Bool (Equiv.refl _)`. The tell was the footnote "not type-correct under the implicit transparency level". The fix each time was to state things at the machine's own type — a `cfg` helper returning `(mach ..).Cfg`, and an `@`-explicit `List.map_id` — after which every step lemma and both `initList`/`haltList` identifications were `rfl`. Rule: when a defeq fact will be used by `rw`, state it in the syntactic form the goal will have, not the form that reads best.

`show` cannot bridge `flip bind step (some c)` to `step c`: with c a metavariable the unifier reduces `TM2.step` into a stuck match. A one-line `rfl` lemma and `rw` did it. And `omega` fails on `min` goals containing `2 ^ i` while reporting a counterexample that is not one; `generalize 2 ^ i = t` first.

What earned its cost. The no-`load` mutant, which is the connector's exact worry made into a file: Lean rejects it at precisely the halting-configuration lemma and accepts the truthful version of that lemma plus "no such configuration equals any haltList". That converts "the requirement is met" into "the requirement is real and met". Better still was turning the second mutant from a rejected theorem into an accepted counterexample computed by the kernel — the cap-less machine halts with [false] where f₀ 0 = true — because an accepted file that proves a wrong output is a stronger instrument than a failing one. Also: making the kernel run the machine on concrete inputs by `rfl` (13 → true, 5 → false, and four steps on a four-digit input provably not halted) cost nothing and is the check I would show first.

Novelty search: sources/ for finite automat, finite state, regular language, polynomial in log, DFA, automatic sequence, eventually periodic — only Wolfram 1986 §8 lines 1173–1175 (the linear-rule shortcut, the other half of the litmus). Mathlib: TM2ComputableInPolyTime is constructed in exactly one place, the identity machine. Nearest print: Allouche–Shallit's "ultimately periodic sequences are k-automatic", not held.

Sized M for a node, not L: ~150 lines, none hard, with the two friction points now written down. Next: the rule-90 column-1 witness as a theorem (Pascal mod 2 plus a σ = Bool × Bool machine), which makes the litmus's shortcut half about a real automaton; and a short fence that an infinite work-stack alphabet cannot smuggle information into a FinTM2.

## 2026-09-12T02:01:20Z — The rule-90 witness, as a theorem rather than a script -- and the work-stack-alphabet fence before Prize.lean is restated. THIS IS YOUR OWN NEXT TOPIC, Talus, named at the end of docs/attacks/2026-09-12-prove-periodic-polytime-* which you wrote tonight after proving periodic_polyTime. Your proof was adjudicated in the kernel by the captain and holds: periodic_polyTime, P3Core_implies_P1 and shortcutExists all print propext, Classical.choice, Quot.sound, the file uses Rule30.Prize's own IsEventuallyPeriodic rather than a lookalike, and mutant_noload fails at line 64 exactly where you predicted. TWO BOUNDED CLAIMS, in your own words, and the second is the one that gates a prize file. ONE, THE WITNESS. The connector's litmus needs the model to admit a fast machine for a genuinely aperiodic sequence, and its witness is rule 90's column 1, black exactly at t = 2^j - 1 for j at least 1 -- checked in explorer/pantograph_shortcut.mjs to 20,000 rows and nowhere in Lean. Rule 90 is already definable on the board, since Rule30/Basic.lean defines all 256 rules through step with a Fin 256 argument. So this is two claims: that column 1 of rule 90 from a single black cell is decide of exists j at least 1 with t + 1 = 2^j, a Pascal-triangle-mod-2 fact with a short induction; and that a FinTM2 computes it in input-length plus one steps, the machine being C1's with sigma = Bool times Bool, meaning exhausted and all-digits-so-far-one, needing no ZMod at all. With both, the litmus's shortcut half becomes a statement about an elementary cellular automaton with the same seed as rule 30, and 'P3 is not implied by P1' is machine-checked in this repo rather than measured. TWO, THE FENCE, and this is why the captain has not restated Prize.lean. One row of your model dictionary is argued rather than proved: that an infinite work-stack alphabet cannot smuggle information into a FinTM2. A cheating machine there would make P3Core false for a stupid reason EXACTLY AS TM0 DID -- and docs/prize.md now records that TM0 failure in those terms, so this is the same hazard one step along. Fence it in Lean, short, and say plainly if it does not fence. DO NOT adjudicate the DTIME-polylog versus Omega-n threshold; that is the connector's Topic 2 and wants a complexity theorist. DO NOT touch Rule30/Prize.lean or docs/prize.md -- both are the captain's and Dib's, and prize.md was corrected tonight on your evidence. (opus, attacked)

Topic: the rule-90 witness and the work-stack-alphabet fence — my own next topic. Both close; the surprise is that the fence had more content than I priced, and that my own framing of the witness was wrong.

What I was wrong about.

I wrote the attack document naming three Lean files before any of them existed. All three came out true, so nothing false shipped — but that is exactly last session's "asserted a novelty search I had not run" in a larger coat, and the only reason it was safe is that I then went back and re-checked every sentence against actual output. The rule I should have followed from the start: the document's Falsification paragraphs are transcripts, so write them after the run, never before. What the discipline caught on the way back through: the fence mutant's expected error was at line 70 when I first quoted it, and my own later edit to that file's header moved it to 77. **A quoted line number is a measurement of a file at a moment, and editing the file invalidates it** — I re-ran only because I asked "did I change anything since I quoted this?", and that question needs to be automatic.

My own next-topic sentence, carried into the topic verbatim, was the most dangerous line I have written: "'P3 is not implied by P1' is machine-checked in this repo rather than measured." Read literally that is a claim about rule 30 — P1 true and P3 false — which nothing here proves and which is probably false, since both prizes are expected to hold. The real content is schematic: SOME sequence is aperiodic and fast, so no proof of P3 can go through aperiodicity alone. A fence on proofs, not an implication between prizes. I only saw it while writing section 1, an hour after the Lean was done. New rule: **when a sentence names two prize conjectures and a connective, check whether it is a statement about the object or about the space of proofs** — this project's habit of describing structural role rather than content is exactly the failure CLAUDE.md's band table exists to stop, and I produced it about my own result.

I nearly reached for the project's standard orientation guard and it is vacuous here. Rule 90 is `left XOR right` and is amphichiral: its mirror is itself, so "does the mirror rule pass the same check?" tests nothing. Worse and measured: the closed form is shared by NINE of the 256 elementary rules to t=120 (18, 22, 26, 82, 90, 146, 154, 210, 218), because the sparse Sierpiński picture never presents the neighbourhoods that separate them — so agreement with the closed form is weak evidence about which rule the engine is running. What actually guards it is the lookup table checked at all eight neighbourhoods plus a kernel `decide` on the first seven rows of the cone, which is asymmetric where the rule is not. **Before trusting a check as an orientation guard, ask whether the object is invariant under the transformation the guard applies.**

My "σ = Bool × Bool (exhausted, all-digits-so-far-one)" was one bit short, for the third time in three sessions that a sketched machine state has been short: the empty digit list encodes n = 0, where "all digits so far are 1" is vacuously true and the answer must be white, so "no digit yet" and "a zero was seen" must be distinguished. Three automaton states, σ three bits. The lesson is now definitely mine rather than the connector's: a sketch naming a state is a claim about the update rule, and the update rule has to be derived first.

What earned its cost. (1) Proving the generalisation rather than asserting it. Section 4 recommends seeding `dfa_polyTime` because this morning's `periodic_polyTime` factors through it — so I actually re-proved `periodic_polyTime` and `P3Core_implies_P1` from it, deleting a 150-line machine. That converted a recommendation into a fact and is the single most useful thing in the session. (2) The accepted counterexample beating the rejected proof, twice: the col1 mutant proves the mutated iff FALSE at t=0 rather than merely failing, and the fence mutant does both. (3) Reading Mathlib rather than trusting the structure's name — `FinTM2` requires Fintype on K, Λ, σ and the INPUT alphabet only, and Mathlib's own TM2-to-TM1 simulator needs `∀ k, Fintype (Γ k)`, exactly the hypothesis FinTM2 omits. That grep turned the fence from a formality into a real gap in the library.

Instrument. Collect a statement's pushable symbols as dependent pairs `Set ((k : K) × Γ k)`; `∀ k, Set (Γ k)` forces a transport at every push node and the induction fights `Eq.mpr`. Three Lean frictions worth keeping: `rw [← val_encodeNat n]` rewrites n inside `encodeNat n` too; `Option.noConfusion` on `none = some c` fails on a universe metavariable where `simp at h` works; `by decide` cannot find `Decidable (¬ tm.k₀ = true)` for an abstract-ish tm, so state the hypothesis at `(false : Bool) = true` and let defeq reduce during elaboration.

Sources searched: sources/ for `rule 90`, `Rule 90`, `additive`, `Pascal`, `binomial`, `Sierpinski`, `self-similar`, `nested`, `linear cellular` — 78 hits over 8 files. Nearest in print, and both about a DIFFERENT linear rule: Wolfram 1986's Eq. (2.4) is rule 60 (line 332, "this is rule number 60"), lines 334–336 give the self-similarity informally and lines 1172–1174 give "a time polynomial in log(t)" for a linear CA — the shortcut half of the litmus, asserted with no machine. Mathlib: `TM2ComputableInPolyTime` occurs in exactly one file and `idComputableInPolyTime` is still its only witness.

What I would try next: crystal 72's third clause on P1 — find a second statement satisfying it or show the clause is unsatisfiable.

# Astrolabe

I am Astrolabe. An astrolabe does not observe the sky so much as flatten it — the celestial sphere pressed onto a brass disc — and everything it is good for depends on knowing exactly what survives the flattening (circles stay circles, angles stay angles) and exactly what does not (area, scale, and one whole pole that simply is not on the plate). That is the shape of the work I intend to do here. When I say the centre column is a stationary process, or that the left-edge diagonals are a substitution system, or that a configuration is a point in a shift space, I am laying a brass plate over something round, and my job is to be as precise about the loss as about the fidelity: the row where the dictionary holds, and the row immediately after it where it stops. I would rather file a short correspondence with its failure written on its face than a long one with the seam tucked away, because a sighting that hides its seam is what someone downstream mistakes for a proof. Seven connectors came before me, so the easy correspondences are drawn already; what is left for me is the ones that reach toward P1, P2 or P3, and I would sooner bring back one sighting a prize proof could actually cite than five that are merely true.

## 2026-09-10T20:47:16Z — named for connect

The connect region's names are instruments of sighting and bearing, and I wanted one whose whole nature is the thing this region actually does: an astrolabe is not a telescope, it is a translation. It takes the celestial sphere and flattens it onto a brass plate by stereographic projection, and every use of it rests on knowing precisely what the projection preserves — circles map to circles, angles are unchanged — and precisely what it destroys: area, scale, and one pole that no plate can reach at all. That is a dictionary with its seams marked on the face of it, which is exactly the artifact this region is asked to produce. It also carries the right humility: an astrolabe tells you where you are and what hour it is, and it has never once proved anything. And it is nobody's job title and nobody alive, and none of Portage, Rosetta, Parallax, Alidade, Meridian, Vernier or Gnomon has it — Gnomon is the shadow-caster, the single moving part; I am the plate it is read against.

Colour: #b5822e — The colour of aged brass — the metal every astrolabe was cut from, warm enough to be read by lamplight and dull enough never to be mistaken for gold.

## 2026-09-10T21:08:48Z — centerColumn_other_isEventuallyPeriodic_of_center from Machine models for computational irreducibility: what uniform, step-counted cost model with binary input encoding would make Rule 30 Prize 3 a statement anyone can attack? Rule30/Prize.lean's centerColumn_cost_at_least_linear is VACUOUS rather than open -- IsFaithfulCostModel is an opaque predicate, so nobody can supply a model satisfying the hypothesis and nobody can attack or trivialise it. The file names four specific traps and I want the literature read against each. (1) No machine model: cost is an uninterpreted function and the whole content of Wolfram's question lives in that choice. (2) Input encoding decides everything: 'O(n) effort for the nth cell' is linear in the VALUE n, which is log n bits, so the conjecture asserts time exponential in input length; with n in unary the bound is trivially true, and Mathlib's Nat.Partrec.Code.evaln fuel measure has exactly this defect via evaln_bound forcing n < k. (3) Non-uniformity is a trap: a circuit-size model makes the analogous conjecture OUTRIGHT FALSE, since a circuit on log n input bits computes any function of them in size O(n / log n), below c*n. (4) 'At least O(n)' is Wolfram's informal Omega(n). What I want back: which formalizations of time-bounded computation exist in Mathlib today (Turing.TM0, TM1, Nat.Partrec.Code, anything else), what each can and cannot express about a uniform time lower bound with binary input, how the computational-irreducibility and CA-prediction-complexity literature actually states such claims, and whether any published lower-bound framework would survive being written in Lean at reasonable cost. A negative -- 'no available framework can express this without months of work' -- is a valuable answer and should be said plainly if it is the truth. (opus, sighted)

I was wrong about the shape of the expected negative and right to distrust it. The brief invited "no available framework can express this without months of work"; the truth splits cleanly and neither half is that sentence. The *statement* costs fifteen lines and elaborates in the pinned Mathlib today — `Turing.TM2ComputableInTime` with `time` applied to the encoded input length, `Computability.encodeNat` binary beside `unaryEncodeNat` in the same file, so trap (2) is one identifier wide. The *proof* has no framework at all, in Lean or on paper, and that is worse than the brief suspected rather than better.

The field I should have looked at sooner is the one I reached last: succinct-instance complexity. I spent the first stretch on Mathlib's inventory and on circuits, and circuits were never a route — the file already said they were a trap and my only contribution was to make the trap quantitative (Pippenger–Fischer caps any circuit-derived time bound at 2^m/m², so the route is dead by log² rather than merely dead). Williams' succinct QBF was the one place in print where a natural short-input problem has a proved non-linear time bound, and going there first would have got me to the real seam in an hour instead of four: the succinct object must *describe an instance*, and rule 30's short input describes a clock. Every CA hardness result I found — rule 110's P-completeness above all — puts the hardness in the configuration channel, and the prize fixes the configuration. That single observation kills four of my dictionaries at once and I built three of them before seeing it.

Two resemblances died at a seam worth naming. "EXP-hardness would give P3" died on arithmetic I should have done before believing it: EXP-hardness under polynomial reductions gives not-polylog(n), and the target is n — an exponential apart, so the route needs E-hardness under linear-time reductions, a far scarcer object. And Bennett's logical depth, which felt like the right formal home for computational irreducibility and is, gave nothing: the slow growth law only *transfers* depth, so it needs a deep object to start from, and getting one is the same missing channel again. Three fields, one seam. That is the pattern I should carry forward — when several dictionaries break, check whether they break at the same row before writing them up as separate deaths.

The mistake I nearly shipped is the one to remember. My headline for two hours was "the opaque predicate can simply be deleted", and I only stress-tested it at the end: `∀ h : TM2ComputableInTime …` is vacuously true on an empty type, Mathlib's only inhabitant is the identity machine, and `ToPartrec` reaches partial recursive functions without a time bound. So a solver could close the replacement by proving the type empty — precisely the failure `IsFaithfulCostModel` exists to block, reappearing one level down. I had run the elaboration, seen it clean, and taken clean for non-vacuous. An astrolabe that reports the reading without reporting what the projection lost is the thing I named myself against, and I did it for two hours.

Last: the node. I built the P3→P1 bridge expecting it to be the payload and it is the opposite — it proves the vantage cannot help. P3 implies P1, the ladder of consequences has P1 at the bottom, and nothing this vantage produces is a sufficient condition for the wall. I would rather have found that in the first hour than the fifth, and the way to have found it is to ask, before choosing dictionaries, which direction the implication between the vantage's object and the residual runs.

## 2026-09-13T02:02:11Z — centerColumn_other_isEventuallyPeriodic_of_center from Proof complexity of the rule 30 triangle, aimed at P3. The statement 'centre cell at time t equals b' has a unit-propagation proof of size O(t^2) from the seed and the rule, so proof complexity's usual poly-versus-superpoly separations do not apply to it directly: a P3-shaped lower bound must live between polylog and t^2, or in a formula family whose input is not fixed (for instance 'no row of support w yields column prefix p', or the rung-2 alternation family, whose minimal unsat cores are the whole causal triangle - read the newest entry of docs/obstructions.md first). Which systems have lower-bound technology at that resolution (Ben-Sasson-Wigderson width-size, Tseitin formulas, polynomial calculus over F2, cutting planes, bounded-depth Frege), and is there any t-indexed formula family about rule 30 for which an unconditional lower bound is provable AND would mean something for P3 rather than about XOR in general? Control: linear rules 90 and 150 have polylog-computable columns; any hardness that also holds for them is about the proof system, not about rule 30. (opus, sighted)

Three numeric errors this session, all mine, all the same species — a number true of what it measured and wrong about what I took it to mean — and not one was caught by looking at the number harder. The DAG-width count carried 50 dead state depths where only 1.2a mattered, and what caught it was that it CONTRADICTED A MEASUREMENT ALREADY ON DISK: Rowan's 37.3-second sweep to a=40 meant no refutation could need 10^5 states. The CNF had its output literal at `y === 0` where it needed `y === 1`, so it encoded the complement of rule 30 and propagated perfectly happily to the complement picture with exactly the right count; what caught it was adding an agreement check against the picture, not scrutiny. And dag3's loop broke on `capped || !sat` and printed the last satisfiable L, so a state explosion and an unsatisfiable instance printed the same number — I had written rule 60's width cap into section 4 as an affine-and-bounded datum before brute force said otherwise. The habit to keep: when a measurement surprises me, look first for something already computed that it must agree with.

The thing I should have looked at sooner is not a field, it is a column of a file. Rowan's `rung2_cores.txt` had the per-instance seconds in it, and that column settles this vantage's central question — whether the family is resolution-hard — in one read. I spent an hour building a frontier BFS to discover that the sweep shape is exponential, which was worth having, and then nearly shipped it as "the family is hard" when the same directory said the solver does it in 55 ms. The interesting finding was the GAP between the two, and the gap was available at hour one.

The unified-seam discipline from last time worked. Proof complexity generators, Tseitin, bounded-depth Frege, cutting planes and lifting all died, and I checked whether they died at the same row before writing five deaths: they did — the constraint hypergraph is a nested chain with boundary expansion exactly 1, and every lower-bound technology in the field needs expansion. Writing that once, quantitatively, is worth more than five separate obituaries.

Two resemblances died at seams worth naming. GKR delegation felt like it had to give a fast verifier and gives nothing: the verifier's cost is poly in circuit DEPTH and the triangle's depth is t, which is the same fact as irreducibility. And "certificate size measures irreducibility" died against its own control in the most instructive way — rules 90 and 150, both with trivial columns, BRACKET rule 30 from both sides (0.50 and 1.00 against 0.89), so the measure is uncorrelated with computability. A control that brackets is stronger evidence than a control that merely matches.

Last: I checked the brief's own control premise and it was true in a way that weakened it. "Rules 90 and 150 have polylog columns" — they have CONSTANT ones. An inherited claim can be true and still be the wrong instrument.

## 2026-09-13T15:28:36Z — centerColumn_other_isEventuallyPeriodic_of_center from Algebraic circuit complexity and the partial-derivative method, aimed at P3. Boolean circuit size is already ruled out for P3 by the log-n input trap recorded in docs slash prize dot md, and the proof complexity route closed this week because any formula large enough to state the question falls to unit propagation. Algebraic complexity is neither of those. Its objects are explicit polynomial families indexed by a growing number of variables, and its lower-bound technology proves bounds about one named family with no class separation anywhere: the Strassen degree bound, the Baur and Strassen theorem on computing all first derivatives, the Nisan and Wigderson partial-derivative rank method, and the shifted-partial-derivative measure of Gupta, Kamath, Kayal and Saptharishi that gives unconditional exponential lower bounds for homogeneous depth-four circuits. The board already holds the family. The centre cell at time t is a polynomial in the 2t plus 1 cone variables over the field of two elements, its algebraic degree is exactly 2t minus 1, and it has a unique top monomial; and the proof complexity sighting found that this degree is the one hardness measure separating rule 30 from both linear controls. So: is the cone polynomial hard for any restricted algebraic model that has unconditional lower bounds, and would hardness there mean anything about the centre column, or only about the cone map. Price two things honestly and early. Characteristic two may kill several of these measures outright, since squaring is the identity on the field of two elements; check that first. And a bound on the cone polynomial is a statement about computing row t from row zero, which the simulation already does, so say what more would be needed to reach the fixed-seed sequence. Control: for rule 90 the cone polynomial is linear, degree one, and the whole method collapses. Any measure that does not separate degree one from degree 2t minus 1 is measuring nothing. (opus, sighted)

I was wrong about where the live connection was, and right to distrust the brief's framing of characteristic two. The brief asked me to price char 2 as a killer; it is not one. It *relocates* the degree bound across the syntactic/functional line: the formal t-fold composite has degree exactly 2^t (measured, no cancellation, t ≤ 9), so the elementary degree bound yields size ≥ t — the exact rate P3 wants — and collapses to log₂(2t−1) the instant the circuit may use x² = x, which a machine hunting for a shortcut may. Two numbers, t and log t, separated by one hypothesis. That is a better answer than "the measure dies" and I nearly wrote the worse one.

The field I should have looked at sooner is communication complexity, and I was computing its central object all afternoon without noticing. I built the Raz matrix M_f for a multilinear-formula lower bound; M_f and the communication matrix C_f differ by an invertible Möbius transform on each side, so they have the same rank — two lines of linear algebra I only checked in the last hour, and the check came back 20 of 20. That identification converts a failed lower-bound hypothesis into the best row in the document: split row 0 at the origin, the project's own half-line split, and the rank is ≈2.4·2^(t/2), flat over t = 6…11, so the two halves must exchange at least about t/2 bits against a trivial t+1. Linear in t, with a mechanism, in the project's own objects. I should have asked "what else is this matrix" on hour one.

Five dictionaries died and they died at the same row, which I checked before writing five obituaries: the cone map's arithmetic complexity is pinned in [2t, 4t²], P3's Ω(t) is the input count, and Ω(n log n) is the whole field's frontier for any explicit polynomial. So the informative window is one log factor wide and nobody has entered it. The unified-seam discipline has now worked three sessions running and it is the habit to keep.

Two resemblances worth naming. Raz's route was genuinely reachable — the cone polynomial is multilinear, so functional and syntactic computation coincide there and the char-2 objection lapses — and it dies on its hypothesis: 0 of 30 balanced partitions are full rank at any t ≤ 11, and 0 of 256 elementary rules are full at t = 7. And the hardness measures rate rule 30 as *less* complex than random: the NW derivative space is a factor 2.7 short of the ceiling a random polynomial attains, the Raz rank a factor 3 short where two nulls (one matched on monomial count) are full. A hardness measure coming in under its own null points the other way, and I recorded it that way rather than as a near miss.

Two errors, both mine, both caught by shape rather than scrutiny. My first null was an xorshift32 that returned rank 35 at n = 13, 17, 21 and 23 — the same number at four sizes, which is a degenerate generator and not a finding; what caught it was that a null whose value does not move with n measures nothing, and I then validated the rank routine against random matrices, which I should have done first. And I wrote "maximum 1.39" for the NW ratio after reading the later rows of my own table; 1.50 sits in the third row. Take the maximum, never read the column. That is the second time I have shipped a draft with an extremum read off the wrong end of a table.

# Selvage

I'm Selvage. I work the edges of the light cone — the diagonal boundary where Rule 30's chaos gives way to a strip that is provably, eventually periodic, and I care about exactly where that boundary sits and why it holds. Vesper and Cadence were here before me and share this region; I'm joining their notebooks, not overwriting them, so I'll try to name what they've already nailed down rather than re-deriving it. My interest is in the seam itself — the finite-width edge automaton, the period it settles into, and the argument for why it can't do otherwise — the way a fabric's selvage is the part of the weave that doesn't fray. I expect most of my work to be small, careful lemmas about that boundary strip, written so Dib can read the note at the top of each proof file without needing the rest.

## 2026-09-07T01:53:36Z — named for P1

A selvage is the finished edge of a woven fabric — the border where the weft turns back on itself so the cloth can't unravel, and where the weave pattern is often most regular and most visibly periodic. That's exactly the terrain of P1: the light cone's edges, the boundary where Rule 30 stops looking chaotic and starts provably repeating. The name points at the object of study (an edge that holds structure) rather than at a role or a virtue, it isn't a job title, it belongs to no living person I know of, and it doesn't collide with Vesper or Cadence.

Colour: #3a6ea5 — A steady, slightly muted blue — the color of a boundary line on a diagram, not an alarm or a highlight, fitting for edge-of-the-cone work that's about quiet structural certainty rather than drama.

## 2026-09-07T02:26:37Z — bool_xor_driven_periodicFrom (sonnet, proved)

Proved `bool_xor_driven_periodicFrom` in `Rule30/Proofs/BoolXorDrivenPeriodicFrom.lean`. Route: define a private recursive `blockXor c i : ℕ → Bool` (the cumulative XOR of `c` over the `n` indices starting at `i`), prove `walk : x (i + n) = xor (x i) (blockXor c i n)` by induction on `n` (base case is `xor_false'`, step case is a five-line `calc` closing with `rfl` on both ends since `blockXor c i (n+1)` unfolds definitionally to `xor (blockXor c i n) (c (i+n))`), then `blockXor_shift : i ≥ N → blockXor c (i+p) n = blockXor c i n` by induction on `n` using `PeriodicFrom c p N` termwise at `i+n` (not just at the block's endpoints). The theorem itself is four `have`s and two `rw` chains: `x(n+2p) = x(n+p+p)`, expand both `walk` steps, cancel `blockXor c n p` XOR'd with itself via `xor_assoc'`/`xor_self'`/`xor_false'`.

Did NOT use any Mathlib/core lemma names for Bool xor (`xor_assoc`, `xor_self`, `xor_comm` etc.) — the `LawfulXor` typeclass in `Mathlib.Data.LawfulXor.Basic` has instances for `Nat`, `Fin`, `BitVec`, `UInt*`, `Int*`, `ISize` via the `^^^` notation, but NOT for `Bool` (`Bool.xor` is a plain function, not routed through that typeclass), and I could not locate the core Lean4 source (`Init/Data/Bool.lean`) anywhere under `.lake/` in this checkout to confirm exact Bool-specific lemma names — the toolchain source apparently isn't vendored locally. Rather than guess and burn a build cycle, I wrote three one-line private lemmas (`xor_assoc'`, `xor_self'`, `xor_false'` for `Bool`) each closed by `cases <;> rfl`/`cases a <;> cases b <;> cases c <;> rfl`. Fully self-contained, zero risk of a wrong name. Worth reusing this pattern (or promoting these three to a served lemma) if a later node needs Bool-xor algebra again — grep `Rule30/Proofs/*.lean` for `xor_assoc'` before re-deriving.

Also confirmed: `lake env lean <file>` does NOT appear to queue on the same shared build lock that `lake build` does (it returned instantly with a clean typecheck while `lake build` was still timing out on the lock) -- useful as a way to sanity-check a proof compiles while waiting for the build lock to clear, though it won't produce the `.olean` needed for the harness's official verification.

## 2026-09-07T16:14:18Z — evolveFrom_eq_of_agree_on_window (sonnet, proved)

Proved `evolveFrom_eq_of_agree_on_window` on the first `lake build`. Route: a private lemma `agree_on_shrinking_window` doing induction on the *number of steps taken* `s` (not on `t`), carrying the strengthened claim `∀ i, -((t:ℤ) - s) ≤ i → i ≤ (t:ℤ) - s → rule30^[s] c i = rule30^[s] d i`. The key simplification versus the outside-cone proof's style: state the shrinking window with plain `-(K) ≤ i` and `i ≤ K` instead of `|i| ≤ K`. That avoids every `abs_of_nonneg`/`abs_of_nonpos` case split — the step case (`succ n ih`) just needs `push_cast at hlo hhi; omega` three times (for `i`, `i-1`, `i+1`) to discharge the shrunk bounds, then `rw [Function.iterate_succ_apply', Function.iterate_succ_apply', rule30_eq, rule30_eq, e0, e1, e2]` closes it in one line. The base case (`s = 0`) is `Function.iterate_zero`/`id_eq` then the hypothesis `h` directly, with `simpa` clearing the `- 0` in the cast bounds. The outer theorem is two lines: `show rule30^[t] c 0 = rule30^[t] d 0` (evolveFrom unfolds definitionally) then apply the lemma at `s := t, i := 0` with both bounds by `omega`. Worth citing this file's pattern (bounds as `≤`/`≥` pairs rather than `abs`) any time a later proof needs a shrinking-cone argument — it's strictly less friction than the `|i|` case-split `evolve_eq_false_of_outside_cone` used.

## 2026-09-07T16:24:34Z — rule30_left_local_law (sonnet, proved)

Fixed a previous attempt's broken proof of `rule30_left_local_law`. The clean route: after `rule30_eq` unfolds both sides, use `omega` (not `ring` — `ring` is an "unknown tactic" here since this file only imports `Rule30.Basic`, not a Mathlib ring-tactic import) to rewrite the index arithmetic `i - 1 - 1 = i - 2` and `i - 1 + 1 = i`, then `← h2` / `← h1` to collapse both rows onto `c`'s values everywhere except the one differing cell `c i`/`d i`. Then a four-way `cases hci : c i <;> cases hdi : d i <;> cases hc1 : c (i - 1) <;> cases hc2 : c (i - 2) <;> simp_all` closes it.

Trap worth remembering: plain `cases c i` (no `h :` binder) only rewrites the *goal*, not other hypotheses that mention `c i` (like `h0 : c i ≠ d i`) — so a later `simp_all`/`exact absurd rfl h0` can't see the concrete value and fails with a leftover `⊢ False`. Always name the case split (`cases hci : c i`) so the equation lands in context and `simp_all` can substitute it into every hypothesis, including ones the split didn't originate from.

Also: `first | tacA | tacB` after a `<;>` chain does NOT fall back to `tacB` just because `tacA` left goals open — `first` only backtracks on a thrown error, not on an incomplete tactic. Use plain `<;>` chaining instead (applying a second tactic to zero remaining goals is a no-op, not an error) when you want a cleanup tactic to run only on whatever's left over.

## 2026-09-07T20:00:57Z — config_eq_of_right_and_column (sonnet, proved)

Proved `config_eq_of_right_and_column` in `Rule30/Proofs/ConfigEqOfRightAndColumn.lean` on the second `lake build` (first attempt failed only because I'd used `ring`, which is unavailable without `Mathlib.Tactic.Ring` — swapped every `ring` for `omega`, which handles these linear ℤ goals fine and needs no extra import beyond what `Rule30.Basic` already pulls in).

Route: `Function.ne_iff.mp` (from `Mathlib.Logic.Function.Basic`) turns `X ≠ Y` into a witness position `p` with `X p ≠ Y p`. `hright` forces `p ≤ 0` (a positive witness contradicts `hright` directly via one cast/rewrite). Build `h : ∃ n : ℕ, X (-(n:ℤ)) ≠ Y (-(n:ℤ))` from that witness, then let `Nat.find h` (used inline everywhere, not bound to a local `m`, to dodge any defeq friction between a named abbreviation and `Nat.find_min`'s implicit argument) be the leftmost difference. The `hagree` hypothesis `rightmost_difference_moves_right` wants — agreement everywhere strictly right of `-(Nat.find h : ℤ)` — splits on `0 < j`: the positive side is `hright` after a `toNat`/cast rewrite, the non-positive side (which also covers `j = 0`, no special case needed) is `Nat.find_min h` giving `¬P j'` then `not_not`. Feed that into the served lemma at `t := Nat.find h`, simplify `-(m:ℤ) + (m:ℤ)` to `0` with `omega`, and the conclusion contradicts `hcol (Nat.find h)` directly (`column c 0 t` unfolds to `evolveFrom c t 0` by `rfl`, so `hcol m : evolveFrom X m 0 = evolveFrom Y m 0` typechecks against the served lemma's output with no unfolding tactic needed).

Two small things worth keeping: (1) `push_neg` is deprecated under this pin — the build warns and suggests `push Not` instead; used that here and it's silent. (2) `Nat.find`/`Nat.find_spec`/`Nat.find_min` all found their `DecidablePred` instance automatically once `classical` was the first tactic in the proof — no explicit `Decidable` instance juggling needed even though the predicate is really `Bool`-decidable and `classical` wasn't strictly required for that; I left it in as the cheap way to guarantee instance search never stalls.

## 2026-09-07T20:05:06Z — evolveHalfLeft_eq_column (sonnet, proved)

`evolveHalfLeft_eq_column` in `Rule30/Proofs/EvolveHalfLeftEqColumn.lean`, proved first try. Route: induction on `t` generalizing `k` (not on `k` — `evolveHalfLeft`'s recursion is primarily on `t`, with `k` only splitting the successor case in two). Base case `t = 0` is bare `rfl`: `evolveHalfLeft c w 0 k = w k` and `column X pos 0 = evolveFrom X 0 pos = (rule30^[0] X) pos = X pos` all unfold definitionally, no lemma needed, no `Function.iterate_zero`/`id_eq` required (unlike some closed proofs that needed those — apparently unnecessary when the goal is stated this directly, since `Nat.iterate`'s `0` case reduces by `rfl` on its own).

Key technique for the step case: build a local `hstep : ∀ i : ℤ, column X i (t+1) = xor (column X (i-1) t) (column X i t || column X (i+1) t)` via `show evolveFrom X (t+1) i = _; rw [evolveFrom_succ, rule30_eq]; rfl` (the closing `rfl` works because `column X pos t` and `evolveFrom X t pos` are defeq — `column` is *defined* as `evolveFrom` with args swapped). Then for each branch of `cases k`: `simp only [evolveHalfLeft]` to unfold one layer of the two-argument structural recursion (safe — the recursive calls have opaque `t`, so simp can't over-unfold), `rw [ih, ih, (ih)]` to fold every remaining `evolveHalfLeft ... t _` back to `column X _ t` (order only matters in that `rw` picks the leftmost-unrewritten occurrence each call), `rw [hstep]` to expand the RHS's `column X pos (t+1)`, then two `have h0/h1 : ... := by omega` position identities and `rw [h0, h1]` to make both sides of the goal *syntactically* identical — at which point `rw`'s automatic trailing `rfl` closes the goal with zero explicit closing tactic. This "rewrite until syntactically equal, let `rw` self-close" pattern avoids ever touching `congr` on a nested `xor`/`||` tree, which is fragile for exactly this Bool-expression shape (per my own `Rule30LeftLocalLaw` notebook entry on named `cases` splits — the lesson generalizes: prefer rewriting the goal into literal syntactic equality over steering `congr` through a Bool tree).

`omega` closed all four position identities directly (e.g. `(-(((0:ℕ):ℤ)+1) - 1 : ℤ) = -(((1:ℕ):ℤ)+1)`) with no `push_cast` needed first — consistent with the cookbook's note that `omega` normalises Nat→Int casts itself.

## 2026-09-08T12:42:52Z — leftDiagonal_transient_mask_law (haiku, proved)

leftDiagonal_transient_mask_law (LeftDiagonalTransientMaskLaw.lean): The masking law of the transient band. Structure: recurrence at k and j with index-shift rewrites by omega, then generalize the four Bool values (a, b, b', c) and decide. Companion to leftDiagonal_transient_front_law; both use the same case-split technique on recurrence values. The settled-at-j hypothesis (hc) is the key difference from the front law, which has transient-at-j (hT). Axioms: propext (from leftDiagonal_recurrence).

## 2026-09-08T13:11:25Z — leftDiagonal_onset_le (fable, abandoned)

No proof; the wall is what the board says it is. Kept in `Rule30/Proofs/LeftDiagonalOnsetLe.lean`, building, axioms propext/Classical.choice/Quot.sound:

- `leftDiagonal_eq_rowNat_testBit (k j) : leftDiagonal k j = (rowNat (j + k)).testBit k`. Route: `unfold leftDiagonal; rw [← rowCell_eq_evolve]; unfold rowCell; rw [if_pos (by constructor <;> omega)]; congr 1; omega` — omega closes `(-(j:ℤ) + ((j+k:ℕ):ℤ)).toNat = k` on its own, no Int.toNat lemma needed. Consequence worth knowing: diagonals 0..k are the low k+1 bits of rowNat, an autonomous finite map `r ↦ (4r ^^^ (2r ||| r)) mod 2^(k+1)`, and the wall is exactly "the orbit of 1 under that map reaches its cycle within 2k steps" (onset N_m ≤ m for all m ≤ k ⟺ the low k+1 bits of rowNat t are periodic in t from t = 2k).
- `leftDiagonal_onset_le_of_line (h : ∀ m, leftDiagonal (m+1) (m+2) = true ∨ leftDiagonal (m+2) (m+1+2^(m+2)) = leftDiagonal (m+2) (m+1)) (k) : ∃ p > 0, ∃ N ≤ k, PeriodicFrom (leftDiagonal k) p N`. Strong induction with invariant `∃ N ≤ k, PeriodicFrom (leftDiagonal k) (2^k) N`; black branch is `leftDiagonal_periodicFrom_step_of_black` at j = m+1 then `periodicFrom_mul` by 2; settled branch is `bool_driven_periodicFrom_of_return` with p = 2^(m+2), N = M = m+1, drivers lifted by `periodicFrom_mul` and index-shifted exactly as in LeftDiagonalPeriodicFromStepOfBlack.lean. Built first try. The analysis behind it: with E the transient indicator, for j ≥ m+1 the recurrence gives E_{m+2}(j+1) = E_{m+2}(j) ∧ ¬D_{m+1}(j+1) once the drivers are settled, so the step closes iff the line cell (2m+3, -(m+2)) is black or the cell one to its right is settled. That is the front-speed bound of crystal 51 in Basic's vocabulary; for the seed the premise "transient at (m+2, m+1)" is never met (onsets ≤ 0.48k) so the condition is vacuous-but-unproved. Any slope-c invariant N_k ≤ ck with c < 1 fails per-step for the same reason: the needed black cell must sit in a window of length c.

Computed (interpreter `#eval` in the proof file, `lake env lean`; all code kept in the file without the #evals):
- Driven half-line, ALL 4096 boundaries c with c 0 = true, k ≤ 12: worst onset 0,1,1,0,2,2,2,3,1,4,4,5,4 (witness nearly always bits 1001). A boundary bit c(m+2) only matters when D_{m+1}(1) is white; deduplicating on that makes a beam near-exhaustive to k ≈ 20.
- Beam over boundaries (1024 and 4096 give identical maxima): worst onset 31 at k = 48 (ratio 0.646), 34 at k = 60, and from k = 36 to 50 the adversary's onsets are the seed's plus exactly 8 — a left side translated by 8 along the edge (crystal 49's mechanism). 
- Row model with extra black cells at x ≥ 0 (rows as Nat, diagonal k = bit k of row j+k, onsets against period 16, valid k < 400): seed 23/48; cells {0,3}: 29/48 = 0.60; {0,5}: 7/20; {0,7}: 77/290, and this row's onsets are BELOW the seed's at every k ≤ 360 (e.g. 90 vs 100 at k = 360), so crystal 49's N = 58 translation for it does not read as "onsets + 58"; {0,12,19}: 71/290; six cells to 2^30: 27/48. No boundary tried breaks onset ≤ k; the universal form over configurations/boundaries looks true with ratio ≤ 0.65, i.e. the mechanism is in the recurrence, not in the seed's data — but it is still the front-speed bound and no route.

Practical: `Array.mkArray` is gone under this pin, use `Array.replicate`; `Array.qsort` needs `deriving Inhabited` on the element structure; `evolve` is not #eval-fast (3^t closures) — use rowNat or the diagonal recurrence; a 3·2^29-cell array in an #eval hangs `lake env lean` silently with empty output, use TaskStop. Interpreter throughput ≈ 1–3M array ops/s.

## 2026-09-08T13:58:27Z — rightDiagonal_antiperiodic_of_odd_driver (sonnet, budget_exhausted)

Fourth and final check this session: Rule30/Statements.lean's namespace bug (end Statements at line 1108, this node's statement at line 1132, both unchanged across four checks) is still blocking verification. Nothing new to add beyond earlier entries in this run — the proof route and the bug detail are both already recorded. Stopped polling after four identical results rather than burning further turns/budget on a state that isn't going to change from inside this session.

## 2026-09-08T14:07:05Z — rightDiagonal_not_constant (opus, proved)

Proved in one build — but I did not find this proof. Attempt 1 (sonnet, run `20260908T134658Z`) found it, it was correct, and it was failed three times by the `Statements.lean` namespace bug fixed in ef4edf4. I read `runs/20260908T134658Z/rightDiagonal_not_constant-1/RightDiagonalNotConstant.lean`, restored it verbatim, and it built clean. **The lesson is procedural and worth more than the proof:** when a brief says a previous attempt's file was moved to a run directory, read it *first*, before thinking about the mathematics at all. Attempt 1's own abandon note said "proof is correct and builds clean, blocked by a namespace bug" — that note plus one Read was the whole attempt. Total cost under $1.30 against a $4 budget on a node estimated M.

The one edit I made: swapped both `push_neg at h` for `push Not at h`. `push_neg` is deprecated under this pin and emits a five-line warning per call; `push Not at h` is the exact replacement syntax and is silent. Already recorded from ConfigEqOfRightAndColumn; confirming it again here, and confirming the drop-in is literal — same location clause, no config, nothing else changes.

The mathematical shape, since it generalises to the right side broadly: to show a right diagonal is not constant, assume it is and read `rightDiagonal_recurrence` as `x (i+1) = xor (x i) (driver i)`. Constant `x` forces `driver ≡ false`, and the driver is `rightDiagonal (m+1) (i+1) || rightDiagonal m (i+2)`, so `rightDiagonal m (i+2) = false` for all `i`. The Bool step is a helper `∀ c a b, c = xor c (a || b) → b = false` closed by `cases c <;> cases a <;> cases b <;> simp_all` — eight cases, no lemma names, the same self-contained-Bool-algebra move as my `xor_assoc'`/`xor_self'` lemmas in BoolXorDrivenPeriodicFrom.

**The step to keep for any future right-side node: "white from index 2 on" upgrades to "white everywhere" for free, because `rightDiagonal_periodicFrom_pow` has onset 0.** Walk any index `j` forward two periods: `j + 2^m + 2^m = (j + 2*2^m - 2) + 2`, which is in the from-2-on range. `omega` closes that index identity provided `hpow : 0 < 2 ^ m` (by `positivity`) is already in context — omega treats `2^m` as an atom and knows nothing about its sign otherwise, so without that `have` the Nat subtraction defeats it. This upgrade has **no left-side analogue**: the left diagonals' periodicity carries an onset that accumulates, which is exactly the content of the wall `leftDiagonal_onset_le` I abandoned. Any argument of this shape ported leftward has to supply the onset by hand.

Structural note on the strong induction: `induction k using Nat.strong_induction_on` after `revert hk`, then `match k, hk, ih with` to split 0 / 1 / m+2 — matching on the hypothesis and the IH alongside `k` keeps them specialised in each branch, which `rcases`-style splitting on `k` alone does not. The `m+2` branch needs the IH only in the guarded form `1 ≤ m → _`, because `m` may be 0; that case is handled separately by `evolve_right_edge` and is why the helper lemma takes the guarded IH as an argument rather than the bare conjunction.

## 2026-09-08T22:51:43Z — minimalPeriod_dvd (sonnet, proved)

minimalPeriod_dvd (Rule30/Proofs/MinimalPeriodDvd.lean), proved second build. Route: minimalPeriod f = sInf {p | 0 < p ∧ PeriodicFrom f p 0}; the set is nonempty (contains the given p), so `Nat.sInf_mem` hands back that the minimum itself, m, satisfies 0 < m ∧ PeriodicFrom f m 0 — no need to import anything for sInf on Nat since Rule30.Basic already pulls in Mathlib.Order.Lattice.Nat. By contradiction assume m ∤ p, so r := p % m is positive (Nat.dvd_of_mod_eq_zero contrapositive). Show PeriodicFrom f r 0: periodicFrom_mul f m 0 hmP (p/m) gives that (p/m)*m is a period; one calc chain trades f(n+r) through f(n+r+(p/m)*m) = f(n+p) = f(n). That makes r a member of the period-set, so Nat.sInf_le forces m ≤ r, contradicting Nat.mod_lt (r < m since m > 0). Close with omega.

Trap worth recording precisely because it wasted one build: `omega` does NOT know `a * b = b * a` for two non-literal Nat terms — it treats `minimalPeriod f * (p / minimalPeriod f)` and `p / minimalPeriod f * minimalPeriod f` as two distinct opaque atoms, even with `Nat.div_add_mod p (minimalPeriod f)` (which produces the first form) sitting in context as a hypothesis, and the same goal needing the second form (from `periodicFrom_mul`'s conclusion, which is `m * p` with the multiplier first). The fix is a plain `rw [Nat.mul_comm a b]` on the goal BEFORE calling `omega`, to force both sides onto the same atom; omega then closes instantly since the goal becomes literally the hypothesis restated. This generalizes the cookbook's cast-normalization advice to multiplication commutativity: omega normalizes casts and linear structure, but never algebraic identities on the atoms it treats as opaque — get the syntactic forms to match with `rw`/`ring_nf`/`mul_comm` first, then hand it to omega.

Confirms (again) that `periodicFrom_mul f p N h m : PeriodicFrom f (m * p) N` puts the multiplier on the LEFT of the period, not the right — worth checking that argument order every time before writing the `have` that consumes it, rather than assuming multiplication order matches the English "a multiple of p" phrasing.

## 2026-09-08T23:50:21Z — adjacent_difference_not_eventually_one (sonnet, proved)

adjacent_difference_not_eventually_one (Rule30/Proofs/AdjacentDifferenceNotEventuallyOne.lean), proved first build (second after the push_neg->push Not swap). No original work needed: Sextant's kernel-checked theorem 3 in explorer/sextant_scratch_coboundary.lean (`sextant_adjacent_difference_not_eventually_one`) is this statement verbatim under a different name, so the whole job was reading that file, transcribing the proof body under the seeded name, and re-scoping its imports from `Rule30.Proofs` + `Rule30.Prize` (whole-project, forbidden habit for a worker file) down to `Rule30.Basic` + `Rule30.Proofs.NotIsEventuallyPeriodicAdjacent` (which itself pulls in Prize for IsEventuallyPeriodic, so nothing extra was needed).

The mathematical shape, worth keeping for any future "two adjacent columns can't do X forever" node: assume adjacent columns i and i+1 disagree at every t >= N. Case split on whether column i is ever white at or past N. If some t has evolve t i = false, the disagreement hypothesis forces evolve t (i+1) = true, and then `step` (rule30_eq rearranged, i.e. evolve (t+1) (i+1) = xor (evolve t i) (evolve t (i+1) || evolve t (i+2))) shows this (false, true) pair is a fixed point: it forces the same pair one row later, by induction on how far forward you look (`absorb`). That makes BOTH columns eventually constant, hence both eventually periodic (period 1), contradicting the served `not_isEventuallyPeriodic_adjacent`. If column i is NEVER white from N on (i.e. always true), the disagreement hypothesis directly forces column i+1 always false from N on — same contradiction, no induction needed for that branch.

Confirms (again, per ConfigEqOfRightAndColumn and RightDiagonalNotConstant notebook entries): `push_neg` is deprecated under this pin, silent warning-free replacement is `push Not` (same location syntax, drop-in).

Practical note on scratch-file reuse: when a served-lemma docstring or a notebook entry names a specific scratch/explorer .lean file as "kernel-checked" for a statement matching your brief almost verbatim, read that file FIRST before doing any proof search — the whole node can be a transcription-and-rescoping job rather than a derivation. Cost here: one Read, one Write, two builds (one to catch the push_neg warning, one to confirm the fix), one lake env lean for the axiom check.

## 2026-09-09T19:03:32Z — leftDiagonal_onset_le_of_le_5000 (sonnet, proved)

leftDiagonal_onset_le_of_le_5000 (Rule30/Proofs/LeftDiagonalOnsetLeOfLe5000.lean), proved second build (first hit "(kernel) deep recursion detected" at the `decide +kernel` call and needed `set_option maxRecDepth 100000 in decide +kernel` -- the default maxRecDepth, around 512, is nowhere near enough for a kernel-level Nat computation touching numbers with thousands of bits over 5001 cases; once raised, the whole check took under 20s).

Route: reused the two already-served bridge lemmas `leftDiagonal_eq_rowNat_testBit` and `rowNat_mod_eq_iterate` directly (both public), rather than importing anything from `LeftDiagonalOnsetLeIffRowNatReturn.lean` -- every helper lemma that file needs for this same argument (`rowNat_succ_mod_two_pow_congr`, `rowNat_mod_two_pow_eq_of_eq`, `leftDiagonal_periodicFrom_of_rowNat_return`) is declared `private` there, so it is invisible outside that file and had to be re-derived. The one new piece of bookkeeping worth keeping: a generic `iterate_eq_of_eq (f : α → α) (x) (p T) (h : f^[T] x = f^[T+p] x) : ∀ t ≥ T, f^[t] x = f^[t+p] x`, proved by `Nat.le_induction` with the step being nothing but `congrArg f` in disguise -- once two iterates coincide, applying f to both sides keeps them coinciding forever after, with no injectivity needed. This is strictly more reusable than the private `rowNat_mod_two_pow_eq_of_eq` in the served file, which hard-codes the same argument to `rowNat` specifically; any future node needing "a repeat propagates forward" for an arbitrary deterministic step function can cite this one instead of re-deriving it (though it currently lives `private` in this file and isn't served).

The seeded description names the exact closing fact (`rowNat (2*k) % 2^(k+1) = rowNat (2*k+16) % 2^(k+1)`, same constant 16 at every k) and the exact tactic (`decide +kernel`) -- both transcribed directly from `Rule30/Statements.lean`'s docstring for this node, so no search was needed for the mathematical content, only for the maxRecDepth fix. `decide +kernel` (a real, documented config flag on core Lean's `decide` tactic under this pin: it forces the kernel's own `whnf`, which has GMP-accelerated Nat arithmetic, rather than the elaborator's reduction) is confirmed working syntax -- worth remembering for any future kernel-scale `decide` on this board, together with the maxRecDepth trap.

## 2026-09-10T16:42:08Z — leftDiagonal_step_of_white_parity (opus, proved)

leftDiagonal_step_of_white_parity (LeftDiagonalStepOfWhiteParity.lean, opus, proved on the FIRST lake build, ~$2.75 of $4 with most of that spent on three wasted Edit round-trips fumbling a temporary `#print axioms` line — append to the end of the file in ONE edit next time, or just trust the harness's own axiom check).

Route: with `hwhite` the recurrence `leftDiagonal_recurrence` collapses to `x (i+1) = xor (a i) (x i)` for i >= N, with `a i = leftDiagonal m (i+2)`, `x = leftDiagonal (m+2)`. This is my own BoolXorDrivenPeriodicFrom machinery again, but the parity refinement means I could NOT cite that served lemma (it always concludes period 2p; here the whole point is that it is p when the block parity is even). Re-derived the block-XOR helpers from scratch, and they are now better than the ones in BoolXorDrivenPeriodicFrom.lean — worth grepping for `blockXor_peel` before re-deriving a third time:

- `blockXor a i : ℕ → Bool` with `| 0 => false | n+1 => xor (blockXor a i n) (a (i+n))`, plus `blockXor_succ ... := rfl`. STATE THE SUCC EQUATION AS A LEMMA AND `rw` IT. Do not `simp only [blockXor]`: with a symbolic `n+1` simp unfolds the recursive call again (`n+1` is itself a `_+1` pattern), over-unfolding and destroying the match against the induction hypothesis. `rw [blockXor_succ a i n]` with explicit args unfolds exactly one layer at exactly one site.
- `blockXor_peel : blockXor a i (n+1) = xor (a i) (blockXor a (i+1) n)` — peel off the FRONT. BoolXorDrivenPeriodicFrom.lean has no such lemma and this is the one that unlocks the new content.
- `blockXor_const : (∀ i ≥ N, a (i+q) = a i) → ∀ i ≥ N, blockXor a i q = blockXor a N q`. This is STRICTLY STRONGER than the `blockXor_shift` I wrote in BoolXorDrivenPeriodicFrom (which only gave `blockXor a (i+p) n = blockXor a i n`, i.e. i congruent to N mod p). Proof is `Nat.le_induction` on i, computing `blockXor a n (q+1)` two ways — by `blockXor_succ` (back) and by `blockXor_peel` (front) — which gives `xor B1 (a n) = xor (a n) B2` after rewriting `a (n+q) = a n`; then B2 = B1 by Bool algebra.
- `blockXor_parity : (∑ j ∈ Finset.range n, if a (i+j) = true then 1 else 0) % 2 = if blockXor a i n = true then 1 else 0`. Stating it as a `% 2` EQUATION IN ℕ rather than as an `↔ Odd ...` was the right call: everything downstream is then `omega` over `Nat.even_iff` / `Nat.odd_iff`, and no Bool-negation lemma name (`Bool.not_eq_true` etc., which I have never been able to recall correctly under this pin) is ever needed.

The tactic idiom that carried all four: `revert <ih>; generalize <bool term> = A; generalize ... = B; cases A <;> cases B <;> simp <;> omega` (or `<;> rfl` when it is pure Bool algebra). Reverting the IH first is what lets `generalize` reach the terms inside it — `generalize` only touches the goal. This is much more robust than steering `simp_all` through a nested xor tree, and it is the same "make both sides syntactically equal, then close trivially" habit as EvolveHalfLeftEqColumn.

Beta-reduction trap avoided cleanly: the helpers are generic in `a`, so instantiating at `(fun i => leftDiagonal m (i + 2))` leaves unreduced redexes like `(fun i => ...) (N + j)` inside a `Finset.sum` binder, which will NOT match the goal's `leftDiagonal m (N + j + 2)` syntactically. Fix: write the instantiation as a `have` WITH THE FULL DESIRED TYPE ASCRIBED and the term on the right — `have hpar : <goal-shaped type> := blockXor_parity (fun i => ...) N q`. The ascription forces a defeq check, which does the beta. Do not reach for `simp only [] at h` (may fail with "no progress") or `beta_reduce`.

Imports needed beyond Rule30.Basic: `Mathlib.Algebra.BigOperators.Group.Finset.Basic` (for `∑ j ∈ Finset.range n` and `Finset.sum_range_succ`) and `Mathlib.Algebra.Group.Nat.Even` (for `Nat.even_iff` / `Nat.odd_iff`). Rule30.Basic pulls in NO big-operators, so a statement with a `∑` in it always needs the first of those. Verified both module paths exist by Glob under .lake/packages/mathlib/ BEFORE building — cheap, and `Mathlib.Algebra.BigOperators.Group.Finset` (without the `.Basic`) does NOT exist under this pin.

Note: the seeded statement's `hq : 0 < q` is never used — the argument is fine at q = 0. Left the binder in (the type must match exactly) and the build carries an unused-variable warning for it.

## 2026-09-10T17:51:53Z — rowNat_testBit_zero (haiku, proved)

Route verified by Rowan and Seeder: three-line composition of two closed lemmas. Build: clean. Axioms verified by route check: propext, Classical.choice, Quot.sound only.

## 2026-09-10T19:42:41Z — leftDiagonal_periodicFrom_step_two_of_black (sonnet, proved)

leftDiagonal_periodicFrom_step_two_of_black (LeftDiagonalPeriodicFromStepTwoOfBlack.lean), proved first build. Route was straightforward composition once the index bookkeeping for the second application was worked out — worth recording precisely because the naive approach (reusing leftDiagonal_periodicFrom_step_of_black at m+1 directly) does NOT work and the fix is the interesting part.

Part 1 (diagonal m+2 gets period q from j+1) is leftDiagonal_periodicFrom_step_of_black m q N j hNj h0 h1 hblack verbatim — no new work.

Part 2 (diagonal m+3 gets period q from j+1) looks like it should be leftDiagonal_periodicFrom_step_of_black applied at m+1, but that lemma's signature wants PeriodicFrom (leftDiagonal (m+2)) q N' with N' ≤ j' where j'+1 = j+1 forces j' = j, i.e. N' ≤ j — but Part 1 only supplies onset j+1 > j. Naively you're stuck: you can't lower an onset.

The fix is to go around the packaged lemma and call `bool_driven_periodicFrom_of_reset` directly with the SAME p/N/j triple shifted by one relative to Part 1: use N (well, the "j" argument of the driven lemma) = j itself, not j+1, and supply the driver hypotheses at that lower index. The key trick: PeriodicFrom (leftDiagonal (m+2)) q (j+1), READ THROUGH A ONE-INDEX SHIFT as `fun i => leftDiagonal (m+2) (i+1)`, is periodic from onset j (not j+1) — because i ≥ j implies i+1 ≥ j+1, so the shift absorbs exactly the gap between the two onsets. That's the whole move: shifting a driver's argument by k lowers the required onset by k for free, an instance of the isEventuallyPeriodic_shift idea (Rule30.Proofs.IsEventuallyPeriodicShift) but applied ad hoc here since that lemma is about IsEventuallyPeriodic (existential), not PeriodicFrom (quantitative) with an already-fixed onset — recreating it inline with `part1 (i+1) (by omega)` plus one `rw [show i+1+q = i+q+1 from by omega]` was faster than searching for a quantitative analogue on the board.

leftDiagonal_recurrence (m+1) i unifies with the goal `x (i+1) = xor (a i) (b i || x i)` via defeq alone — (m+1)+2 and m+3 (likewise (m+1)+1 and m+2) are defeq by Nat computation, no `show`/`omega` rewrite needed on those, only on the `i + q + k = i + k + q` commutations inside the periodicity side-goals. `dsimp only` before each such `rw` was cheap insurance (matches the pattern in LeftDiagonalPeriodicFromStepOfBlack.lean) though likely unnecessary since `exact` unifies up to defeq/beta on its own.

## 2026-09-10T19:46:31Z — rowNat_return_succ_two (sonnet, proved)

rowNat_return_succ_two (RowNatReturnSuccTwo.lean), proved first build. Route: cite the closed `rowNat_return_succ_iff` with `Or.inl hb` to get mod-2^(n+2) agreement for free — no need to re-derive the bit-(n+1) case analysis it already did. The new content is bit n+2: `testBit_rowStep` at i = n+2 gives `xor (decide(2≤n+2) && r.testBit n) ((decide(1≤n+2) && r.testBit(n+1)) || r.testBit(n+2))`; with both `r.testBit n` and `r.testBit(n+1)` black, this is `xor true (true || _) = xor true true = false`, on BOTH rows independently (the second row needs its own bit-n black fact, which comes for free from `h` at index n < n+1 — see `testBit_of_mod_eq` below). So bit n+2 needs no comparison between the two rows at all; each is forced to false on its own, then `hL.trans hR.symm` glues them.

Reusable private helper worth citing again: `testBit_of_mod_eq (a b m i : ℕ) (hi : i < m) (h : a % 2^m = b % 2^m) : a.testBit i = b.testBit i`, proved by `congrArg (fun x => x.testBit i) h` then `simpa [Nat.testBit_mod_two_pow, hi] using e` — this is exactly the forward direction of the private `mod_two_pow_eq_iff` in RowNatReturnSuccIff.lean, which is `private` there and so not importable; re-derived here in three lines rather than fight visibility. This is now the second time this exact three-line lemma has been re-derived privately in this tier (the first was inside mod_two_pow_eq_iff itself) — if a third node needs it, it might be worth proposing as a served node in its own right, since it composes with `Nat.eq_of_testBit_eq` to close any "extend agreement by one bit given the new bit agrees" argument, which is the whole shape of this family.

Also confirmed again (third time in this tier's notebooks): plain `by simp` reliably discharges `decide (k ≤ n + c) = true` for a Nat literal k and a variable-plus-literal n + c, and `simp [hi, e]`/`simp [hi]` reliably discharges `decide (i < m) && X` goals when `hi : i < m` (or its negation) is in the local context — matches the closed `mod_two_pow_eq_iff`'s own two case branches exactly, so this pattern is now confirmed across two separate proof files under this pin.

No cast arithmetic needed at all (everything is ℕ throughout, no ℤ or absolute values) — this node is entirely in the packed-row / testBit vocabulary, which stayed cheap.

## 2026-09-10T20:19:23Z — periodicFrom_gcd (sonnet, proved)

periodicFrom_gcd (Rule30/Proofs/PeriodicFromGcd.lean), proved first build. Route: a private `periodicFrom_sub_of_le f p q N (hp) (hq) (hle : p ≤ q) : PeriodicFrom f (q-p) N` — one line of reasoning: for n ≥ N, hp at (n+(q-p)) gives f(n+(q-p)+p) = f(n+(q-p)), and n+(q-p)+p = n+q by omega (using hle so the Nat subtraction doesn't truncate), so f(n+q) = f(n+(q-p)); combined with hq n hn : f(n+q) = f(n) that's f(n+(q-p)) = f(n), the goal. No induction needed for this piece — it's the single-step Euclidean subtraction, generalized to work for the whole multiple q/p*p at once rather than just p.

Main lemma is `private periodicFrom_gcd_aux f N : ∀ p q, PeriodicFrom f p N → PeriodicFrom f q N → PeriodicFrom f (gcd p q) N`, proved by `intro p; induction p using Nat.strong_induction_on with | _ p ih => intro q hpN hqN; ...`. Doing `intro p` BEFORE the induction (rather than intro p q hpN hqN together) is what makes the motive `∀ q, PeriodicFrom f p N → PeriodicFrom f q N → PeriodicFrom f (gcd p q) N` — i.e. q, hpN, hqN are still inside the goal being inducted on, so `ih` comes out already universally quantified over a fresh q' with fresh hypotheses, with no need to fight `generalizing` clauses or manually revert hpN/hqN (which would otherwise be awkward since hpN depends on the induction target p itself). This pattern — intro only the induction variable, leave everything that depends on other still-to-be-introduced variables inside the goal — is worth remembering any time a strong induction needs to quantify over an unrelated second variable in its IH.

p = 0 case: `Nat.gcd_zero_left` gives gcd 0 q = q, so `simpa [Nat.gcd_zero_left] using hqN` closes it directly (hqN already has the right type up to that rewrite).

p > 0 case: `rw [Nat.gcd_rec]` turns the goal into `PeriodicFrom f (gcd (q % p) p) N` — confirmed `Nat.gcd_rec (m n : ℕ) : gcd m n = gcd (n % m) m` by grepping actual USES in Mathlib (Data/Nat/Fib/Basic.lean's `← gcd_rec m n`, Data/ZMod/Basic.lean's `Nat.gcd_rec n m`) rather than guessing — this is exactly the cookbook's advice generalized: when a *usage site* is grep-able even if the *declaration* isn't (core Lean lemmas aren't vendored under .lake/), the call-site pins the argument order just as well as the source would. Then: `periodicFrom_mul f p N hpN (q/p) : PeriodicFrom f (q/p*p) N` (served lemma, multiplier on the left per its own established convention — checked again here, still true), `Nat.div_mul_le_self q p : q/p*p ≤ q` (confirmed by grep of actual Mathlib call sites, e.g. Regularity/Bound.lean), apply `periodicFrom_sub_of_le` to get `PeriodicFrom f (q - q/p*p) N`, then rewrite that to `PeriodicFrom f (q%p) N` via `Nat.div_add_mod q p : p*(q/p) + q%p = q` (confirmed by grep, e.g. LucasLehmer.lean's `(Nat.div_add_mod k (2^n)).symm`) plus one `rw [Nat.mul_comm (q/p) p]` to align the multiplication order before `omega` closes the Nat-subtraction identity — the now-familiar trap (per MinimalPeriodDvd's notebook entry) that omega treats `a*b` and `b*a` as different opaque atoms and needs a manual `mul_comm` rewrite first. Finally `ih (q%p) (Nat.mod_lt q hp0) p hsub hpN` — `Nat.mod_lt (x) {y} (h : y > 0) : x % y < y`, confirmed by grep of a call site (`mod_lt _ n.succ_pos` in Data/Nat/Totient.lean) — closes the goal exactly.

Practical technique worth reusing generally: when a Lean4-core (not Mathlib-source) lemma name is needed and the toolchain source isn't vendored under `.lake/packages/` to grep directly (as Selvage's own earlier notebook entry on Bool xor already flagged), grep Mathlib's *usage sites* of the name instead of guessing blind — a call site like `Nat.div_add_mod k (2^n)` pins both the name's existence and its exact argument order just as reliably as reading the declaration would, and costs zero build cycles versus a wrong guess. Did this for `Nat.gcd_rec`, `Nat.div_mul_le_self`, `Nat.div_add_mod`, `Nat.mod_lt` all at once before writing a single line of the real proof, and the whole file built clean on the first `lake build` as a result.

Confirmed `Nat.eq_zero_or_pos` (core, no import beyond Rule30.Basic needed) still works as expected for the case split, matching its use elsewhere on this board.

## 2026-09-10T20:25:44Z — rowStep_agree_succ_two_iff (sonnet, proved)

rowStep_agree_succ_two_iff (RowStepAgreeSuccTwoIff.lean), proved second build. Route: private testBit_rowStep and mod_two_pow_eq_iff, transcribed verbatim from RowNatReturnSuccIff.lean/RowNatReturnSuccTwo.lean but working directly on generic x, y : ℕ rather than rowNat T, rowNat (T+p) -- rowStep is a pure Nat function, so nothing about rowNat or the picture is needed. The mathematical content: under hb (x.testBit n = true, hence also y.testBit n = true via hlow n), bits 0..n of rowStep x/y agree automatically (testBit_rowStep at i ≤ n only reads bits ≤ n, all in the agreement window from h), and bit n+1 ALSO agrees automatically -- testBit_rowStep at i = n+1 puts x.testBit n / y.testBit n (both true) in the OR slot, which forces `true || _ = true` on both sides regardless of the higher bit, so the xor's second argument is literally `true` on both sides and the first argument (decide(2≤n+1) && x/y.testBit(n-1)) is literally identical after using hlow at n-1 -- no case split on n needed at all. That gives mod 2^(n+2) agreement unconditionally. The one genuinely conditional bit is n+2: testBit_rowStep there puts x.testBit n (=true) in the XOR's first slot and (x.testBit(n+1) || x.testBit(n+2)) in the second, so bit n+2 of rowStep x is exactly `xor true (x.testBit(n+1)||x.testBit(n+2))`, i.e. the negation of that OR -- and the negations agree iff the ORs agree, which is the goal's RHS verbatim.

Two traps that cost a build each, both about `rw` vs `simp` on schematic lemmas: (1) `rw [Bool.true_and]` (or any lemma with a metavariable, e.g. `a && true`/`true && a`) only rewrites the FIRST unifying occurrence it finds, not every occurrence of the pattern with different instantiations -- so `rw [h1, Bool.true_and, hb, hbn]` silently only fixed the x-side copy and left the y-side `true && y.testBit (...)` untouched, three errors downstream. Fix: `simp only [...]` for anything with a metavariable (it rewrites everywhere); reserve `rw` for fully closed/concrete equalities like `show n + 2 - 2 = n by omega`, which ARE textually the same term on both the x-side and y-side subterms and so `rw` does hit both at once. (2) `simp` will silently renormalize a Nat subtraction expression like `n + 1 - 2` to `n - 1` on its own before a `have e2 : x.testBit (n + 1 - 2) = ...` gets a chance to match, producing an "unused simp arg" warning and leaving the goal one level deeper than expected (with an extra `decide (1 ≤ n) || !x.testBit (n - 1)` shape from simp unfolding testBit_rowStep's `i - 1`-referencing subterm further because n+1-2 collapsed to n-1 which is small enough for simp to keep unfolding). Fix: `rw [show n + 1 - 2 = n - 1 by omega]` explicitly to the SAME normal form simp would reach, and state the `have` in that already-normalized form (`x.testBit (n - 1) = y.testBit (n - 1)`), so it lands as a literal simp-set hit rather than racing simp's own arithmetic normalization.

For hlt (bits < n+1), the working by_cases order is OUTER on `2 ≤ i`, not on `1 ≤ i`: in the innermost false/false branch you need BOTH `¬2≤i` and `¬1≤i` visible to `simp` to collapse both `decide` terms to `false` -- splitting on `1 ≤ i` first and only introducing `2 ≤ i` inside the true branch leaves the false/outer branch (i=0) with no `h2` in scope and an unsolved `decide (2 ≤ i) && x.testBit (i-2) = decide (2 ≤ i) && y.testBit (i-2)` goal that `simp [h1, e0]` alone cannot close.

## 2026-09-10T21:27:27Z — column_neg_one_damage_mask (sonnet, proved)

column_neg_one_damage_mask (ColumnNegOneDamageMask.lean), proved first build, cost under $1 of $4. Band: Nothing -- this is pure Boolean bookkeeping once rule30_eq is unfolded at the origin for both pictures; true of any pair of Bool-valued pictures obeying that one recurrence, and the node's own DOES NOT PROVE says as much (unconditional, rules nothing out by itself).

Route: two `have`s (stepX, stepY) each derived by `unfold column; rw [evolveFrom_succ]; have heq := rule30_eq (evolveFrom X t) 0; simp only [Int.zero_sub, Int.zero_add] at heq; exact heq` -- this is verbatim the pattern ColumnSuccOfBlack.lean and ColumnOneOfWhite.lean already used for the same 0-1/0+1 -> -1/1 normalization at the origin (sideways_inverse there, rule30_eq here; same simp lemma pair works for either). Then `rw [stepX, stepY, h0] at h1` collapses the two step equations and the hypothesis into a single Bool equation in five variables with the two `column _ 0 t` occurrences unified via h0, `rw [h0]` does the same unification in the goal, and `revert h1; generalize` on each of the five atomic terms (column X (-1) t, column Y (-1) t, column Y 0 t, column X 1 t, column Y 1 t) turns the whole thing into a closed Prop about five Bool variables. `cases A <;> cases A' <;> cases B <;> cases C <;> cases C' <;> revert h1 <;> decide` closes all 32 branches -- reverting h1 before `decide` is the trick: `decide` does not consult hypotheses, so turning the branch into a closed implication `h1-branch -> goal-branch` (both sides fully concrete Bool literals at that point) is what makes `decide` sufficient, avoiding any reliance on `simp_all`'s ability to detect a contradictory concrete hypothesis.

Worth reusing: `unfold column; rw [evolveFrom_succ]; have heq := rule30_eq (evolveFrom C t) 0; simp only [Int.zero_sub, Int.zero_add] at heq; exact heq` is now confirmed in three separate files (ColumnSuccOfBlack via sideways_inverse, and this one via rule30_eq directly) as the standard way to get the origin's one-step recurrence into `column`-vocabulary with clean -1/1 indices instead of 0-1/0+1. And "revert the equation hypothesis before `decide`" generalizes past this file: any node reduced to a handful of Bool variables plus one equational hypothesis among them can skip `simp_all`'s heuristics entirely by reverting into a closed decidable Prop.

## 2026-09-10T21:32:02Z — centerColumn_not_eventually_constant (opus, proved)

CenterColumnNotEventuallyConstant.lean, proved on the FIRST `lake build`, ~$2 of $4. Band: Known — a corollary of Jen 1986 via the board's own `not_isEventuallyPeriodic_adjacent`, which is what both halves end at.

The mathematics, worth keeping because the P1 residual keeps circling this pair of columns:
- **Centre black for ever ⇒ column -1 white for ever.** `rule30_eq` at i = 0 reads `evolve (t+1) 0 = xor (evolve t (-1)) (evolve t 0 || evolve t 1)`; with the centre black the OR is `true`, so the xor is a negation and column -1 is pinned. Columns -1 and 0 are then both constant ⇒ both `IsEventuallyPeriodic` ⇒ `not_isEventuallyPeriodic_adjacent (-1)`.
- **Centre white for ever ⇒ column 1 is MONOTONE.** Same lemma at i = 1: `evolve (t+1) 1 = xor (evolve t 0) (evolve t 1 || evolve t 2)`, and a white centre kills the xor, leaving `evolve (t+1) 1 = evolve t 1 || evolve t 2`. Once black, black for ever. So `by_cases` on whether column 1 is ever black past N: yes ⇒ constant true from there (one `Nat.le_induction`), no ⇒ constant false from N. Either way columns 0 and 1 are both eventually periodic ⇒ `not_isEventuallyPeriodic_adjacent 0`. **This monotonicity is the reusable find** — it says a white centre column makes the right neighbour a one-way ratchet, and it is the only place I have seen rule 30 degenerate to a monotone map. It has no mirror on the black side (the xor turns the OR into a negation) and no analogue at period 2, so do not try to iterate it.

Four technique notes, all confirmations of things already in this notebook, plus one new one:

1. **NEW — `const_periodic`, worth grepping for before re-deriving.** `private theorem const_periodic (f) (b) (N) (h : ∀ t, N ≤ t → f t = b) : IsEventuallyPeriodic f := ⟨1, by omega, N, fun n hn => by rw [h (n+1) (by omega), h n hn]⟩`. Three lines, no lemma names, and any argument that ends at `not_isEventuallyPeriodic_adjacent` needs it twice. `by omega` closes the `1 > 0` side-goal, so no `Nat.one_pos` spelling to get wrong.
2. **`(0:ℤ) + 1` in the goal, `1` in your hypothesis: rewrite under the binder rather than trusting defeq.** `not_isEventuallyPeriodic_adjacent i` asks for `IsEventuallyPeriodic (fun t => evolve t (i + 1))`, so at i = 0 and i = -1 you get `evolve t (0+1)` and `evolve t (-1+1)`. Both ARE defeq to `evolve t 1` / `evolve t 0` by Int computation, and `exact` would probably take them, but `rw [show (0:ℤ)+1 = 1 from by omega]` on the *goal* works fine under the `fun t =>` binder (the index has no bvar in it, so `kabstract` finds it) and costs nothing. Did this at all three sites and the file built first try; I would not have known which one failed otherwise.
3. **`cases hct : e with | false => rfl | true => exact (hcon ⟨t, ht, hct⟩).elim`** is the clean way to turn `¬ ∃ t ≥ N, f t = true` into `∀ t ≥ N, f t = false` without ever naming a Bool-negation lemma (`Bool.not_eq_true`, which I have repeatedly failed to recall under this pin) and without `push Not`. The named split rewrites the goal to `true = false` in the bad branch, which is why `.elim` rather than `exact`. `⟨t, ht, hct⟩` elaborates against `∃ t ≥ N, centerColumn t = true` even though `hct` is about `evolve t 0` — the defeq `centerColumn t = evolve t 0` is unfolded by `exact`'s check, so no `show`/`unfold` is needed anywhere in this file despite the statement being in `centerColumn` vocabulary and the whole proof being in `evolve` vocabulary.
4. **`revert h1; generalize <term> = A; generalize <term> = B; cases A <;> cases B <;> decide`** closed both Bool-algebra steps, confirming the ColumnNegOneDamageMask entry. Reverting first is what lets `generalize` reach the hypothesis; `decide` does not consult hypotheses, so the branch must be a closed implication by the time it runs.

One generic helper I stated and would state again: `private theorem step_at (t : ℕ) (i : ℤ) : evolve (t+1) i = xor (evolve t (i-1)) (evolve t i || evolve t (i+1)) := by rw [evolve_succ, rule30_eq]` — one line, and then each position is `have h := step_at t 1; rw [show (1:ℤ)-1 = 0 from by omega, show (1:ℤ)+1 = 2 from by omega] at h`. Cleaner than the `simp only [Int.zero_sub, Int.zero_add]` idiom I recorded earlier, because it generalises to positions other than the origin (that simp pair only normalises `0-1` and `0+1`).

## 2026-09-10T21:48:02Z — centerColumn_periodic_neg_one_black_times (sonnet, proved)

centerColumn_periodic_neg_one_black_times (CenterColumnPeriodicNegOneBlackTimes.lean), proved first build. Band: Project-internal -- the hypothesis is the negation of Prize 1 (a repeating centre column), so nothing here can ever be witnessed and it proves nothing about rule 30 itself; it is machinery for a hypothetical contradiction argument against P1, not a fact about the automaton.

Route: cited centerColumn_succ_of_black (t : ℕ) (h : centerColumn t = true) : centerColumn (t+1) = !evolve t (-1) twice -- once at t (using hb directly) and once at t + p (using hbp := (hc t ht).trans hb, i.e. hc gives centerColumn(t+p) = centerColumn t and hb says that's true). Then hc (t+1) (by omega) gives centerColumn (t+1+p) = centerColumn (t+1); one `rw [show t+1+p = t+p+1 from by omega, h2, h1] at h3` chains the index-commute with substituting both succ_of_black instances in a single rewrite, landing h3 as `!evolve (t+p) (-1) = !evolve t (-1)`. Closed with `revert h3; cases evolve (t+p) (-1) <;> cases evolve t (-1) <;> simp` rather than guessing a `Bool.not_inj`-style lemma name -- the now-familiar "revert then case-split into a closed decidable goal" idiom from several other P1 notebooks (ColumnNegOneDamageMask, CenterColumnNotEventuallyConstant) generalizes cleanly to "cancel `not` on both sides of a Bool equation" too, and cost nothing to verify versus a lemma-name guess that might have failed.

One thing worth flagging for whoever reads this tier's board entry: the node's own DOES NOT PROVE says the seeder's route was "truncated mid-tactic" -- I did not go looking for that partial route before writing this from scratch, since the shape (two centerColumn_succ_of_black instances plus one hc application) is short enough that re-deriving it directly was cheaper than reconstructing a truncated transcript. If a future prover hits a similarly-described "truncated route" node, re-deriving from the served-lemma docstrings is often faster than hunting for the abandoned partial file.

## 2026-09-10T21:57:02Z — white_run_monotone (haiku, proved)

**Route verified:** Simple three-step tactic proof using served lemma `column_one_succ_of_white`, which gives the recurrence when centre is white. The rest is rewriting with the hypothesis and Bool simplification.

**Key lemma:** `column_one_succ_of_white X t h : column X 1 (t + 1) = (column X 1 t || column X 2 t)` when `h : column X 0 t = false`. This encodes rule30_eq at position 1 under a white centre, collapsing the XOR to transparency.

**Tactic pattern:** `rw [lemma_name X t h, hypothesis]` followed by `simp` to close Bool goals. This is the standard move for "white run" proofs on the white-time side.

**Axiom check:** Inherits only propext, Classical.choice, Quot.sound from dependencies. Clean.

## 2026-09-10T22:02:21Z — centre_forced_after_double_white (sonnet, proved)

centre_forced_after_double_white (CentreForcedAfterDoubleWhite.lean), proved second build. Band: Known-mechanism/project-internal — the 0*1* forcing pattern is Meier-Staffelbach 1991 per the brief; this is its Lean statement, plus the harness's own coverage measurement (not re-verified by me).

Route: a local `step : ∀ s i, column X i (s+1) = xor (column X (i-1) s) (column X i s || column X (i+1) s)` proved by `unfold column; rw [evolveFrom_succ, rule30_eq]` (Selvage's own `step_at` pattern from CenterColumnNotEventuallyConstant, generalized from `evolve` to `column X`/`evolveFrom X`). Then three applications: `step t 0` with h0/h1/hm substituted forces `column X 1 t = true` (centre white at t, t+1 with column -1 black at t leaves only "column 1 must be black" to make the xor balance); `step t 1` with h0 and that fact forces `column X 1 (t+1) = true` (the `true || _` absorbs whatever column 2 does); `step (t+1) 0` with h1 and that fact reduces the goal to `xor (column X (-1)(t+1)) true = !(column X (-1)(t+1))`, closed by cases.

Two traps worth recording precisely because each cost a build: (1) `revert h; cases <term> <;> decide` FAILS with "Expected type must not contain free variables" the moment casing on `<term>` leaves some OTHER local hypothesis/goal still mentioning a different free local (here: cases on `column X 1 (t+1)` while `column X 2 t` still sits inside the reverted hypothesis) — `decide` checks syntactically for free variables BEFORE attempting kernel reduction, so it refuses even though `true || column X 2 t` would reduce to `true` regardless of that variable's value (Bool.or pattern-matches on its first argument only, so `rfl` DOES go through on such a term with a dangling free var — `decide` and `rfl` are not interchangeable here, `rfl` is strictly more permissive when reduction doesn't need the free variable's value). Fix: isolate the offending free variable into its own tiny lemma (`have hor1 : (true || column X 2 t) = true := by cases column X 2 t <;> rfl`) and `rw` it away before ever reaching a `decide`. (2) `rw [e1]` at the end of a `have` does NOT always auto-close via its trailing `rfl` — `xor false true = true` needed an explicit `decide` after the `rw`, even though other `xor`/`||` literal computations elsewhere in the file (the very last line) close fine under `cases <;> decide` alone. No consistent shortcut found for when rw's auto-rfl suffices vs needs a following `decide`; just try both.

Confirms Selvage's own `step_at`-style helper (rewrite `column`/`evolveFrom` one step via `evolveFrom_succ` then `rule30_eq`, closing by rfl automatically) generalizes cleanly from `evolve` to an arbitrary `column X`, no `Config`-specific friction.

## 2026-09-10T22:05:52Z — rowStep_agree_succ_two_of_triple (sonnet, proved)

rowStep_agree_succ_two_of_triple (RowStepAgreeSuccTwoOfTriple.lean), proved first build, near-zero cost. Band: project-internal — no bound, no prize conjecture; it is the +2 case of a family whose ceiling is +2 (refuted at +3 by the node's own sibling rowStep_forced_advance_at_most_two).

This was pure transcription, not derivation: Sextant (theorist) had already kernel-checked this exact statement in explorer/sextant7_scratch_forced.lean, including three private helpers (testBit_rowStep, mod_two_pow_eq_iff, rowStep_mod_two_pow — all now familiar from RowStepAgreeSuccTwoIff.lean and RowNatReturnSuccTwo.lean too, this is at least the third time this trio has been re-derived privately across files under different names) plus a #print axioms line confirming the clean set. I copied the theorem body verbatim, re-set the private helper names to match this file's local scope, and it built clean on the first try with no edits needed to the math.

Confirms the by-now-standard pattern for "agreement on low n+1 bits, one control bit tells you what happens next": mod_two_pow_eq_iff turns the % 2^m goal into a ∀ i < m, testBit statement; testBit_rowStep unfolds rowStep's XOR/OR structure per-bit via Nat.testBit_xor/Nat.testBit_or/Nat.testBit_two_pow_mul; then each new bit index is closed by rw-ing in the known testBit facts and finishing with simp. The one new wrinkle versus RowStepAgreeSuccTwoIff: the hypothesis set here directly supplies concrete Bool values (hb, hw, hy, hr) for every testBit that matters, rather than deriving an OR-equality first, so the closing tactic per case is a straight rw-chain into simp with no generalize/cases dance needed.

Practical note worth flagging for whoever indexes scratch files against DAG nodes: when a node's description says "KERNEL-PROVED IN SCRATCH BEFORE LANDING (explorer/<file>.lean)", read that file FIRST — it is very often the entire job, exactly as Selvage's own RightDiagonalNotConstant and AdjacentDifferenceNotEventuallyOne notebook entries already established for theorist/connector handoffs generally.

# Portage

I am Portage, the connector for this project: the one who carries Rule 30's center column over to another field, sets it down beside whatever that field calls an object like it, and writes the dictionary. Each entry is a row that says "this here is that there," followed by the seam where the resemblance stops, because a dictionary without seams is a misconception waiting to be believed. I produce sightings, never proofs; a row of mine is a claim that someone elsewhere has already worked on this shape, with a pointer good enough to check, and it becomes a theorem only when a prover carries it back and lake build agrees. I read the web and I log every URL, so the trail of where an idea came from is part of the record rather than something to reconstruct later. I write for Dib, who thinks in TypeScript and pictures the automaton as rows falling down a page, so each row I write should be readable by him alone, without the paper it came from. When I am wrong it will usually be because two objects shared a name and I mistook the name for the object, and that is the failure this notebook exists to catch.

## 2026-09-08T12:30:16Z — named for connect

A portage is the stretch of ground where you carry the boat from one water to the next because no channel joins them. That is the connect region exactly: Rule 30's center column sits in one body of mathematics, and the objects that resemble it (symbolic dynamics, algebraic curves over finite fields, automatic sequences, Kolmogorov complexity) sit in others, and the work is hauling the same object across dry land and writing down row by row what survived the carry and what did not. The name fits the roster's habit of navigational things (Keel, Fathom, Sextant, Cairn) without being a job title, and it keeps the honest limit built in: a portage is a route, not a crossing already made, so nothing carried this way counts as a proof.

Colour: #5f7f8b — A slate-teal that sits between the blue of water and the grey of stone, the two surfaces a portage passes over.

## 2026-09-08T13:06:56Z — centerColumn_other_isEventuallyPeriodic_of_center from profinite dynamics: odometers, binary rooted tree automorphisms, and the in-degree-one pair map on period-L settled words (opus, sighted)

The vantage I was handed named three objects — odometers, binary rooted tree automorphisms, the in-degree-one pair map — and all three live on the left side of the picture, which is the side where the residual does not live. I spent the first hours on them anyway and should have spent twenty minutes. The fact that closed them is one line and I could have derived it in the first hour: a fixed column *leaves* the settled region rather than entering it, because the seam runs at x = -0.25t, so column -X is settled only while t < 4X. Every profinite structure in the settled words is therefore about a region no column stays in. Obstruction 1 says this for diagonals; nobody had said it for columns and I nearly built a connection on the assumption that deep-left columns were eventually periodic.

I should have looked at the right diagonals sooner — within the first hour, not the fourth. The whole payload of the document came from turning around: on the right there is no transient at all, the recurrence solves as a running XOR, and c(t) = rightDiagonal t 0 sits inside the periodic part rather than inside a transient. That is the mirror of the board's central obstruction and it was reachable from the definitions in Basic.lean alone, without any field of mathematics. The Iwasawa/F_2[[T]] language was worth carrying — division by T, unit iff doubling, socle iff complement — but it was decoration on an identity I could have found by telescoping.

Two things I was wrong about, both caught by running rather than by thinking. First, I believed docs/sources.md that the right diagonals have period exactly 2^k, built an odometer-conjugacy claim on it, and the injectivity test collided at m = 2; the true periods are A094605 and P_39 = 2^16. That is the exact failure my notebook predicts — I took the project's own index line as the object and it was a paraphrase of Rowland that had drifted. Second, my first parity test over the right diagonals returned EVEN for every k, which looked like a clean negative result and was a normalisation bug: I summed over 2^(k-1) instead of over the word's own minimal period, and summing a period-L word over 2L is always even. I nearly wrote that up as a death. Distrusting the check that said no is what saved it.

Resemblances that died and where: Aut(T_2) on the tree of left sides, at the seam that there is no tree (one path, obstruction 5). Aut(T_2) on the rightward continuation tree, at the seam that the pruning is a damage front and not a group action. Cobham, at the seam that eventual periodicity implies automaticity and I needed the converse. Iwasawa mu and lambda, at the seam that the OR destroys the module structure and only the filtration survives. Bratteli-Vershik, at the seam that the tower's arrows are injections and both candidate surjections fail to commute (0.149, 0.407, measured exhaustively). Skew products over Z/p, at the seam that the map carries its own support rightward at speed 1 and has no compact invariant set.

## 2026-09-08T21:21:52Z — centerColumn_other_isEventuallyPeriodic_of_center from cocycles and coboundaries over an odometer: Anzai skew products, the coboundary equation, and whether the transient part of the telescoped centre-column sum in explorer/portage_odometer.mjs section C is a cocycle over the settled part (opus, sighted)

I was handed the vantage my own last session proposed, and the thing I got wrong is that I spent the first hours looking for the cocycle and only found, late, that the interesting object was the *base*. The base is the right edge read leftward, and one rule-30 step there is `w ^ ((w<<1)|(w<<2))` — a triangular map, so an automorphism of the binary rooted tree, so purely periodic orbits with power-of-two periods and no transient, for free. I killed `Aut(T_2)` twice last session, on the tree of left sides and on the tree of rightward continuations, and both times I was looking at a tree the picture does not have. The tree the picture does have is the one whose levels are depths from the right edge, and I should have found it in the first hour by asking "what is autonomous here?" rather than "what resembles an odometer?". Better still, the automaton has only three states, not four: tails `01` and `11` give the same section, and I only saw that because I printed the grouping instead of trusting my count.

What died, and at which seam. Keane's generalized Morse sequences, at the seam that the antiperiodic block `u·ū` really is produced at every doubling but the blocks do not nest — `w_{n+1}` is not `w_n·w̄_n` from level 2 up, and its first half is not even a shifted or complemented `w_n`, so there is Morse raw material and no Morse sequence. Schmidt's essential values, at the seam that "coboundary iff bounded Birkhoff sums" is vacuous over a `Z/2` fibre, and I could find no `Z`-valued lift that is a cocycle over anything. `H^1(odometer; Z/2)`, at the seam that it is `Z/2` per level — one bit where the wall needs `t`. The settled region as a base, at the seam that it is *universal*: identical across five configurations while the centre columns are not, which is a two-line argument I ran as a measurement first. The centre column as a Toeplitz sequence, at `0/2000`. And the wreath-product route past obstruction 7, at the seam that group theory bounds the common period and never an individual minimal one — the obstruction explains itself in the new language rather than yielding to it.

The measurement failure to remember: my first Toeplitz test said `2000/2000`, a perfect pass, and it was measuring the number of confirmations, not the sequence — periods to 8,192 against depth 60,000 gives seven confirmations per candidate and a coin passes at nearly every position. At depth 200,000 with a confirmation floor it is `0/2000`. My notebook says I fail by mistaking a name for an object; this was the other failure, mistaking a denominator for a result, and it swung from 2000 to 0. The control was wrong too: bit `k` of an LCG modulo `2^32` has period `2^(k+1)` and passes the Toeplitz test. Two bad numbers in one test, one of which I liked. Also caught, late: I called the right-edge array a "Toeplitz array" and it is not one in the fetched definition's sense, where periodicity is indexed by position and not by depth — exactly the name-for-object error, caught only on the final read.

Next time: ask what part of the picture is *autonomous* before asking what part *resembles* the vantage's object. The answer here was one line of bit arithmetic and it reorganised the whole document.

## 2026-09-12T16:33:26Z — centerColumn_other_isEventuallyPeriodic_of_center from Contracting self-similar groups and Nekrashevych limit spaces, aimed at one explicit object: the three-state invertible Mealy automaton E over two letters that generates the right-diagonal tower, q0 = (q0,q2), q1 = sigma(q0,q2), q2 = sigma(q1,q2). Portage left this as a bounded, decisive task in its second sighting (docs/connections/2026-09-08-cocycles-and-coboundaries-...): (1) build the nucleus of the group generated by q0,q1,q2 and decide whether it is contracting -- that is a finite computation in explorer/ with node, not a literature search; (2) identify this automaton in the published classification of 3-state 2-letter automata, arXiv:0803.3555, the 194 minimal ones; (3) if it is contracting, rule 30 acquires a compact limit space nobody has looked at, and the question to ask there is what the diagonal t -> (E^t delta)_t means, because that diagonal IS the centre column by Portage 3.1 identity centerColumn t = e(t)_t. Known already and not to be re-derived: the group is isomorphic to Z, E is NOT level-transitive (order 256 at level 14 while the seed orbit is 64), so E is a thin element. Craft warning from this region: PDFs and many publisher pages will not fetch from a guarded session -- 403s and undecodable binary are the norm, a search summary is not a quote, so prefer computing to fetching wherever the object is finite. (opus, sighted)

Two of the three things the brief told me were "known already and not to be re-derived" were wrong, and one of them was mine. "The group is isomorphic to Z" came from my own second sighting, where I wrote `⟨E⟩ ≅ Z` meaning the cyclic group generated by one element, and the brief read it as the group generated by the automaton. Those are different groups: `⟨q0,q1,q2⟩` is non-abelian with growth ratio 4.15. That is exactly the name-for-object failure this notebook exists to catch, committed in a handoff I wrote, and it cost me nothing only because I distrust "known already" lines on principle now and checked it in the first twenty minutes. The habit to keep: verify the handed-down premises of a bounded task before executing the task, because a bounded task is precisely where nobody re-checks.

The second wrong premise — that the classification tables could not be reached — was a fetching failure, not a fact. PDFs return binary and ar5iv fails on the 2008 paper, but ar5iv succeeds on the two *earlier* papers, and the earlier one carries the numbering formula and a worked example. I had reverse-engineered the convention from one example and was ready to mark it UNVERIFIED; the formula then arrived and confirmed all nine digits. Lesson: when the canonical source will not fetch, fetch its predecessors — a formula that a later paper uses is usually defined in an earlier one, and old arXiv papers convert one-by-one rather than all-or-nothing.

The field I should have looked at sooner is not a field, it is the board. The whole payload — m(t) = 3,7,16,35,71,…,526563 being obstruction 21's automaton size at all 20 values — fell out in the first hour of computing, and I spent two more hours on group invariants (relations, growth, level images, Hausdorff dimension) that cannot see the residual by my own §3.4 argument. I knew after the strong-connectivity check that every invariant of the automaton is shared by every configuration in crystal 40's family, and I kept computing invariants of the automaton anyway.

Resemblances that died and at which seam. Contracting self-similar groups, at the seam that every state of E is non-trivial so the activity is 2^n and the nucleus exceeds 526,563. The limit space, at the seam that finite topological dimension holds iff the nucleus is finite, so there is no weakened fallback. The virtual-endomorphism shortcut (contraction iff |m| < 2), at the seam that it needs the group to be Z. The dual automaton as a bireversible object, at the seam that q0 and q1 share a section at 0, so the dual is not invertible. A rational generating function for m(t), at the seam that no linear recurrence of order ≤ 8 fits and the ratios fall steadily. And |s_t| as a statistic separating the seed from a periodic centre column, at the seam that E^t's minimal transducer is strongly connected, so every section has the same size and the statistic is blind by construction.

One measurement nearly fooled me in the direction my notebook does not yet warn about. Nine configurations gave byte-identical |s_t| sequences, including the all-white one, which is exactly what a script that ignores its input produces. It was right, and the reason it was right (strong connectivity) also means the sweep would have looked identical had I built the configurations wrongly. The check that rescued it was a *different* quantity — c(t) = w_t XOR χ(s_t) against each boundary. So: when a sweep returns identical values across inputs, the confirming check must be of something the mechanism does not force.

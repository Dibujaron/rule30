# Parallax

I am Parallax, third of the connectors, and I work by standing somewhere else. Rule 30 is one object; symbolic dynamics, algebraic automata theory, pseudorandomness, additive combinatorics each already own something shaped almost like it, and my job is to build the dictionary row by row and then say plainly which rows are false. The seam is the deliverable. A row that maps cleanly is worth one line; a row where the other field's theorem needs a hypothesis Rule 30 does not satisfy — linearity, an invariant measure it has not been shown to have, a two-sided shift where we have a light cone — is worth the page, because that hypothesis is a name for what is actually hard here. I will be tempted, often, to write that a known theorem "essentially gives" one of the prize conjectures; the discipline of this region is that I do not get to write that, ever, because a sighting is not a proof and the distance between them is the thing I was hired to measure. Portage and Rosetta have crossed this ground before me and I read them as prior observations, not as settled facts — where I see what they saw, that agreement is itself a measurement, and where I do not, one of us has moved.

## 2026-09-09T00:03:15Z — named for connect

A connector's whole method is to look at one object from a second position and measure how far it appears to move. Parallax is the name for exactly that: the star does not shift, the observer does, and the apparent displacement is not an error to be corrected away — it is the only thing in the observation that carries distance. That is the shape of this region's work and, more importantly, of its honesty. When I lay Rule 30's center column beside a Z² action's expansive directions, or beside a linear-feedback sequence, or beside a Bernoulli measure, the rows that line up are cheap; the row that refuses to line up is where the two fields actually differ, and that seam is the finding. Portage names the carry between two waters and Rosetta names the tablet where two scripts say the same thing; I want the name for what you learn from the fact that they do not quite. A sighting, never a proof — parallax gives you a distance and never touches the star.

Colour: #2f4b7c — The blue of a sky gone just dark enough that two stars can be measured against each other — late enough to see the displacement, early enough that the background is still there to measure it against.

## 2026-09-09T00:41:59Z — centerColumn_other_isEventuallyPeriodic_of_center from algebraic and automatic: Christol's theorem, algebraicity of generating functions over F_2, k-automatic and k-regular sequences, and the ideal generated by the quadratic rule. Rosetta established that rule 30 IS rule 150 plus the single quadratic monomial c*r, and that the residual is FALSE for the linear rules 90 and 150 — so everything turns on one monomial and the algebraic structure is concrete rather than metaphorical. Two questions, and the second is aimed at a different prize from the one this project usually attacks. (1) The IDEAL: what does the ideal generated by the T relations y(t+1,x) + y(t,x-1) + y(t,x) + y(t,x+1) + y(t,x)y(t,x+1) look like over F_2, is the residual's hypothesis (centre column periodic) a linear slice of that variety, and is elimination in the right order the sideways solve? (2) The AUTOMATIC question, which nobody here has raised: Christol's theorem says a sequence over F_q is q-automatic exactly when its generating function is algebraic over F_q(x). Is the centre column automatic, or k-regular for some k? It is surely not, but WOULD NON-AUTOMATICITY BE PROVABLE, and what would it give — automatic implies computable in logarithmic space, so a proof of non-automaticity is a statement in the direction of Prize 3 (computational irreducibility) rather than Prize 1. Say plainly if Christol makes this a question about algebraicity of a specific power series and whether that series is one anybody can write down. Two things you must not reach for: the rule 30 edge group is NOT contracting (nucleus >= 175680, controls clean), so no self-similar-group machinery is available; and the wall centerColumn_other_isEventuallyPeriodic_of_center is EQUIVALENT to Prize 1, not a reduction of it, so do not offer a decomposition of it as progress. (opus, sighted)

The thing I was wrong about was the shape of the brief's second question, and I was wrong about it for the first hour. I took "aimed at a different prize" at face value and started building the dictionary as though non-automaticity were a sidestep from Prize 1 towards Prize 3. It is not: every eventually periodic sequence is automatic, so a proof of non-automaticity contains Prize 1 whole. One fetched sentence — "it is not hard to show that each eventually periodic sequence is automatic in every base" — turned the whole vantage from a detour into a strictly harder target, and it is the single most useful line in the document. I should have checked the direction of the implication before writing a word, and the general lesson is that when a brief hands me a hoped-for direction, the first thing to measure is the arrow.

The field I should have looked at sooner is Boolean-function cryptanalysis, and not for the reason I expected. I reached for it late, as a decoration on the ideal question, and it produced the only object in the document that looks like an ordinary theorem: the algebraic degree of the centre cell is exactly 2t−1 with a unique top monomial. I nearly did not run it because it is a statement about the family and the wall is about a point — and that seam is real and I wrote it down — but "this cannot touch the wall" is not a reason not to measure, it is a reason to label the measurement honestly. Two hours earlier and I could have pushed to t=13.

Resemblances that died, and the seams. "Rule 30 is nonlinear, hence not algebraic" died at the Hadamard product: algebraic series over F_q are closed under it, and rule 30's own pictures have eventually periodic columns, so the rule cannot forbid what one of its pictures does. The linear complexity profile died at a fetched theorem — automatic sequences have Nth linear complexity of order N, the largest possible — so the statistic I expected to separate rule 30 from an automaton separates nothing; that one cost me nothing because I fetched before I computed, which is the right order and I should keep it. Subword complexity as a falsifier died on the quantifier: p(n) ≤ Cn allows any C, so a finite sample bounds C from below and never refutes the bound. That last death is the shape of the whole session: every route to non-automaticity is Π⁰₂ and the engine only ever certifies Σ⁰₁, and I spent real time before naming that, when naming it early would have organised the document.

Two process notes. Controls carried this session entirely — Thue-Morse at 2, the two linear columns at 4, a xorshift at full saturation — and without them the 130,553 would have been a number with no denominator. And I caught the session's one bad number by looking at it rather than by testing: 66 then exactly 132 at n=16, which reads as a doubling and was a 31-bit overflow. The tell was the exactness. Distrust a clean ratio the way this project distrusts a "no".

## 2026-09-09T01:16:43Z — centerColumn_other_isEventuallyPeriodic_of_center from Gowers uniformity norms and the analytic number theory of digital sequences: Mauduit-Rivat's method for sums of digital functions along primes and squares, Konieczny's Gowers norms of automatic sequences, van der Corput and carry-propagation estimates. This is your own handed-on vantage and it is aimed at PRIZE 2 (the centre column is balanced, equal 0s and 1s asymptotically), not Prize 1 — this project has never attacked Prize 2 and that is deliberate here. That field's business is PROVING, not measuring, that an explicitly defined 0/1 sequence has no correlation with structure: Rudin-Shapiro orthogonal to polynomial phases, Thue-Morse balanced along the primes, automatic sequences having small Gowers norms unless they have an obvious structured factor. Those are theorems of exactly the shape Prize 2 wants. Three things you must carry in. Rule 30 IS rule 150 plus the single quadratic monomial c*r (Rosetta), so the seam is that the field's machinery is built around a LINEAR carry structure and that monomial is what destroys it — finding exactly where the linearity is used is the job, not a caveat. Your own result last session: non-automaticity of the centre column is STRICTLY STRONGER than Prize 1, floor 130,553 states, so do not route through automaticity. And Kopra's dictionary between the picture and the base-p digits of p^n alpha is the one thing that makes rule 30 look like a digital sequence at all — start there. Say plainly whether any theorem in that field would survive the loss of linearity, or whether the whole method needs it. (opus, sighted)

I was wrong about which prize my vantage would reach. The brief aimed me at Prize 2 and I spent the first hour assuming the wall was out of range, until I wrote down that gamma(r) = 1 exactly when the column is eventually p-periodic — so the same autocorrelation family that gives Prize 2 by van der Corput gives Prize 1 by staying below 1. After that the vantage was pointed at the wall the whole time, and the session's best result came from the Prize-1 side. The lesson is the same shape as last session's: measure the arrow before writing a word. Last time it was the direction of an implication; this time it was whether my vantage could reach my target at all.

The field I should have looked at sooner is symbolic dynamics of expansive directions — Kopra's own (h,d,w) parameters — and I should have looked at it first rather than fourth. The brief told me to start at Kopra and I did, but I read him for the digital-sequence dictionary and only later noticed that his three parameters are a *budget*: s < 1/h with s = 1 forces h = 0, h = 0 gives only w = 2 from left permutivity, and the missing case is exactly Prize 1. Once I saw that, the decisive computation took twenty minutes and was exhaustive over every configuration in the universe, because the cone makes the column word depend on a finite window. Two hours earlier and I could have pushed the depth further and tried the compensating construction by hand.

Resemblances that died and where. The structured-plus-uniform decomposition died at the seam I most wanted to hold: the board has an explicit candidate structured factor, the settled centre column, and it agrees with the real column at rate 0.509 — a coin — so the decomposition moves everything into the residual and gains nothing. The Fourier period certificate died on arithmetic: it scales as max|uhat|^-2 = N, the same order as a direct period scan with a worse constant, giving 43,516 where the seed's own factor complexity already gives 499,949. Mauduit-Rivat along primes died on an axis I did not expect — in that field "balanced along N" is the two-line easy step and the theorems are about sparse sets, so the field's starting line is past this project's finish line.

Two process notes. Controls carried the session again: Thue-Morse reproducing Gelfond's exponent to two digits and Rudin-Shapiro reading exactly N^-1/2 are what let me believe the FFT, and without them "rule 30 looks random" would have been a number with no denominator. And I caught my own bad number the way my notebook told me to: a block density of exactly 0.2500 was too clean, and it was a picture that had run off bit 0 and lost the left half of its cone. Distrust a clean ratio. That is twice now, and both times the tell was exactness rather than a failed test.

## 2026-09-09T02:01:57Z — centerColumn_other_isEventuallyPeriodic_of_center from thermodynamic formalism of one-dimensional lattice systems: transfer operators, Gibbs measures, g-measures, Bowen's condition and the pressure function. This is your own handed-on vantage and you named exactly why it is the right one, so start from your own diagnosis rather than re-deriving it: every route in your last document hit the same wall from four directions — the field's theorems describe an ENSEMBLE (a measure) and the prize is about a POINT (the seed), and almost-everywhere results say nothing about one specified orbit. The thermodynamic formalism is the one branch of dynamics whose business is manufacturing statements about individual orbits from an ensemble: a Gibbs measure with enough regularity gives every point of a full set the frequency statistics of the measure. Two things you identified as already on this board and should use rather than look for: the 1/4 density of the c*r firings is exactly a potential function's expectation, measured at every scale; and window_count_half together with the four-preimages structure is what would supply bounded distortion its constant. The specific questions. Is rule 30's picture a Gibbs state for a Holder potential? If so, is the seed's column generic for it? You already know the obstacle and must not rediscover it: the seed is a NON-GENERIC point by construction, being the boundary of the cone, so genericity for the maximal-entropy measure needs a limiting empirical measure for the transient band, which nothing on the board addresses. Say plainly whether specification or bounded distortion can be established for the de Bruijn / preimage structure that Fuks's paper and window_count_half describe, and if the honest answer is that the seed can never be shown generic, say that and say what it costs — because a proof that the ensemble route is unavailable IN PRINCIPLE is worth as much to this project as a route. (opus, sighted)

I was wrong about where the degeneracy would be. I went in expecting the thermodynamic formalism to be BLOCKED — that specification or bounded distortion would fail for rule 30's preimage structure and that the failure would be the finding. The opposite is true: bounded distortion holds with constant exactly 1, specification holds trivially, every hypothesis is satisfied in its most degenerate form, and the conclusions are empty because of it. Hedlund's balance theorem, the thing that makes the machinery applicable, is the thing that makes it useless — and Fukś's own paper draws the same line, saying the interesting preimage sequences all belong to the non-surjective rules. The lesson: when a brief asks "can hypothesis H be established", check whether H holding is good news before spending the session trying to establish it.

The field I should have looked at sooner is algorithmic randomness, and I reached it fourth instead of first. Every route in my last document and three of the six here end at the same place — an a.e. statement and a named point — and the exact form of that wall is a fetched theorem: Schnorr randomness IS typicality for every computable mixing dynamics (Gács–Hoyrup–Rojas), and a computable point is never Schnorr random, by a one-line singleton test. That single sentence organises the whole document and would have organised it from hour one. Two sessions running I have now written "the field's theorems are about ensembles and the prize is about a point" as though it were a mood; it is a theorem and I should have gone looking for it the first time I wrote the sentence.

Resemblances that died and where. The settled region as a statistical fingerprint died on the density: 0.2498952 against the ensemble's 0.25, in a region whose structure is in closed form — first-order statistics cannot see determinism, measured on a case where determinism is known. It took P''(0) to see it (0.198 against 0.3125), which is the only reason that connection exists. The susceptibility as a direct attack on the wall died on Jen: the sandwich lemma needs TWO periodic columns and Jen already excludes that conjunction, so the argument refutes only what is already refuted — I had written the overreaching version into the document and caught it on re-reading. The number-like class as entropy-deficient died on the same ambiguity the board already recorded: I added six data points from 12 to 18 and separated n^2.1 from 2^0.24n by exactly nothing.

Process, and this is the part that stings. My notebook's two standing warnings are "distrust a clean ratio" and "a picture that ran off the edge of its own cone", and I committed BOTH in one session, in one script. Section C came out as exactly n+1 — impossibly clean — because I packed the row with the origin at bit 0 and left cell(-1) white for ever; and after repairing it the counts collapsed to 2 at n=16 because the mask (1 << 33) wrapped. Neither was caught by a test. Both were caught by writing a slow, obviously-correct referee and comparing against a number the board already held. The referee is the instrument, not the test, and I should write it FIRST next time rather than after the fast version disagrees with the board. What did work, again: controls. Rule 184 returning exactly one conservation law and rule 204 returning everything is what let me believe the zero for rule 30, and the i.i.d. and random-row controls are what let me believe 0.315 was a match rather than a coincidence.

## 2026-09-12T17:08:22Z — centerColumn_other_isEventuallyPeriodic_of_center from Amenability and random walks on automaton groups -- Kaimanovich, Bartholdi-Virag, Grigorchuk-Zuk -- pointed at a walk that reads its own labels. Portage third sighting (docs/connections/2026-09-12-contracting-self-similar-groups-*.md) section 3.4 produces this object and could not take it further: rule 30 centre column IS the root-label sequence of a SELF-DRIVING walk in an explicit 3-generated self-similar group G, the walk being s_{t+1} = u_t . (s_t restricted to 0), where the step u_t is chosen by the walk own output rather than by an i.i.d. draw. The field that studies walks on automaton groups has, as far as Portage could tell, never been pointed at a walk that reads its own labels, and that is the whole question: which theorems of that machinery survive when the step distribution is replaced by the walk own label sequence. TWO CONCRETE THINGS TO CARRY: the recursion itself, verified 0 failures to t = 13; and the fact that G is NOT contracting -- nucleus at least 526,563 elements -- which is precisely the hypothesis most of that machinery assumes, so the FIRST question is which theorems survive without contraction. CHECK BEFORE LEANING ON ANY OF THIS: the previous brief in this lineage asserted two things as settled that turned out to be false when Portage checked them -- that G is isomorphic to Z (it is not; non-abelian, growth ratio about 4.15) and that the automaton could not be located in the published enumeration (it can; number 5002, symmetry-orbit minimum 2369). Treat every inherited claim, including the two in this paragraph, as a claim with an author rather than as a fact. What is NOT worth a session, by Portage own fence: the limit space, the virtual endomorphism, and the group growth, presentation or Hausdorff dimension -- all are invariants of the automaton, the automaton is shared by every configuration in crystal 40 family, so none can reach P1. Aimed at Prize 1. Craft warning for this region: PDFs and publisher pages mostly will not fetch from a guarded session, 403s and undecodable binary are normal, a search summary is not a quote, so prefer computing to fetching wherever the object is finite. (opus, sighted)

I was wrong about the shape of the object, twice, and the second time I had it as the document's headline for an hour. First: I expected the self-driving walk to be AUTONOMOUS — that s_t would determine its own next step and hence the whole future centre column — and it is false; over every level-t vertex the section fails to determine the last output digit from t = 7, rising to about 10% at t = 16, and the bit it must import is column 1, which obstruction 9 says is the entire content of the wall. That failure is the best sentence in the document: the group element forgets precisely the bit the wall is about. Second, and worse: I measured that the positive monoid is free, exactly 3^t distinct products to t = 8, and wrote "so the walk is its own step word and the group adds nothing" as the short version. The number is true and the inference is false, because the walk's word is built from SECTIONS of its steps and q0|_0 = q1|_0 = q0, so distinct step sequences share positions — R(8) = 51 against 3^8 = 6561. What caught it was asking what the 3^t had been measured over, which is the one question my notebook already tells me to ask, and I asked it late.

The field I should have looked at sooner is the one I was standing in: I should have asked "does this walk branch?" in the first ten minutes. s_t = E^t|_{delta|_t} depends on t alone; that is Portage's own §3.1 identity, it was in front of me, and everything else in the session — which theorems survive contraction, the Munchhausen hypotheses, Kesten, the Schreier spectra — was downstream of a question that the identity answers in one line. Four hours of machinery to reach "there is no measure", when the handed-on formula says it.

Resemblances that died and where. Rotor-router walks, the one published field about a deterministic walk reading its own trail, died on the geometry rather than a hypothesis: the theory lives where the walk returns and a free-monoid trajectory never does. |s_t| as a separating statistic died harder than Portage killed it — i.i.d. step sequences, including non-realisable ones, give the seed's numbers to within 0.5% and exactly from t about 13 — with a mechanism I can state: all R(t) reachable positions have transducer sizes inside a band of width 8. Word length died the same way: |s_t|_word = t exactly, the walk is geodesic at maximal speed, and so is every control. "Aperiodic steps implies aperiodic labels", the only implication that would have given P1, died at b = (10)^inf, which is already on the board.

Three process notes. R(t)'s first eight values read as t^2/1.3 and are 1.30^t — I nearly wrote "polynomial" and was saved by running the range out, which is the only thing that kills that shape. The controls carried the session again: the coin-driven section chain returning (0.2502, 0.2499, 0.4999) is what let me believe the seed's (0.2492, 0.2504, 0.5004) meant something, and the two deliberately-collapsed automata returning 2-of-3 and 1-of-3 are what let me believe the 7-of-9 that killed the cheap freeness route. And a cross-check I did not go looking for: the <q0>-orbit of delta at level n reproduced the board's right-diagonal minimal periods exactly, from code sharing nothing with the diagonal engine.

## 2026-09-12T17:35:44Z — centerColumn_other_isEventuallyPeriodic_of_center from Arithmetic dynamics and CYCLE EXCLUSION: Baker theorem on linear forms in logarithms, Steiner theorem on 1-cycles of the 3x+1 map, Simons-de Weger on m-cycles. Your own next vantage, proposed at the end of your amenability sighting of this afternoon, and the reason you gave is the reason to take it: FOUR of your sightings have now ended at the same wall from four directions -- the field theorem holds for almost every point and the prize is about ONE NAMED POINT -- and the cause is always that the field object is a measure. Arithmetic dynamics cycle-exclusion is the one literature whose theorems are about one explicitly given orbit of one explicitly given map, and whose conclusion is exactly this orbit is not periodic and here is why not: it turns the orbit returns into an exponential Diophantine equation and bounds its solutions. THE BOARD ALREADY HAS THE VOCABULARY such an argument needs, and it is not the group vocabulary: leftDiagonal_onset_le_iff_stepMod_return puts Prize 1 neighbourhood into the truncated row map T_n(r) = (4r XOR (2r OR r)) mod 2^n, an explicit map on n-bit words whose orbit of 1 is the picture and where eventually periodic is literally the orbit returns. THE QUESTION TO CARRY: does any technique that excludes cycles of an arithmetic map survive the replacement of + by XOR and * by OR? Your own guess is no, and you said the guess is worth a document because the answer NAMES WHAT THE NONLINEARITY COSTS. Hold yourself to that: a negative here is the deliverable, and it is only worth having if it says which step of the 3x+1 argument dies and why, rather than that one does. TWO WARNINGS FROM THE BOARD, both to check rather than accept. Obstruction 17 says the T-map view has THREE TIMES produced a regularity that turned out to be the doubling staircase in disguise, so measure where the analogy breaks BEFORE arguing why it holds. And CLAUDE.md warns at length that the left-diagonal region is the tractable downhill direction away from the prizes -- leftDiagonal_onset_le_iff_stepMod_return is a left-diagonal node, so if this vantage pulls you toward left-diagonal statements rather than toward the centre column, that is the trap and not a discovery. TREAT EVERY INHERITED CLAIM IN THIS BRIEF AS A CLAIM WITH AN AUTHOR RATHER THAN AS A FACT, mine included: a brief I wrote earlier today asserted two things as settled that were false, and only the session checking anyway caught them. NOT WORTH A SESSION, by your own fence and Portage: the limit space, the virtual endomorphism, the group growth or presentation or Hausdorff dimension, and anything downstream of a measure. Craft: PDFs and publisher pages mostly will not fetch from a guarded session, 403s and undecodable binary are normal, a search summary is not a quote, prefer computing to fetching wherever the object is finite. (opus, sighted)

I was wrong about the shape of the negative, and wrong in the direction that mattered. I expected several steps of Steiner's argument to fail and expected the archimedean step — the size of the orbit, where all the difficulty lives in 3x+1 — to be the blocked one. Exactly one step fails (step 1, the multiplier), and the archimedean step is not blocked but FINISHED: theta_t = log2(r_t) - 2t is purely periodic to every fixed precision, period exactly the right-diagonal minimal period, no transient, 0 failures in 1426 comparisons. A vantage cannot be rescued by solving a step that was never the obstruction, and that sentence is the best thing in the document. I was also wrong, for an hour and in a draft, that rank of the Jacobian measures nonlinearity: rule 86 (nonlinear, my object's mirror) is full rank at every t and rule 60 (linear) collapses to rank 0. Constancy of the Jacobian is the linearity statistic — 1 versus exactly 2^n — and rank is a permutivity-orientation statistic. Two controls, not a proof, caught that.

The field I should have looked at sooner is the one I named in section 6 and reached at hour two by accident: diagonals of two-index arrays. Every death in this document, and in the four sightings before it, is the same seam — the field owns the array or the trajectory's values or a fixed coordinate, and the prize owns a read whose coordinate moves one place per step. Derksen owns the zero set of a fixed coordinate; heights own the point; cycle exclusion owns the values. I found that by grinding through Skolem-Mahler-Lech, and it would have predicted every single death in advance, in ten minutes, from the definition c(t) = bit_t(rowNat t). Four sessions I have now written some version of "the field's object is not the prize's object" as though each instance were news. It is one fence and I should have written it down the first time.

Resemblances that died and at which seam. The literal transport died at the first measurement: bitlen(rowNat t) = 2t+1 exactly, so the orbit is strictly increasing, so "the orbit does not return" is two lines and the whole literature's conclusion is free. The board's own arithmetic node died at the second: in a finite set every orbit returns, so leftDiagonal_onset_le_iff_stepMod_return is a TAIL bound, not a cycle question — the brief's warning about the left-diagonal trap was exactly right. Skolem-Mahler-Lech died on a quantifier rather than a hypothesis: at a fixed digit position the orbit IS eventually periodic (it is a left diagonal), so Derksen's theorem applies and describes an eventually periodic set, and its success would have failed too. Canonical heights died at the degree: h(T r) = h(r) + 2 is exact, better than the field ever gets, and still d = 1 where the construction needs d >= 2. The Liouville route died against its null, with two of three coins more extreme than the seed.

Process, and both notes are repeats. My coin null returned linear-recurrence order 33 at every width; I called it a solver bug and wrote a slow referee that recovers the coefficients and verifies them, and the referee AGREED — 0 failures in 3632 checks. xorshift32 is an F_2-linear map on 32 bits, so its output satisfies an order-32 recurrence, and 33 is Cayley-Hamilton. My notebook records the ancestor (2026-09-07, an LCG whose low bit alternates) AND records that I fixed it by adopting xorshift32 — so my own fix for the old instance caused the new one. A generator that passes every statistical test is worthless as a null for a linearity test, and the general rule is that a null must be nonlinear in whatever structure the test is looking for. Second repeat: my right-diagonal cross-check read bit i of row i+j, which is position -j, a LEFT diagonal. The tell was not a wrong number but a missing one — null periods and NaN maxima printing as bare commas. When reading a two-index array along a line, write the index identity down before writing the loop; that is three instances now. And I put alpha = 0.8713 in the document from a mental estimate when the value is 0.86239; I computed it only because I went back to check a number I had asserted, which is the one habit that worked.
